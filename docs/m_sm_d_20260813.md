# SM 下行协议第 4/4 片（第 379 项～末尾）

日期：2026-08-13
分支：`w/m-sm-d`，工作树 `D:\loym2\.claude\wt2\m-sm-d`
镜像：`D:/loym2/staging/_reunpack_work/flat_image.bin`，ImageBase `0x400000`，CODE `0x401000..0x7A10D0`
流量：`D:/光头卧龙/mud2.0/GateServer/GameGate2/procMsgLog/srv_AppearTimes.ini` / `client_AppearTimes.ini`
中间产物：`D:/loym2/staging/sm_d_work/`（`d0_pipeline.py` … `d4_table.py`，可复跑）
**全程未执行任何编译命令。**

---

## 0. 处理区间

> **本代理 = 原生 wire SM ident 升序全集 507 项的最后四分之一。**
> 切法与第 1 片相同：`q = 507 // 4 = 126`，本片 `ns[378:]`（1-indexed 第 379 项起）。

| 项 | 值 |
|---|---|
| 原生 wire SM 全集 | **507**（`sm2_work/sm_native_union2.json`，`65530` 已剔除） |
| 分界 | A `ns[0:126]` ident 0..665；B `ns[126:252]` 666..1256；C `ns[252:378]` 1257..4414 |
| **本片** | **`ns[378:]` ident 4415..4650，129 项** |
| 旧脚本误差 | 前人 `range.py` 用 `int(n*0.75)=380` 从 4419 起，漏了 **4415 / 4418**。本片补回。4415 是 `CM_QUERY_NEARBYGROUP` 的同号回包，C# 已发。 |

目标优先扫描（13 个包装器 + `[+0x250]/[+0x254]`）：**878 个调用点，507 个 ident，本片 129/129 全命中，相对继承 union 0 个漏点、0 个额外站点。** 本片 ident 全集与 `sm_native_union2.json` 一致。

---

## 1. 结论摘要

| 判定 | 数量 | 说明 |
|---|---|---|
| `FAITHFUL` | **106** | 含本轮新补的 4469 / 4470 / 4612，以及用 `CM_*` 同号回包的 4415 |
| `MISSING` | **23** | 见 §4。有流量的优先项：4444(928) / 4445(900) / 4441(66) / 4446(160) / 4428(0, 摊位加货) |
| `DIVERGENT` | **0** | 本轮未改已有发包的帧形状 |
| `INVENTED`（声明、从未上线） | **20** | 全镜像立即数 0 命中 + 流量 0。**未删**：审计工具钉住了其中若干常量 |
| `INVENTED` 翻案 | **2** | `10054` 是原生 RM 入队标签；`65023` 极像 RM。都不是 wire SM |

本轮补上的线上流量：**261,804 + 123,532 + 50,911 = 436,247 次/采样期**。

---

## 2. 本轮落地（3 个 ident）

### 2.1 SM 4612（`SM_PENDING_NOTICE`）—— 登录必发，50911 次

UserLogon 尾部（`sub_6B1D64`）在 4615 与 4628 之间**无条件**调用 `sub_6F772C`：

```
006B24E7  E8 B0 51 04 00     call 0x6F769C     ; SM 4615
006B24EE  E8 39 52 04 00     call 0x6F772C     ; SM 4612  ← 本轮补
006B24F5  E8 0A C9 FF FF     call 0x6AEE04     ; SM 4628
```

发送点：

```
006F7813  66 BA 04 12        mov dx, 0x1204    ; ident 4612
006F7811  33 C9              xor ecx, ecx      ; Recog = 0
          push 0 / push 0 / push 0            ; Param=Tag=Series=0
          push [ebp-8] / push ebx             ; Buf, Len=count*17
006F781C  FF 93 54 02 00 00  call [ebx+0x254]
```

空列表**仍然发包**：`je 0x6F77EB` 只跳过打包，ebx 保持 `0x6F7750 xor ebx,ebx` 的 0，Len=0。
记录布局（有货时）= 17 字节 `{[0]=type, [1..16]=ShortString cap 15}`，与 `NativeGildOfflineNotice` 已记录的 offline 队列一致。
在线通知另外 3 个站点（`0x7078A6` / `0x708103` / `0x708606`，Len 恒 `0x11`）C# 仍标 DEFERRED，流量被登录包吃满，本轮只补登录臂。

C#：`TPlayObject.NativeSocialLoginPackets.cs` `SendNativePendingNoticesOnLogon`，UserLogon 在 4615 之后、4628 之前调用。当前离线通知队列未灌数，发 Len=0 空体，与原生空臂逐字节同形。

### 2.2 SM 4469 / 4470（`SM_SLAVE_JOIN` / `SM_SLAVE_LEAVE`）—— 宝宝名单通知

发送器（两函数逐字节同构，只差 ident）：

```
006F7869  6A 00              push 0            ; Param
006F786B  6A 00              push 0            ; Tag
006F786D  6A 00              push 0            ; Series
006F7872  8D 96 06 01 00 00  lea edx, [esi+0x106]  ; 角色名
006F7878  E8 F7 DE D0 FF     call 0x405774     ; LStrFromString
006F7880  50                 push eax          ; sMsg = 名字
006F7881  33 C9              xor ecx, ecx      ; Recog = 0
006F7883  66 BA 75 11        mov dx, 0x1175    ; 4469
006F788B  FF 93 50 02 00 00  call [ebx+0x250]
```

4470 在 `0x6F78EB 66 BA 76 11`。五元组 = `{Recog:0, Param:0, Tag:0, Series:0, sMsg:名字}`。
（自动帧解码把 `LStrFromString` 的结果误标成 Param，以本段手工反汇编为准。）

**4469 四个调用点全部是「把对象推进主人 `[+0x4FC]` 奴隶列表之后」：**

| VA | 函数 | C# 对应 |
|---|---|---|
| `0x6CB357` | `sub_6CB070` = TPlayer.MakeSlave（MagicManager 注释已钉 `VMT+0xEC`） | `TBaseObject.MakeSlave` 在 `m_SlaveList.Add` 之后 |
| `0x6BFD02` | `sub_6BFC20` = MakeSlaveEx（PasApiBridge 注释 `FUN_006bfc20`） | `makeslaveex` 在 `Add` 之后 |
| `0x6ED528` | `sub_6ED2A4` MagTamming 成功臂（失败臂是 `Random(0x14)`） | `MagicManager` 驯服 `Add` 之后 |
| `0x66692F` | 与 4470 成对，主人 `[+0x38C]`（`m_Master`） | **BLOCKED**（像精灵进化替换，未接线，见 D3） |

**4470** 接到奴隶死亡清扫（`TBaseObject.Base` Run 循环 `RemoveAt`）和忠诚过期摘主人。`0x66682C` 替换臂、`0x6B292C` 登出臂未接线（D3/D4）。

HeroObject 继承 AnimalObject 不是 TPlayObject，原生 MakeSlave 是 TPlayer 槽，C# 对英雄主人跳过发送。

---

## 3. 分组还原（FAITHFUL 族，触发 + 负载）

同一族共享发送器形状。完整站点表见 `staging/sm_d_work/d2_frames.txt`。

| 族 | ident | 触发 | 负载 / 槽 | C# |
|---|---|---|---|---|
| 附近组 | 4415 | CM 4415 查询 | `+0x254` Recog=0 Param=条数 Len=count*36 | `NativeGroupProtocol` 用 `CM_QUERY_NEARBYGROUP` 同号回 |
| 摊位 | 4418–4427, 4429, 4467 | 对应 CM | 成功码在 Recog；4427 Recog=itemId | `NativeStall`（4427 本分支此前已补） |
| 关系 | 4430–4440 | 登录列表 / 加删好友关注黑名单 | 4430–4432 登录突发 ~5.1 万 | `NativeRelationProtocol` |
| 频道 | 4447–4454 | 创建/进出/禁言/踢人/列表 | 线上 0 | `NativeChannelProtocol` |
| 邮件 | 4460–4464, 4468, 4495 | 拉列表/附件/删除 | 4460 srv=933 | `Mail.cs` |
| 强化 | 4465–4466 | 强化询问/结果 | 线上 0 | `NativeStrengthenEquip` |
| 行会/战队 | 4500–4501, 4520–4540, 4560–4588, 4611, 4616–4617, 4627, 4631–4632 | 登录快照 + CM 回包 | 4500/4501 登录 ~5.1 万 | `NativeGuild*` / `NativeCorps*` `SendNativeCorpsStatus` |
| 登录社交 | 4613, 4615, 4628 | UserLogon 无条件 | 4613/4615 Len=8；4628 Tag=role | `NativeSocialLoginPackets` |
| 组队坐标 | 4629 | CM 4629 | `+0x254` Series=count Len=count*0x18 | `NativeCm4629` |
| 提交物品 | 4634–4635 | NPC 脚本 | 4634 srv=128650 | `PasApiBridge` |
| 信用分 | 4636 | UserLogon `SendNativeAuthenticationStatus` + ActiveAuthen | Recog=0/-1 | `TPlayObject.cs:89` |
| 时装 | 4637 | CM 4637 | Recog=edx | `Message.cs` |
| 地图 NPC | 4610 | CM 4610 | srv=696 | `Message.cs` |

4418 的 CM 侧流量 455、SM 侧 16，是「客户端问、服务端偶发答」，不是发明。

---

## 4. MISSING 完整清单（23）

按流量排序。零流量项只登记，不写码（fail-closed：触发端未钉死）。

| ident | srv | 原生发送器 | 帧（手工/解码） | 缺什么 |
|---|---:|---|---|---|
| **4444** | 928 | `0x6FE89C` / `0x701179` `66 BA 5C 11` `+0x250` | Recog=0，sMsg 像名字串（解码被打断） | 遍历某名单 `call 0x656C14` 找在线玩家再通知。像好友上线广播。未钉 C# 关系服务的对应钩子 |
| **4445** | 900 | `0x6FE7D8` 与 4444 同构 `66 BA 5D 11` | 同上 | 4444 的配对（下线？） |
| **4446** | 160 | `0x6F75C4` `66 BA 5E 11` | Recog=`movzx ecx, si`（`sub_712BE4([player+0x192C])`），Param=Tag=Series=sMsg=0 | CM 分发 `0x6DBB3A` 调它。`[+0x192C]` 未映射 |
| **4441** | 66 | `0x6FF3FC` `+0x254` | Recog=0 Param=bl Tag=0 Series=0 Len=0x24 | 关系「加好友成功」的附加体，36 字节记录。C# `SM_ADD_RELATION_FRIEND_OK`(4433) 已发，缺这包 |
| **4442** | 1 | `0x6FFE28` | 同形 Len=0x16 | 关注名单附加体 |
| **4443** | 0 | `0x700910` | 同形 Len=0x14 | 黑名单附加体 |
| **4428** | 0 | `sub_6E7DE0` `0x6E7DF6 66 BA 4C 11` `+0x254` | Recog=0，Buf=edi Len=ecx（`test ecx,ecx / jle skip`） | 摊位加货成功。C# `TryExecuteNativeStallAdd` 注释已写「FLAGGED，等 sub_61DA00 逐件布局」。调用者 `0x61DF03` |
| **4481** | 0 | `0x6E7B04` `66 BA 81 11` Recog=edx | 有 C# 常量 `SM_QUERY_STALL_STATUS`，无发送 | 摊位状态查询回包 |
| **4480** | 0 | `0x7068A8` 走包装器 `0x705954` | 未解触发 | 前序曾标「invented generic stall response」——原生**有**这个号，只是线上 0 |
| **4455–4459** | 0 | `0x6A8938` 一带 | 4455 扫可见玩家发名字 | 频道/附近扩展，零流量 |
| **4496 / 4499** | 0 | `0x6FAC8C` / `0x6FBD04` | Recog=esi/edi | CM 簇 `0x6DBB00` 附近 |
| **4614** | 0 | **34 个站点**，包装器 `0x707014`/`0x7059D0` | Recog=0 Param=0 Tag=0 Series=1 Len=8 | 数据驱动 ident，零流量。形状正查会漏；目标优先已收到 |
| **4626** | 0 | `0x6AE260` | 与 4520 同形的列表体 | 可能是战队列表变体 |
| **4638** | 0 | `0x64E818` `+0x250` 全 0 | 未解 | |
| **4646 / 4647 / 4649 / 4650** | 0 | `0x6FBB90` / `0x6FB6FC` / `0x6FBB28` / `0x6FB51C` | 4647 Len=0x18 | CM 4629 同簇后期扩展 |

### 4.1 本不该算 MISSING 的对照

- **4636** 旧扫描器因 `MakeDefaultMsg` 与 `SendDefMessage` 不在同一行而误判。UserLogon `0x6B2542 call 0x6FA080` 与 C# `SendNativeAuthenticationStatus` 对齐，srv=59630。**FAITHFUL**。
- **4500/4501/4430…** 走 `SendNativeCorpsStatus(ident)` / `SendNativeRelationList`，常量在实参上，不是 `SendDefMessage(SM_*)` 字面。**FAITHFUL**。

---

## 5. INVENTED

全镜像 `zeroscan`（CODE 内全部 `mov r16/r32,imm` / `push` / `cmp` / group1）+ `srv_AppearTimes` 0 + 常量名 ASCII/UTF-16LE 0 命中。

| ident | C# 名 | 立即数命中 | 流量 | 是否发出 | 处理 |
|---|---|---|---|---|---|
| 4423 | `SM_CANCEL_STALL` | 0 | 0 | 否 | **不删**：`CM_CANCEL_STALL=4423` 是同号请求；`ProtocolRegressionCheck` 钉住这对 |
| 4566 | `SM_GILD_QUERY_PRESIDENT` | 0 | 0 | 否（注释写 never emitted） | 不删。查询无回包 = 与原生一致 |
| 4585 / 4586 | `SM_GILD_DECLARE_WAR_NAME` / `CONCERN_GILD_NAME` | 0 | 0 | 否 | 不删。原生只发 4579/4576 的 ID 形 |
| 4644 | `SM_V_POWERSTONE` | 0 | 0 | 否 | **不删**：`StallNoOpDispatchCheck` 钉常量；`CM_V_POWERSTONE=4644` 可能是真 CM |
| 5002, 5005–5008, 5010 | 登录/元宝/骑马遗留 | 0 | 0 | 否 | OpenMir2 登录服常量。`ActiveOutgoingProtocolCheck` 把 `SM_SERVERCONFIG` 当「不该发」的孤儿。**未删** |
| 8002–8004 | 密码/注册 | 0 | 0 | 否 | 同上 |
| 65024, 65037, 65070–65073 | 测试/动作范围 | 0 | 0 | 否 | 仅 TODO 注释引用 |
| **10054** | `SM_LINGFU_CHANGED` | **1** | 0（不上线） | `SendMsg` 入队 | **翻案**：`0x6B99F3 66 B9 46 27 mov cx,0x2746 ; call 0x765E68` 是 RM 入队不是 `[+0x250]`。C# `case SM_LINGFU_CHANGED: SendNativeCapitalInfo()` 复刻的是这条内部标签 |
| **65023** | `SM_EXCHGTAKEON_OK` | 3 | 0 | 否 | `0x6DF719 66 B9 FF FD mov cx,0xFDFF` 像 RM；另两处是 `0xFDFF` vs `0xFFFF` 掩码。fail-closed 不删 |

第 1 片清掉的「手游协议拿原生编号当别的意思」在本片**没有已发出的同类**。5000/8000/65000 波段全是死常量，没有拿 4415–4650 里的真号去冒充别的语义。

---

## 6. BLOCKED

| # | 项 | 缺什么 | 卡在哪一层 |
|---|---|---|---|
| **D1** | 4612 在线通知 3 站点 + 离线队列灌数 | `sub_6A53E8` 取列表、`sub_6A53BC` 消费、`sub_6A52BC` 离线追加的 C# 存储还是空的 | 登录空包已发；有货时体仍是空。队列写入端在行会申请路径，前序标 DEFERRED |
| **D2** | 4428 摊位加货体 | `sub_61DA00` 逐件 wire（调用者 `0x61DEE3..0x61DF03`：`call 0x7567B8` 编码后 `lea edx,[esi+0x10]`） | C# 注释已承认。线上摊位流量 0，不挡互通 |
| **D3** | 4469/4470 的 `0x66682C` 替换臂 | `[ebx+0x38C]`=m_Master，先 4470(ebx) 再 4469(esi)，中间 `VMT+0xB4`。很像 `Monster.MakeClone` 精灵进化 | 未接线。进化路径会少一对通知 |
| **D4** | 4470 的 `0x6B292C` | fstart `0x6B2604`，edx=`[eax+0x6C4]`，紧挨 UserLogon 之后。`[+0x6C4]` 未映射 | 可能是下线拆宝宝。C# 登出是否走 Run 清扫未知 |
| **D5** | 4441–4445 关系附加包 | 4441 Len=0x24 记录布局；4444/4445 的名单基址 | 4433 已发，缺伴随帧。4444 srv=928 值得下一轮单独开 |
| **D6** | 4446 `[player+0x192C]` | `sub_712BE4` 返回值作 Recog | 字段未进 C# |
| **D7** | 4614 的 34 站点 | 包装器传入的 8 字节体 + Series=1 | 线上 0，不优先 |

---

## 7. 改了什么

| 文件 | ident | 改动 |
|---|---|---|
| `SystemModule/Grobal2.cs` | 4469 / 4470 / 4612 | 新常量，注释写死 VA + 字节 + 流量 |
| `TPlayObject.NativeSocialLoginPackets.cs` + `TPlayObject.Base.cs` | **4612** | UserLogon 在 4615 与 4628 之间发空体 |
| `TBaseObject.cs` | **4469** | `NotifyNativeSlaveListChanged`；`MakeSlave` 在 `Add` 之后发 |
| `MagicManager.cs` | **4469** | 驯服成功 `Add` 之后发 |
| `PasApiBridge.cs` | **4469** | `makeslaveex` `Add` 之后发 |
| `TBaseObject.Base.cs` | **4470** | 死亡清扫 `RemoveAt` 前、忠诚过期摘主人前发 |

未跑 `dotnet build`。

---

## 8. 建议优先级

1. **D5 4444/4445**（928+900）—— 本片剩下唯一「有流量、完全没包」的关系族。
2. **D1 4612 队列灌数** —— 登录空包已在，申请通过/拒绝时在线玩家少 17 字节通知。
3. **D2 4428** —— 摊位加货；等 `sub_61DA00` 布局，线上 0 不急。
4. **D3/D4 4470 剩余两臂** —— 进化/下线可能少包。
5. 声明型 INVENTED（5000/8000/65000）—— 批量删要先改审计工具，别夹在协议任务里。

---

## 9. 方法备忘 / 翻案

- 本片 ident **没有**「C# 用原生编号当手游协议」的已发包；重灾区在死常量波段，不在 4415–4650 真号。
- `SendNativeCorpsStatus(ident)` 这类间接发送会被「同一行必须出现 SendDefMessage」的扫描漏掉。判 MISSING 前要跟进 helper。
- 4612 空列表仍发：看 `je` 跳到发送点而不是 `ret`。
- 4469 不是 SM_USERNAME(42) 的替身：Recog 恒 0、只有名字、挂在奴隶列表而不是可见列表。
- 10054 不是 wire SM。`call 0x765E68` 是入队。
