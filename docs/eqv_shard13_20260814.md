# 等价核验报告 — 分片 13（MINING / GATHERING, MINE-05..MINE-30）

- 日期：2026-08-14
- 分支：`w/eqv-13`（基线 `00841828` = master merge `w/trade-storage`）
- 底本：`D:/loym2/staging/_reunpack_work/flat_image.bin`（ImageBase `0x400000`）
- 反汇编工具：`staging/_eqv13_work/mdis.py`（capstone 5.0.7, py311）
- 范围：分片内 26 条契约（实为 MINE-05..MINE-30 共 26 条）
- 挖矿代理已核 FAITHFUL（本报告仅复核未重改）：**MINE-06 / 07 / 14 / 21**

---

## 一、判定计数

| 判定 | 数量 | 条目 |
|---|---|---|
| FAITHFUL | **26 / 26** | MINE-05..MINE-30 全部 |
| DIVERGENT | 0 | — |
| MISSING | 0 | — |
| INVENTED | 0（活代码）| 见 §四 死字段 `StoneMineEvent.AddToMap` |
| BLOCKED | 0 | — |

**结论：分片 13 全部条目当前均行为忠实，无需外科式修复（0 修复提交）。**
分片 TSV 的 `csharp_status` 列（DIVERGENT/MISSING/UNKNOWN）反映的是分片创建时的旧态；
其中被标 DIVERGENT/MISSING 的 10 条（MINE-06/08/11/12/15/16/21/24/25/26）均已被前序提交
修好并**逐字节复核通过**（提交清单见 §五）。

高价值发现：**台账 MINE-12 的契约文字「CASE-SENSITIVE」与字节矛盾**（详见 §三）。

---

## 二、逐条核验（含原生 VA+字节证据、C# 位置、结论）

### 地图格与旗标（INTERNAL）

**MINE-05 — 格子记录 12 字节，X-major 索引 `X*height + Y`** — `FAITHFUL`
- 原生 `0x7776CB 0F AF 50 40 imul edx,[eax+0x40]`(×height) / `0x7776CF 03 CA add ecx,edx`(+Y) /
  `0x7776D1 8D 14 49 lea edx,[ecx+ecx*2]`(×3) / `0x7776D4 8B 40 38 mov eax,[eax+0x38]`(base) /
  `0x7776D7 8D 04 90 lea eax,[eax+edx*4]`(×4 ⇒ 步长 12)。
- C# `Envirnoment.cs:66` `index = nX * wHeight + nY;`；格子拆成三条并行数组
  `MapCellAttributes/SkillFlags/ObjectLists`（`Envirnoment.cs:1377-1379`），索引式完全一致。
  「12 字节单记录」是原生内存布局细节，C# 用并行数组表达，行为等价。

**MINE-06 — 格子 +0x00 地形属性必须非零才能挖（只挖挡格）** — `FAITHFUL`（挖矿代理已核，复核通过）
- 原生 `0x6BC24A cmp byte[cell+0x00],0 / je fail`。
- C# `TPlayObject.Attack.cs:152` `if (mapCell && cellInfo.Attribute != CellAttribute.Walk)`；
  `CellAttribute.Walk=0`（`Envir.cs:54`），`!= Walk` ⇔ `!= 0`，方向一致（Walk 拒绝，HighWall=1/LowWall=2 可挖）。

**MINE-07 — 格子 +0x04 区域限制标记必须为零才能动作** — `FAITHFUL`（挖矿代理已核，复核通过）
- 原生 `0x6EC08F call 0x772A50`（读**玩家自己格** `[esi+0x130]`Y/`[esi+0x12C]`X）非零则弹绿字返 false。
- C# `TPlayObject.Attack.cs:97` `if (!m_PEnvir.IsSkillAllowedAt(m_nCurrX, m_nCurrY, 0)) { SysMsg("当前区域不可使用技能",绿); return; }`，
  查玩家自身格、对所有 hit ident 生效，方向正确。

**MINE-08 — MINE 旗标在任何 RNG 之前测试，未置则降级为普攻** — `FAITHFUL`（原 DIVERGENT，已修）
- 原生 `0x6EC0F1 cmp di,0xBC7`(CM_HEAVYHIT) → `0x6EC0FE cmp byte[map+0x6A],0 / 0x6EC102 je 0x6EC15A`
  （落回普通 ident 跳表）→ 之后才 `test esi,esi`(武器)/`cmp[std+0x15],0x13`(shape19)/`cmp word[item+0x26],0`(耐久)。
- C# `TPlayObject.Attack.cs:134` `if (wIdent==CM_HEAVYHIT && boMINE && weapon!=null && weapon.Dura>0)`
  在派发器内、`PileStones`(含全部 RNG)之前测旗标；非 MINE 图直接落到 `switch` 走普攻，
  不进 DigXY、不扣矿点、不抽签。旧缺陷（旗标在 yield 内测、非 MINE 图也扣次数耐久）已消除。

**MINE-09 — MINE 旗标只门控挖矿一件事** — `FAITHFUL`
- C# `boMINE` 消费点：`Attack.cs:112`(@OnDig 插件通知，挖矿分支)、`Attack.cs:134`(挖矿派发)、
  `Envirnoment.cs:2001`(状态串输出，诊断非玩法)。不影响移动/刷怪/掉落/PK，与原生普查一致。

**MINE-10 — MINE 可被一个解析器运行时切换，另一个只能置位** — `FAITHFUL`（文件路径；运行时切换见注）
- 原生**解析器 A**（`0x77523D` 起）：逐旗标 `cmp edi,1 → set +0x6A=1` / `test edi,edi(==0) → clr +0x6A=0`
  （`0x775257`/`0x775268`），是「按名+值」的**运行时切换器**（同函数还切 +0x67/+0x68/+0x69=RUNFLAG）。
- 原生**解析器 B**（`0x7763CE`）：`0x7763DF C6 43 6A 01 mov byte[+0x6A],1`，只置位无清零（MapInfo 文件加载器）。
- C# `Maps.cs:259-262` `if (CompareLStr(s34,"MINE",4)) boMINE=true;` 忠实对应**解析器 B**（只置位）。
- **注（超挖矿范围，报告不改）**：C# 无「按名+值运行时切换任意地图旗标」的处理器（全库无 `boMINE=false`），
  即解析器 A 未在 C# 复刻。这是**地图旗标命令子系统**级别的缺口，非挖矿专属；且 MINE 的可观测挖矿行为
  只取决于 MapInfo 置位 + `boMINE` 门控，均已忠实。建议交地图旗标子系统负责人核查是否需补运行时切换命令。

**MINE-11 — 两个旗标字面池 54/60 词、次序不同，旗标序号不稳定** — `FAITHFUL`
- C# 对每个旗标用**具名 bool 字段** + 按名比较（`CompareLStr`/`Equals`），**不按列表位置索引**，
  故「序号不稳定」无害。只有写入偏移 `+0x6A` 是契约，C# 用 `boMINE` 承载，一致。
- **台账内部冲突（转记）**：MINE-11 称 B-only 词 6 个，MFLG-06/07 称 5 个；C# 注释（`Maps.cs:363`）已按
  「池 A 是池 B 严格子集、NORIDE 为池 B 独有」处理，属 MFLG 领域，本分片不裁决。

**MINE-12 — 旗标名比较是长度限定的** — `FAITHFUL`ﾠ⚠️**台账契约文字错误**
- 台账写「length-qualified **and CASE-SENSITIVE**」，**后半与字节矛盾**：
  比较器 `0x4C6E94` 对**两侧**取字符后都过 `0x4034D4`：
  `0x4034D4 3C 61 cmp al,0x61 / 72 06 jb / 3C 7A cmp al,0x7A / 77 02 ja / 2C 20 sub al,0x20 / C3 ret`
  ——这是 **UpCase(a→A)**，即比较**大小写不敏感**；并要求 `N<=Len(a) && N<=Len(b)`（`0x4C6ED1`/`0x4C6EDF`）。
- C# `HUtil32.CompareLStr`（`HUtil32.cs:704-718`）：`compn<=0` / `src.Length<compn` / `targ.Length<compn` 三道长度门
  + 逐字符 `char.ToUpper` 比较，与原生逐条同构。**C# 用不敏感比较是对的，台账「CASE-SENSITIVE」判错。**
- 真缺陷（原 DIVERGENT）是 C# 曾用**全等**而非**长度-4 前缀**，已由 `599d5726` 修为 `CompareLStr(...,4)`。
  直接后果一致：地图写 `MINE2` 在原生/现 C# 都命中 MINE 前缀、置 `+0x6A=1`。

**MINE-13 — 原生方法名 TPlayer.DigXY** — `FAITHFUL`（信息性）
- 原生 `0x6BC378` Delphi 长串 len=26 = `[Exception]: TPlayer.DigXY`（字节 `5B 45 78 63 65 70 74 69 6F 6E 5D 3A 20 54 50 6C 61 79 65 72 2E 44 69 67 58 59`，实测确认）。
- 仅用于识别原生函数身份；C# 对应方法为 `TPlayObject.PileStones`。异常串为诊断文本、非协议，无 C# 动作。

### 矿点事件（对象/惰性/补矿）（MONEY_ITEMS）

**MINE-14 — 每格矿态是挂在格上的对象，非位图/数组** — `FAITHFUL`（挖矿代理已核，复核通过）
- C# `StoneMineEvent : Event`（类实例），经 `GetEvent(x,y,ET_MINE) as StoneMineEvent` 按格取，与原生
  `TStoneMineEvent` VMT `0x716888`、按节点种类字节匹配一致。

**MINE-15 — 首次挖矿惰性建点，charges=Random(200)，补矿量=Random(80)** — `FAITHFUL`（原 DIVERGENT +1，已修）
- 原生 `0x71769F mov eax,0xC8(200) / call Random → [ebx+0x0C]`；`0x7176AC mov eax,0x50(80) / call Random → [ebx+0x10]`；
  `0x7176B9 GetTickCount → [ebx+0x14]`。两抽签都在 nil 门之后**无条件**执行。
- C# `StoneMineEvent.cs:32-34` `MineCount=Random(200); _addStoneCount=Random(80); AddStoneMineTick=GetTickCount();`
  **无 +1**（旧缺陷会让零值不可达，已由 `3a2478a5` 修）。惰性建点入口 `TPlayObject.cs:2287-2289`。

**MINE-16 — 补矿量构造期抽一次、永不重抽（零值矿点永久枯竭）** — `FAITHFUL`（原 DIVERGENT，已修）
- 原生 `0x7176E8` reset：`mov eax,[ebx+0x10] / mov [ebx+0x0C],eax`（MineCount←存下的补矿量，无重抽）/ GetTickCount。
- C# `StoneMineEvent.cs:45-46` `MineCount=_addStoneCount; AddStoneMineTick=GetTickCount();`。原生缺陷照抄（注释已标）。

**MINE-17 — 有次数即每挖必扣一次（含失败），扣减在成功卷之前** — `FAITHFUL`
- 原生 `0x71770C cmp[esi+0x0C],0 / 7E jle`（<=0 跳过）→ `0x717712 dec[esi+0x0C]` → `0x71771A Random(4)`（扣减在**前**）。
- C# `TPlayObject.cs:2290-2294` `if(MineCount>0){ MineCount-=1; if(Random(4)==0){...} }`；`int`+`>0` 对应有符号 `jle`。

**MINE-18 — 补矿窗口严格大于 600000 ms** — `FAITHFUL`（分片已标 FAITHFUL）
- 原生 `0x717781 sub eax,[esi+0x14]` / `0x717784 3D C0 27 09 00 cmp eax,0x927C0(600000)` / `0x717789 76 jbe`（<=则不补）。
- C# `TPlayObject.cs:2348` `if ((GetTickCount()-AddStoneMineTick) > 10*60*1000)`，严格 `>`，值与方向皆一致。

**MINE-19 — 无每挖冷却常量，唯一节流是矿点次数经济** — `FAITHFUL`
- 原生 VMT+0x40 → `0x6E6700`：`call 0x76B354`(bodyState) + `cmp[esi+0x574],0`，纯 can-act 谓词，**无 tick 比较、无间隔常量**。
- C# 挖矿路径无专用冷却；仅有通用攻击测速（`m_dwAttackTick`）与矿点次数经济。

### 产出抽签与矿石（MONEY_ITEMS）

**MINE-20 — 两阶段抽签定序：25% 门 → 产率卷 → 耐久卷（共享全局种子）** — `FAITHFUL`
- 原生 DigXY 主体：`0x6BC294 call 0x717700`（内含 dec + `Random(4)` 门）→ `0x6BC2AC/0x6BC2C8 Random(24|12)`(产率)
  → `0x6BC3CC MakeMine`（内 `Random(120)` + 耐久卷）→ `0x6BC2DD Random(15)`(武器耐久) → `0x6BC2F8 dx=0x274(628)` 发包。
- C# `PileStones`：`Random(4)`(2294) → 矿堆(无 RNG) → `Random(mineRate)`(2331) → `MakeMine`(2333) → `DoDamageWeapon(Random(15)+5)`(2341)
  → `SendDefMessage(SM_MINESUCCESS)`(2342)。抽签**顺序与次数逐一对齐**。矿堆分支经复核无隐藏 RNG（`0x71773B..0x717760` 建点时限 `0x493E0=300000` 无抽签）。

**MINE-21 — 有效矿石概率 1/4×1/12=1/48，tier==2 时 1/96** — `FAITHFUL`（挖矿代理已核，复核通过；原 MISSING 已修）
- 原生 `0x6BC2A3 cmp byte[ebx+0x1828],2 / jne` → tier2:`Random(24)` else:`Random(12)`。
- C# `TPlayObject.cs:2326` `mineRate = m_btNativeFatigueTier==2 ? 24 : nMakeMineRate(12)`，`Random(mineRate)==0`。

**MINE-22 — 无任何玩家属性参与挖矿概率** — `FAITHFUL`
- 原生 yield 子树（`0x6BC2A3`/`0x6BC3CC`）只调 Random 与 item DB，无属性 getter；唯一玩家输入是 tier 字节。
- C# 路径只用 `Random` 与 `m_btNativeFatigueTier`，无等级/幸运/职业/属性/技能读取。

**MINE-23 — 背包门在产出例程内、成功卷之后（满包扣次数+武器耐久但零产出）** — `FAITHFUL`
- 原生 `0x6BC3E9 call [ecx+0x244]` dl=1（进 MakeMine 后、`Random(120)` 前），假 → `0x6BC499` 无矿。
- C# `MakeMine` 顶部 `TPlayObject.cs:3136` `if (m_ItemList.Count >= BagCapacity.Of(this)) return;`（在 `Random(120)` 前）；
  `PileStones` 中 `DoDamageWeapon`（2341）在 `MakeMine` 之后**无条件**执行 ⇒ 满包仍扣次数与武器耐久。RNG 次数随满包与否一致地变化。
- 注：原生门为虚方法 `[obj+0x244]`，C# 用「件数 vs 容量」表达；门的**位置/RNG 定序**忠实，门的**精确判据**属背包容量契约（跨分片交叉引用）。

**MINE-24 — 矿型为 Random(120) 减权重阶梯，权重和恰 120、无兜底** — `FAITHFUL`（原 DIVERGENT，已修）
- 原生 `0x6BC3F7 Random(120)` → `sub 0x0C jb`(金12) / `sub 0x12 jb`(银18) / `sub 0x0F jb`(铁15) / `sub 0x3C jb`(黑铁60) / `jmp`(铜余15)。12+18+15+60+15=120。
- C# `MakeMine`（`TPlayObject.cs:3157-3242`）逐分支 `nRandom-=12/18/15/60; if(<0)...` 与 `jb` 阶梯逐条对齐，末支铜矿无条件。旧「区间比较+兜底铜矿」缺陷已由 `b688261b` 修。

**MINE-25 — 权重 10%/15%/12.5%/50%/12.5%，黑铁最高** — `FAITHFUL`（原 DIVERGENT，已修）
- 同上权重 12/18/15/60/15，黑铁 60=50% 为最高。C# 一致。旧「默认黑铁 ~9.2%/铜 ~53.3%」缺陷已修。

**MINE-26 — 黑铁矿石为四字 len=8** — `FAITHFUL`（原 DIVERGENT，已修）
- 原生 `0x6BC4F4` len=8 `BA DA CC FA BF F3 CA AF`=黑铁矿石（实测确认）；金/银/铁/铜均 len=4。
- C# `GameSvrConfig.cs:1023` `sBlackStone="黑铁矿石"`（四字），其余四矿名对齐。旧三字「黑铁矿」按名查库零命中→静默 Dispose，已由 `bffd96d0` 修。

**MINE-27 — 矿石按名经 DB 查表克隆，查不到则零产出** — `FAITHFUL`
- 原生 `0x6BC460 mov eax,[0x7D5D6C]`(itemDB) / `0x6BC46A call 0x74DE54`(查+克隆) / `0x6BC471 test/je 0x6BC499`(null 无矿)。
- C# 各分支 `if (CopyToUserItemFromName(名, ref UserItem)) {...} else { Dispose(UserItem); }`。

**MINE-28 — 耐久 Random(13000)+3000，1/20 概率再 +Random(10000)（正常 3000..15999，奖励 3000..25998）** — `FAITHFUL`（分片已标 FAITHFUL）
- 原生 `0x6BC398 mov eax,0x32C8(13000) / Random` → `0x6BC3A4 add ebx,0xBB8(3000)` → `0x6BC3AA Random(0x14=20)` / `0x6BC3B6 jne` 跳过 → `0x6BC3B8 Random(0x2710=10000)` / `0x6BC3C2 add`。
- C# `MakeMineRandomDrua`（`TPlayObject.cs:3120-3128`）用配置 `nStoneGeneralDuraRate=13000 / nStoneMinDura=3000 / nStoneAddDuraRate=20 / nStoneAddDuraMax=10000`（`GameSvrConfig.cs:1367-1371`），四常量与抽签序全对。

**MINE-29 — 只写当前耐久 +0x26，不写 +0x28、不钳（当前可超活动上限）** — `FAITHFUL`
- 原生 `0x6BC475 call`(耐久) → `0x6BC47A 66 89 46 26 mov word[esi+0x26],ax`，无 +0x28 写、无钳。
- C# 各分支 `UserItem.Dura = MakeMineRandomDrua();`（只写 `Dura`）；`TUserItem.Dura` 为裸 `ushort` 字段（`TUserItem.cs:75`，无 setter 钳制），`DuraMax` 另存不动。非钳缺陷照抄。

**MINE-30 — 入包失败则 Free 实例，不掉落到地面** — `FAITHFUL`
- 原生 `0x6BC488 call [ebx+0x248]`(加入) / `0x6BC490 jne 0x6BC499`(成功跳过) / `0x6BC494 call 0x404690`(失败释放)。
- C# 各分支失败走 `else { Dispose(UserItem); }`，不 AddToMap 落地。旧「满包掉地上」缺陷不存在。

---

## 三、复核中发现的台账/前人结论错误（高价值）

1. **MINE-12 契约文字错误**：台账写「CASE-SENSITIVE」，但比较器 `0x4C6E94` 对两侧字符都过 `UpCase 0x4034D4`
   （`cmp al,'a' / jb / cmp al,'z' / ja / sub al,0x20`），实为**大小写不敏感**。C# 的 `OrdinalIgnoreCase`/`ToUpper`
   在这一点上**一直是对的**；真缺陷只是「全等 vs 长度-4 前缀」，已修。**照台账把 C# 改成敏感比较会引入新错。**

2. **分片 TSV `csharp_status` 列已过期**：MINE-06(MISSING)/08(DIVERGENT)/11(DIVERGENT)/12(DIVERGENT)/
   15(DIVERGENT)/16(DIVERGENT)/21(MISSING)/24(DIVERGENT)/25(DIVERGENT)/26(DIVERGENT) 均已修复并复核 FAITHFUL。
   直接采信旧状态会导致重复修改已正确的代码。

3. **MINE-11 与 MFLG-06/07 的 B-only 词计数冲突（6 vs 5）**：属地图旗标池普查领域；对挖矿无影响
   （C# 按名不按序号索引），转记备查，本分片不裁决。

---

## 四、INVENTED / 死代码（不构成行为缺口，按本仓惯例留给可编译集成方）

- `StoneMineEvent.AddToMap`（`StoneMineEvent.cs:10`）：原生构造器无此字段。当前**只写不读**
  （全 GameSvr 仅 `:25` 写 `true`，无任何 `stoneMineEvent.AddToMap` 读点），行为已与原生等价（死字段）。
  注释 `:20` 已标为发明。按本仓既有惯例（`Maps.cs:338-346` 的死旗标字段处理），物理删除留给能
  `dotnet build` 的集成方；本代理不可编译验证，故不删，仅登记。**不影响任何可观测行为。**

（其余历史发明均已删除并在 HEAD：`MakeMine2` 宝石产线 `997f161c`、事件类型 55/56/57 `76733538`、
`GoldStoneMin` 等死配置 `3fc3587f`、`AddToMapMineEvent` 的 3×3 可走性扫描 `a2f2d687`、满包掉地上=改 `FreeAndNil`。）

---

## 五、修复项与提交

**本分片 0 修复提交**：26/26 已忠实，无行为缺口；唯一 INVENTED 为死字段（见 §四，按惯例不删）。

前序已落地并在 HEAD(`00841828`) 祖先链中的挖矿修复（本报告逐字节复核确认其正确）：
`d331a114 / 997f161c`(MINE-01 移除)、`599d5726`(MINE-12 前缀比较)、`26f2e903 / 7c9c62da`(MINE-08 派发器测旗标)、
`6198b120`(MINE-15 惰性建点)、`3a2478a5 / e42a997e`(MINE-15/16 去 +1、补矿复用)、`b688261b`(MINE-24/25 减权重阶梯)、
`bffd96d0`(MINE-26 黑铁矿石)、`d6e14177 / ef91a9cc`(MINE-21 tier==2 半速)、`76733538`(MINE-54 删 55/56/57)、
`3fc3587f`(删死配置)、`401565b2`(MINE-01/06/08/11/12/36 结构缺陷)。

---

## 六、覆盖率

- 分片 26 条：**26 条已判定（100%）**，其中 26 FAITHFUL、0 BLOCKED。
- 二进制证据：核心 MONEY_ITEMS 与 PROGRESSION 契约（MINE-05/08/12/15/16/17/18/20/21/24/25/26/28/29/30）
  均**独立反汇编复核**（非仅信注释），字符串经 GBK 长度前缀实测。
- 未做穷尽验证的（依契约声明的强支持，非本分片可加固）：MINE-09（disp8 普查看不到 [reg+reg] 形）、
  MINE-22（跨体调用目标枚举）——两者 C# 侧均无反例。

## 七、建议优先级

1. **无紧急修复**。分片 13 可视为已收敛。
2. （P3，跨子系统）MINE-10 运行时旗标切换器（解析器 A）：交地图旗标命令子系统负责人评估是否需在 C# 补
   「按名+值切换任意地图旗标」的运行时命令；当前对挖矿零可观测影响。
3. （P4，清理）`StoneMineEvent.AddToMap` 死字段由能编译的集成方物理删除。
4. **修正台账**：MINE-12 的 `strength/contract` 去掉「CASE-SENSITIVE」，改注「length-qualified, CASE-INSENSITIVE (UpCase 0x4034D4)」。
