# 分片 03 等价核验结论（PRICE-13~25 / TRADE-01~13）

- 工作树：`D:\loym2\.claude\wt2\eqv-03`　分支：`w/eqv-03`
- 底本：`D:\loym2\staging\_reunpack_work\flat_image.bin`（ImageBase=0x400000），capstone 5.0.7 反汇编逐地址核实
- 构建：`dotnet build GameSvr` → 0 错误（基线与修复后各一次）
- 提交：`912cfb67` TRADE-10 GM 权限旁路补齐（1 文件）

## 状态计数（共 26 行）
| 状态 | 数量 | 行 |
|---|---|---|
| FAITHFUL | 23 | PRICE-13~23（11）；TRADE-01~09,11,12,13（12） |
| FIXED（原 MISSING→本波补齐并提交） | 1 | TRADE-10 |
| BLOCKED | 2 | PRICE-24（方法极限，固有）；PRICE-25（结构已解，C# 不可达） |

> 全部 26 行原 `csharp_status=UNKNOWN` 且 `csharp_ref` 为空；本波逐行定位 C# 落点并对二进制核实。

---

## PRICE-13~25（定价 VMT slot [itemVMT+0x20]）
C# 落点：`GameSvr/Npcs/Merchant.cs` `GetUserItemPrice`（≈1652-1857）、`GetSellItemPrice`（2087）、`ClientQueryRepairCost`（2376）、`ClientRepairItem`（2422）；`SystemModule/HUtil32.cs` `RoundOrePriceBonus`；`GameSvr/Items/NativeItemFactory.cs`。

| id | 状态 | 依据（二进制↔C#） |
|---|---|---|
| PRICE-13 | FAITHFUL | sub_786208：0x786226 `cmp edi,esi`；equal 0x78622A→`_MAX(2,price)`；under 0x786240..0x78626A；over 0x78627B..0x78629B（`sub edi,esi`=cur-liveMax）；0x7862A3 `call 0x783d70` 尾调基类。C# StdMode==40 把 equal+under 折叠为 `Dura<=DuraMax`（Dura==DuraMax 时 n20=0 等价），over 用 `n10 + Round(n10/DuraMax*2.0*(Dura-DuraMax))`。 |
| PRICE-14 | FAITHFUL | over 臂 0x78629B `add [ebp-4],eax` 之后**无** `call 0x4C7004`；C# over 分支 `n10 = n10 + …` 无 `_MAX` 钳位。 |
| PRICE-15 | FAITHFUL | sub_7862B4：0x7862DA `cmp ebx,0x2710`/0x7862E0 `jge`/0x7862E2 `mov ebx,0x2710`；ebx 为局部，**从不写回 [esi+0x28]**。C# `oreDuraMax` 局部钳位，不改 `UserItem.DuraMax`。 |
| PRICE-16 | FAITHFUL | 0x786347 `fld xword [0x786378]`，常量原始字节 `66 66 66 66 66 66 66 A6 FF 3F`（80 位扩展≈1.3）；两处 2.0 除数 0x7862B0/0x786374 = `00 00 00 40`（float32）。C# `HUtil32.RoundOrePriceBonus` 用精确有理数 13/10（**未折叠成 float/double**）。 |
| PRICE-17 | FAITHFUL | TEquipItem V=0x75CAC8，+0x20（@0x75CAE8）= `70 3D 78 00` = 0x783D70（基类体，无自体）。C# 仅 StdMode 40/43 特判，装备走通用 StdMode>4 基公式。 |
| PRICE-18 | FAITHFUL | TCloseAttrItm V=0x77EEC0：真实 +0x20（@0x77EEE0）= `70 3D 78 00`=0x783D70；+0xF4（@0x77EFB4）= `08 62 78 00`=0x786208（TMeatItem 体）落在自体区结束 0x77EF48 **之外**，确系连续 VMT 别名假象。C# 不对 StdMode 32/42 特判，与结论一致。 |
| PRICE-19 | FAITHFUL | sub_63F200：0x63F22E `call 0x63f380`(买价 shell)、0x63F235 `sar esi,1`+`jns/adc`(带符号除2向零)。C# `GetSellItemPrice` = `(int)nPrice/2`（注释已记录从 Round 改整除的修复）。 |
| PRICE-20 | FAITHFUL（原生finding确认） | 三 setter 确认非 stub：0x640148 `mov byte[edx+0x185C],1`、0x64016C `…,cl`、0x64018C `…,2`，各 `mov cx,0x279d`+`call 0x765e68`。解决 ECON-40「可能是 stub」冲突。**注**：C# `click_repairex`(sub_64016C) = `RejectUnsupportedNativeApi`（已登记 fail-closed）；C# 用脚本标签而非 +0x185C 模式字节驱动修理种类。 |
| PRICE-21 | FAITHFUL | sub_63EE9C：0x63EF73 `call 0x63f380`(shell)→0x63EF7F `call 0x640208`(rate)→0x63EF92 `idiv 3`(真整除)→`/liveMax`→`*ABS`→0x63EFCC `@ROUND`；0x63EFD6 `cmp byte[player+0x185C],2`/0x63EFDF `lea eax,[esi+esi*2]`(×3，在 Round 之后)。C# `ClientRepairItem`/`ClientQueryRepairCost` `Round((GetUserPrice(shell)/3)/DuraMax*(DuraMax-Dura))`，×3 用 `nSuperRepairPriceRate`(默认3)。`nRepairItemDecDura=30`=0x1e 亦对（0x63F07A `idiv 0x1e`）。 |
| PRICE-22 | FAITHFUL（主域）；边缘发散已记 | Abs 惯用式 0x63EFBD `2B C2/99/33 C2/2B C2` 确认。C# 用 `(DuraMax-Dura)` + 门 `DuraMax>Dura` 替代 abs——正常域(Dura≤DuraMax)等价。**边缘**：Dura>DuraMax 时原生用 abs 得正成本并继续修理，C# 因门拒绝；该态可达性未证，C# 更保守。 |
| PRICE-23 | FAITHFUL（finding，**修正配对**） | 三标签真实且各引用一次：0x63F114=`@fail_s_repair`(len14, ref 0x63EEB8 提前返回)、0x63F130=`@SRepairDone`(len12, ref 0x63F044)、0x63F148=`@RepairDone`(len11, ref 0x63F091)。**shard 把名字↔地址配反了**（0x63F114 实为 @fail_s_repair、0x63F148 实为 @RepairDone），但实质契约成立。**注**：C# 成功跳转用 `~@repair`/`~@s_repair`(M2Share.cs，**禁改**) 而原生用 `@RepairDone`/`@SRepairDone`——需主代理裁决脚本标签命名约定（涉禁改文件+脚本数据交叉验证）。 |
| PRICE-24 | BLOCKED（固有，确认） | 契约自陈为方法极限而非否定结论：静态类引用分析无法钉住任一商业交易到达的具体物品类（0 个 imm32 引用跨 147 物品 VMT）。多态本身已证。C# 以 `NativeItemFactory` 按 StdMode/Shape 字段派发正确复刻多态，不依赖静态类工厂。保持 BLOCKED。 |
| PRICE-25 | BLOCKED→**结构已解** | 从函数头 0x63EE9C 对齐反汇编（非错位起点）后「第二修理分支」0x63EF1C-0x63EF6C 完全解析：选择条件 = `player[+0x185C]==3`（0x63EF12 `mov al,[+0x185C]`/0x63EF18 `sub al,3`/0x63EF1A `jne`）；基址 = UserItem([ebp-8])；公式 = `_MAX(-1, Round(StdItem.Price[+0x3c] * (DuraMax-Dura) / [0x63F124]))`，常量 0x63F124=`00 00 7A 44`=1000.0f。shard 所述「乱码字节」系错位假象。C# 不实现 mode-3（唯一置模式入口 click_repairex 已 fail-closed，故 mode-3 不可达，缺口与 C# 一致）。 |

---

## TRADE-01~13（交易 / 个人仓库）
C# 落点：`GameSvr/Players/TPlayObject.Operate.cs`（`ClientAddDealItem` 1651、`ClientDelDealItem` 1720、`ClientChangeDealGold` 1742、交易请求 ≈1560-1649）；`TPlayObject.cs` `OpenDealDlg`(3101)/`GetBackDealItems`；`Services/NativeItemDropDestroy.cs` `CheckTransferPermission`；`SystemModule/Grobal2.cs` 操作码。

| id | 状态 | 依据（二进制↔C#） |
|---|---|---|
| TRADE-01 | FAITHFUL（finding） | 0x6D805C `movzx eax,[msg+4]` 后 `cmp/jg/je` 二分梯（0xcd6/0x45c/0x412/0x3f7…），非跳表；唯一索引跳表 0x6D8120 经 0x6D810B `add eax,0xfffffc12`(-1006)/`cmp 8`/`ja`/`jmp [eax*4+0x6d8120]` 仅覆盖 1006-1014。C# Grobal2 `CM_DEALTRY..CM_DEALEND=1025..1030` 定义正确。 |
| TRADE-02 | FAITHFUL | sub_6C3F00：0x6C3FD6 `call 0x6c4090`(self→partner)、0x6C3FDF `call 0x6c4090`(partner→self)。C# `OpenDealDlg` 双向调用（Operate.cs:1631-1632）。 |
| TRADE-03 | FAITHFUL | 0x6C3F2B/0x6C3F44 两次 `call 0x767e80`(前置对象，读朝向+0x154/坐标+0x12C/+0x130)；0x6C3F49 `cmp ebx,eax`/`jne` 互相面对门。C# `GetPoseCreate()` + `TargetPlayObject.GetPoseCreate()==this`。 |
| TRADE-04 | FAITHFUL | 0x6C3F6B `cmp byte[esi+0xba0],0`/0x6C3F72 `je` 拒绝；拒绝消息 @0x6C407C 解码 GBK=「对方拒绝和你交易。」(len18)。C# `m_boAllowDeal`。**注**：C# 失败发 `g_sPoseDisableDealMsg`(文案异，镜像0命中) + 额外 `m_boCanDeal`(密码锁族，恒真，已注明)。 |
| TRADE-05 | FAITHFUL | 开仓助手 sub_6C4090：0x6C40B0 设交易对象、0x6C40B8 `call 0x6c4114`(GetBackDealItems) 在开仓内、交易开始前。GetBackDealItems：押金逐个回背包→escrow.Clear→`add [+0x15c],[+0x6e0]`(退金)→清 dealGold→清 dealOK。C# `OpenDealDlg`(3101) 调 `GetBackDealItems()` 后 `SM_DEALMENU`。 |
| TRADE-06 | FAITHFUL | 0x6C4268 `call 0x424ab8`(escrow.Add(item)) + 0x6C427F `call 0x424b30`(bag.Delete(idx)) 同一指针，纯移动无克隆。C# `m_DealItemList.Add(UserItem)` + `m_ItemList.RemoveAt(i)`（Operate.cs:1705/1707）。 |
| TRADE-07 | FAITHFUL | 0x6C41CE `mov eax,[ebx+0xbac]`(partner)/0x6C41D4 `cmp byte[eax+0x684],0`/`jne` 拒绝。C# `if(!m_DealCreat.m_boDealOK)`（1678）。 |
| TRADE-08 | FAITHFUL | 0x6C4254 `mov eax,[ebx+0x6dc]`(escrow)/0x6C425A `cmp [eax+8],0xC`/`jge` 拒绝。C# `m_DealItemList.Count < 12`（1686）。 |
| TRADE-09 | FAITHFUL | 0x6C4223 `mov cl,[ebx+0x4b7]`/0x6C4229 `mov edx,2`/0x6C422E `mov eax,esi`/0x6C4230 `call 0x78389c`/`jg reject`。mode-2 体 0x783911：`cmp[item+0xfc],0`/`test[std+3],2`→返回3。C# `CheckTransferPermission(…,TransferModeTrade=2)`；`Reserved02&0x0200`→3。 |
| TRADE-10 | **FIXED**（原 MISSING） | 0x6C41AD `cmp byte[ebx+0x675],4`/`jae` + 0x6C41BC 对 partner 同样 → 任一方 authority≥4 置 skip 标志([ebp-0x12])；0x6C421D `cmp [ebp-0x12],0`/`jne 0x6C423E` **整段跳过** eligibility。+0x675=m_btPermission。旧 C# 无条件校验，漏 GM 旁路。**已补**：`if(m_btPermission<4 && m_DealCreat.m_btPermission<4){…CheckTransferPermission…}`（提交 912cfb67，构建 0 错误）。 |
| TRADE-11 | FAITHFUL（[item+0xFC] 省略已注明） | sub_78389C 前置门 0x7838AE `call 0x784710`(bindType≠0)/0x7838BB `test[[+0x1c]+3],8`(0x0800)/0x7838C3 `call 0x784720`(0x4000,timed)→返回1。C# `CheckTransferPermission` pre-ladder：`ReadBindWord!=0 \|\| Reserved02&0x0800 \|\| &0x4000`→1；mode-2 `[item+0xFC]` 无 C# 字段已记录省略（recorded-not-invented）。 |
| TRADE-12 | FAITHFUL | sub_6C4348：扫描 escrow 比对 `[item+0x18]`(Recog) 与目标，命中置标志→0x6C43B4 `call 0x6c43c4`(DealCancel)；partner 已确认(0x6C436F)则跳过不取消。C# `ClientDelDealItem` 扫描命中→`DealCancel()`；门 `m_DealCreat.m_boDealOK`（1722）。SM 677-680 全镜像 0 命中佐证无单件撤回。 |
| TRADE-13 | FAITHFUL | sub_6C4454：0x6C4463 `cmp esi,[ebx+0x6e0]`(新值 vs 现押金)/0x6C4469 `jge` 继续/0x6C446D `call 0x6c43c4`(DealCancel)/`jmp` 收尾**不发**失败包。C# `if(nGold<m_nDealGolds){DealCancel();return;}`（1755-1758），排在 `<=0` 门之前。 |

---

## 转交主代理的观察（本波未改，涉禁改文件或需跨源裁决）
1. **PRICE-23 脚本标签命名约定**：原生修理成功跳 `@RepairDone`/`@SRepairDone`（0x63F148/0x63F130），而 C# `sREPAIROK="~@repair"`/`sSUPERREPAIROK="~@s_repair"`（`M2Share.cs`，**禁改**）。GotoLable(sub_63dc98) 为虚派发到脚本引擎的跳转，标签串即脚本节名。若 NPC 脚本数据按战神写作「@RepairDone」节，则 `~@repair` 不匹配。需交叉验证脚本数据后统一。
2. **PRICE-22 边缘态**：Dura>DuraMax 时原生（abs）产正成本并把 Dura 压到 DuraMax，C#（门 DuraMax>Dura）拒绝。可达性未证，方向上 C# 更安全但非逐字等价。
3. **PRICE-20/25 修理模式字节 +0x185C**：原生用玩家 +0x185C（0/1/2/3）选修理种类（普通/×3/mode-3 特殊），C# 改用脚本标签驱动，且 `click_repairex` 已 fail-closed，故 mode-3 分支不可达。若将来接线 click_repairex，需按 PRICE-25 已解结构补 mode-3 公式 `_MAX(-1, Round(StdItem.Price*(DuraMax-Dura)/1000.0f))`。
4. **TRADE-04 拒绝文案**：C# `g_sPoseDisableDealMsg`「对方禁止进入交易」镜像 0 命中，原生为「对方拒绝和你交易。」(@0x6C407C)。属另一条待裁项。
