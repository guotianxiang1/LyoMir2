# 等价核验报告 — 分片 21（MOVE-26 .. MOVE-51）

- 子系统：MOVEMENT / WALKABILITY / TELEPORT（走路 / 可行走判定 / 传送）
- 底本：`D:\loym2\staging\_reunpack_work\flat_image.bin`（base 0x400000，capstone 5.0.7）
- 分支：`w/eqv-21`（worktree `D:\loym2\.claude\wt2\eqv-21`，基线 `00841828` master）
- 判定口径：`FAITHFUL` / `FAITHFUL⚠`(忠实但有潜伏/次要偏差) / `MISSING`(缺失，仅报) / `BLOCKED`(无据可判)
- 铁律：有据不臆造；MOVE-15/83/NOTHROUGH 归他人处理，本片不动这些文件。

---

## 一、状态统计

| 判定 | 数量 | 条目 |
|---|---|---|
| FAITHFUL | 20 | 26,27,28,29,30,32,33,35,37,38,40,42,44,45,46,47,48,49,50,51 |
| FAITHFUL⚠（忠实但有潜伏/次要偏差或谓词细节未逐字节证） | 3 | 36, 41, 43 |
| MISSING（真缺口/良性缺口，仅报不改） | 3 | 31, 34, 39 |
| BLOCKED | 0 | — |

**核验覆盖率：26/26（100%）逐条对二进制取证。** 其中 23 条行为等价（含 3 条次要/潜伏偏差），3 条缺失。

**修复项：0 处代码改动。** 结论见第四节——3 处缺口分别为：托管内存下的良性差异(31)、跨越 cell 数据模型的整块子系统(34)、英雄跟随子系统(39)。均不满足"外科式 + 有据"的双重门槛，按"大缺口/热点只报不改"处理。

核心可行走/传送原语（MoveToMovingObject、GetCell、CanWalk、GetNextDirection、GetRandomXY、两个 mover 的边界与闸、随机传送的旗标、走/跑代价）——即本片的主体——**已被前序工作高度忠实移植并逐条对上二进制**。

---

## 二、关键 C# 位置总表

| 原生 | C# 位置 |
|---|---|
| `sub_7797CC` MoveToMovingObject | `GameSvr/Maps/Envirnoment.cs:363` `MoveToMovingObjectCore` |
| `sub_7776A8` GetCell | `Envirnoment.cs:62` `TryGetMapCellIndex` + `GetMapCellInfo` |
| `sub_777EF8` 只读探针 | `Envirnoment.cs:536` `CanWalk(nX,nY,boFlag)` |
| 单元格记录布局 | `GameSvr/Maps/Envir.cs`（`CellObject`/`CellType`/`CellAttribute`/`MapCellinfo`） |
| `sub_765D94` 生物阻挡谓词 | `GameSvr/Actors/TBaseObject.cs:648` `IsNativeCellBlocking` |
| `sub_741224` 人形 mover | `TBaseObject.cs:1230` `WalkTo` + `TPlayObject.cs:952` `WalkToInBounds` override |
| `sub_71F0F4` 怪物 mover | `WalkTo` + `TAnimalObject.cs:32` `WalkToInBounds` override |
| `sub_6BBCD8` 走路原语 | `GameSvr/Players/TPlayObject.Attack.cs:872` `ClientWalkXY` |
| `sub_764A90` GetNextDirection | `GameSvr/M2Share.cs:1871` `GetNextDirection` |
| `sub_7782D0` GetRandomXY | `TBaseObject.cs:2062` `NativeGetRandomXY` |
| `sub_6BD294` 同图随机传送 | `TBaseObject.cs:2132` `TrySpaceMoveToEnvironment`（同图臂） |
| `sub_76BECC`/`sub_76BEC8` 代价 | `ClientWalkXY`/`ClientRunXY`：`m_nHealthTick`/`m_nSpellTick`/`DecreaseHealthSpellRecoveryStep` |

---

## 三、逐条判定与证据

### MOVE-26 MoveToMovingObject 既测又搬 / Envir[+0x44] 闸 / TRUE 单点 — FAITHFUL
- 原生 `sub_7797CC`：0x77980A `cmp [eax+0x44],0 / je 0x779b7e` 整调用即败；结果字节 `[ebp-9]` 仅在 0x779A95 置 1。
- C#：`MoveToMovingObjectCore` 单函数既判又搬；`+0x44`（地图就绪标志）在 C# 由 `wWidth/wHeight/MapCellAttributes` 为空/为零兜住（`TryGetMapCellIndex` 越界即 -1）。唯一"成功=搬链后置 result=1"在 `Envirnoment.cs:516`。行为等价（INTERNAL）。

### MOVE-27 列主序寻址 cell = base + (x*Height + y)*12 — FAITHFUL
- 原生 GetCell `sub_7776A8`：0x7776CB `imul edx,[eax+0x40]`(Height) → lea×3 → lea×4；MoveToMovingObject 内联于 0x779845/0x779A1C 同式。
- C# `Envirnoment.cs:66` `index = nX * wHeight + nY`。**Height 为行距**（非 Width），一致。

### MOVE-28 地形闸 cell[0]==0 才可走 — FAITHFUL
- 原生 0x779863 `cmp byte [eax],0 / jne fail`；只读探针 0x777F5A 同。
- C# `MapCellinfo.Valid => Attribute == CellAttribute.Walk(0)`（`Envir.cs:64`）；`Envirnoment.cs:428` `Attribute != 0 → result=-1`。

### MOVE-29 单元格对象链 / kind 语义 — FAITHFUL（行为级）
- 原生节点 [0]=kind、[4]=obj、[0xC]=next；kind 1 生物 / 2 物品 / 3 地图事件 / 4 传送点 / 8 矿旁。
- C#：`CellObject{CellType,CellObj}` + `IList<CellObject>`（`Envir.cs:19`）。C# 枚举值与原生 tag 不同但**内部自洽**，`Envir.cs:27` 注释明确告警不得跨用。行为等价。

### MOVE-30 占用扫描仅 kind==1 — FAITHFUL
- 原生 0x7798A7 `cmp byte [eax],1 / jne next`。
- C# `Envirnoment.cs:404` 仅 `CellType.OS_MOVINGOBJECT` 参与占用判定；物品/金币/尸体/事件/传送点均不挡路（默认可走）。

### MOVE-31 陈旧节点自愈（摘链+日志）— MISSING（良性，仅报）
- 原生 0x7798C0 `call 0x765D64`(存活探针 obj+0x106 && obj+0x128 && obj.Envir+0x44)；不存活则 0x7798CD..0x7798E7 摘链、0x7798E9..0x779980 拼诊断串、0x779991 `call 0x79DF74` 记日志、继续扫描。
- C#：占用扫描无存活探针/摘链/日志。**但在托管内存下不存在"悬垂/被释放对象"**：死亡/幽灵对象经 `IsNativeCellBlocking`（`!m_boDeath && !m_boGhost && ...`）返回"不挡"，可观测的走路结果与原生一致（陈旧对象不挡路）；缺的只是原生对裸指针链表的自愈+诊断日志，在 C# 无对应必要。判定：可观测行为等价，自愈副作用不适用/不需要移植。

### MOVE-32 逐对象 VMT+0 阻挡（普查）— FAITHFUL
- 原生 0x7799A0 `call [edx]`(VMT+0)，0x7799A6 test；生物 = `sub_765D94`。物品(0x783894)/事件(0x7172F4)/TLinkPoint(0x77C260) 的 VMT+0 在本占用扫描**不可达**（只走 kind==1）；TStoneMineEvent(0x71779C 恒挡) 为 kind 8，同样不可达——故不挡移动。
- C#：占用扫描对 `OS_MOVINGOBJECT` 调 `IsNativeCellBlocking`（=sub_765D94），其余类型不进扫描。一致。

### MOVE-33 生物阻挡谓词六项合取 — FAITHFUL
- 原生 `sub_765D94`：0x765D9B `mov bl,1`(默认可走)；+0x73==0 && +0x2E6!=0 && +0x74==0 && +0x2E3==0 && !(+0x2E2 || InBodyState(0x3C)) 才挡（0x765DCB `xor ebx,ebx`）。
- C# `TBaseObject.cs:648` `!m_boGhost && bo2B9 && !m_boDeath && !m_boFixedHideMode && !(m_boObMode || HasNativeActiveState(0x3C))`。逐项对上（含第六项 `sub_772EB8` 的 ObMode||State(0x3C)，见 `HasNativeCellPassThroughGrant`）。

### MOVE-34 cell[2] 怪物不可走属性 — MISSING（真缺口，跨数据模型，仅报）
- 原生读点：MoveToMovingObject 0x7799D5 `cmp byte [eax+2],0`，且 0x7799DE `cmp byte [Cert+0x178],0` 二者皆非零才 `[ebp-0xA]=0`(挡)。**此闸在 boFlag(忽略占用)=1 时仍生效**（0x779874 boFlag 短路跳到 0x7799C6，cell[2] 检查在其后）。
- +0x178：TCreature ctor 0x764E5F 写 0x32、TPlayer ctor 0x6AD76F 写 0——故 cell[2] 挡一切生物、从不挡玩家。
- **写点确证（字节搜索）**：`0x7793D4 mov byte [eax+2],1`，位于一个"向格子登记 TLinkPoint"的函数（0x779354..0x7793EF：`call 0x77C264` 造 TLinkPoint → 分配 16B 节点、kind=4、挂入 cell[8] 链 → `mov byte [cell+2],1` → `[Envir+0x18]` 列表登记）。即 **cell[2]=1 标记"传送点(LinkPoint)所在格"，怪物不可入、掉落/放置扫描(0x778DEF/0x778F93 读同字节)亦避开**。
- C# 现状：`MapCellinfo` 只有 `Attribute`(cell+0) + `SkillFlag` + `ObjList`，**无 cell+2**；且 C# 未把 LinkPoint 登记进格子（`m_PointList` 来自 `Point\*.txt`，对应 Envir+0x10 的随机点列表，与 Envir+0x18 的入格 LinkPoint 是两套东西）。
- 为何仅报不改：忠实移植需同时补 (a) 每格 cell+2 字节、(b) LinkPoint 入格加载器(sub_779354)、(c) +0x178 的字段映射——跨越 cell 数据模型的整块子系统，且无 cell[2] 的地图来源逐字节证据以避免臆造。属"大缺口"，按规仅报。

### MOVE-35 破坏性谓词：摘旧链→头插新格→才 TRUE — FAITHFUL
- 原生：旧坐标上界校验 0x7799F5/0x779A05；0x779A52..0x779AAB 扫旧格找自身；找到才 0x779A80..0x779A92 头插目标格 `destCell[8]`、0x779A95 置 TRUE；找不到落 0x779AAD 保持 FALSE。
- C# `Envirnoment.cs:445` `TryGetMapCellIndex(nCX,nCY)` 旧坐标校验、449..491 扫旧格摘链、`if(!boUnlinkedFromSource) return 0`、515 `ObjList.Insert(0,...)` **头插**、result=1。`Envirnoment.cs:434-515` 有详尽字节级注释佐证。头插使格内迭代顺序可观测，已对齐。

### MOVE-36 地图事件探针 sub_779204（怪物 mover 专属，+0x481 闸）— FAITHFUL⚠
- 原生怪物 mover 0x71F173 `cmp byte [ebx+0x481],0 / je 0x71F195`；非零才 0x71F186 `call 0x779204`。探针：GetCell → 扫 cell[8] 找 kind==3 且 obj!=0 且 obj+0x34!=0 的节点，命中则 mover 置 `[ebp-0xd]=0`(拒走)。人形 mover 无此调用。
- +0x481 == C# `bo2BA`（`UsrEngn.cs:2961` 注释，race 95/96/97/101 交叉印证）。C# `bo2BA` 默认 false（`TBaseObject.cs:737`），仅在 `UsrEngn.cs`(怪物创建 2898/2902/2906/2917/2967)对特定种族置 true，玩家永不置位。
- **C# 已实现**：`WalkTo`（`TBaseObject.cs:1298`）`if (bo2BA && !m_PEnvir.CanSafeWalk(nNX, nNY)) bo29 = false;`——即 bo2BA 类对象移动前扫目标格、遇有害地图事件则拒走。门控(bo2BA=+0x481)与作用域(仅这些怪物、玩家不触发)精确对上。`CanSafeWalk`（`Envirnoment.cs:1591`）扫 `OS_EVENTOBJECT` 且 `m_nDamage>0`。
- ⚠ 次要偏差：原生用专用探针 `sub_779204`(判 kind-3 且 obj+0x34!=0)，C# 用 `CanSafeWalk`(判 OS_EVENTOBJECT 且 m_nDamage>0)。行为/门控/范围等价，但 `obj+0x34!=0` 与 `m_nDamage>0` 的字段对应未逐字节证实。判定 FAITHFUL⚠。

### MOVE-37 人形 mover 先落 Dir 再判可走 — FAITHFUL
- 原生 `sub_741224`（**序言已被 reunpack 覆盖**，与旧注释"未 dump"相反）：0x74124A `mov [ebx+0x154],al`(落朝向) 在 0x7412C8 `call 0x7797CC`(可走判定) **之前**；方向校验 0x74123E 在落朝向之前。
- C# `WalkTo`：`WalkToDirectionIsValid`(1246) → `m_btDirection = btDir`(1254) → 边界(1295) → `MoveToMovingObject`(1312)。次序一致：被拒的移动仍改朝向。
- 备注：人形方向校验字节为 `not eax / sub eax,8 / jb`——**实测仅拒负方向(-1..-8)，不拒 8..255**；C# `btDir<=7` 更严（对 byte 入参 fail-closed）。实战 dir 恒 0..7，无可观测差异。

### MOVE-38 人形边界严格 >0 且 <Width/Height — FAITHFUL
- 原生 0x741276 `test esi,esi / jle`、0x741284 `cmp esi,[eax+0x3C] / jge`、0x74128D、0x7412A0（Y 同）。
- C# `TPlayObject.cs:952-956` `nNX>0 && nNX<wWidth && nNY>0 && nNY<wHeight`。逐条对上，玩家永不站第 0 行/列。

### MOVE-39 人形 mover 成功尾部（清 bodyState 0x17 / 广播 / 英雄跟随）— MISSING/PARTIAL（COSMETIC，仅报）
- 原生 `sub_741224` 尾（0x7412D5..0x741350）：提交 X/Y → 0x7412E8 `mov dl,0x17 / call 0x76B4D0`(清 bodyState 0x17) → 0x74130D `mov dx,0x2712` `call [vmt+0xD8]`(广播 RM_WALK: Dir/Y/X) → 0x741323 `call 0x778EC0`(Envir,Self) → InBodyState(0x33) && Self[+0x3C0]!=0 → 0x741350 `call 0x6BBEE4`(英雄跟随)。
- C#：`WalkTo` 提交 X/Y 并经 `Walk(RM_WALK)`(`TBaseObject.cs:4531`) 广播（`SendRefMsg(RM_WALK,dir,X,Y)`）——**提交+广播已在**。但缺 (1) 清 bodyState 0x17、(2) `sub_778EC0(Envir,Self)`、(3) 主人推动式英雄跟随 `sub_6BBEE4`。英雄跟随在 C# 大概率由英雄自身 AI 完成（结构差异、行为近似）。清 0x17 / sub_778EC0 需字段与函数映射证据。COSMETIC，仅报。

### MOVE-40 mover VMT+0x30 按类型分三槽 — FAITHFUL
- 原生：TCreature/TPsNpc→0x767568(`xor eax,eax;ret` 永不动)；TAnimal/TMonster/TAIMon/卫士/TFieldHero→0x71F0F4；THumanKind/TPlayer/THeroAct→0x741224。
- C#：基类 `WalkToInBounds` 返回 false（对应永不动槽，`TBaseObject.cs:1205`）；`AnimalObject` override 怪物边界（`TAnimalObject.cs:32`）；`TPlayObject`/`HeroObject` override 人形边界（`TPlayObject.cs:952`/`HeroObject.cs`）。三套边界不合并。

### MOVE-41 怪物 mover 额外入口闸 +0x480 / 无符号 dir 校验 — FAITHFUL⚠
- 原生怪物 mover 0x71F106 `cmp byte [ebx+0x480],0 / jne fail`(+0x480=m_boHolySeize)；0x71F115 `sub eax,8 / jae fail`(dir>=8 拒)；0x71F11E 落朝向。
- C# `WalkToEntryGateBlocks`（`TBaseObject.cs:1225`）= `m_boHolySeize || HasTimedAbility(13)`；`WalkToDirectionIsValid` = `btDir<=7`。怪物侧对上 +0x480。
- ⚠ 潜伏偏差：**人形 mover 序言（现已 dump，0x741224..0x74124A）确无 +0x480 闸**，而 C# 共用 `WalkToEntryGateBlocks` 对玩家也查 `m_boHolySeize`——但 m_boHolySeize(定身)实战仅施于怪物，玩家路径永不触发，故为潜伏差异、无可观测效果。`HasTimedAbility(13)` 亦非人形 mover 序言所有。旧注释"序言未 dump"已过期，可更正。不改因潜伏且移除 `HasTimedAbility(13)` 有解禁风险；仅报并附证据。

### MOVE-42 怪物边界宽松 >=0 且 <=Width/Height — FAITHFUL
- 原生 0x71F141 `jl`、0x71F14F `jg`、0x71F158 `jl`、0x71F166 `jg`（对比人形 jle/jge）。
- C# `TAnimalObject.cs:34` `nNX>=0 && nNX<=wWidth && nNY>=0 && nNY<=wHeight`。x==Width 再由 MoveToMovingObject 的 [0,Width) 闸(0x779825 jge)兜掉；净效果怪物可达第 0 行/列，玩家不可。

### MOVE-43 走路原语 sub_6BBCD8：清传送闩 / 求向 / 传缓存穿透 — FAITHFUL⚠
- 原生：0x6BBCEC 清 `+0x157`；0x6BBD05 `call 0x764A90`(求向)；0x6BBD0C `mov cl,[ebx+0x3FE]`(缓存穿透) 作 mover 第三参；0x6BBD16 `call [vmt+0x30]`。
- C# `ClientWalkXY`（`TPlayObject.Attack.cs:872`）：`m_bo316=false`(955，=传送闩 +0x157，`TrySpaceMoveToEnvironment:2199` 传送时置 true) → `GetNextDirection`(958) → `WalkTo((byte)n14, false)`(1005)。
- ⚠ 次要偏差：mover 第三参 C# 硬传 `false`，未接原生缓存穿透 `+0x3FE`。`+0x3FE` 是 ability-recalc 缓存（唯一写点 0x6B30A3，值来自 `sub_768454`），普通玩家为 0（=false，等价）；仅当玩家处于"穿透占位"态时有差异。不改因 `sub_768454` 逻辑未证据化，硬接即臆造。低影响。

### MOVE-44 走路原语三分支（败/短/到达）— FAITHFUL
- 原生：0x6BBD1B mover 败→FALSE 无代价；0x6BBD2C/0x6BBD37 未达目标→0x6BBD63 计代价、FALSE；到达/传送(0x6BBD1D 查 +0x157)→0x6BBD39 到达块(`call 0x6E37C4`、`call 0x6F2EC8`、`[ebp-5]=1`、`+0x6C=GetTickCount`、清 +0x3E8/+0x3EC)、再计代价。
- C# `ClientWalkXY`:1005 `if(WalkTo(...))`：WalkTo 假=不计代价(mover 败)；WalkTo 真但 `m_bo316||到达` 否=计代价 `m_nHealthTick-=10`、result 假(短)；到达=计代价、result 真。到达 tick 由 CM_WALK 处理器 `m_dwActionTick=GetTickCount()`(`TPlayObject.Message.cs:1476`，仅成功时)承载(=+0x6C，见 `NativeMotaeboForcedMove.cs:71` MOVE-21 注释)；+0x3E8/+0x3EC 之一 = `m_dwSearchTick`(WalkTo:1329 到达清零)。三分支+代价+tick 核心对上。
- 备注：到达块两个辅助 `sub_6E37C4`/`sub_6F2EC8`(节流类)未逐一映射；可观测的代价/tick/到达判定已忠实。

### MOVE-45 走路代价 +0x10 减 10 — FAITHFUL
- 原生 `sub_6BBCD8` 0x6BBD6E `call 0x76BECC`(edx=0x0A,ecx=0,reset=0)；helper 0x76BEE1 `sub [eax+0x10],edx`。
- C# `ClientWalkXY:1011` `m_nHealthTick -= 10`（+0x10 = m_nHealthTick）。

### MOVE-46 跑步代价 +0x10 减 60 / +0x14 减 10 / +0x20 减 1 — FAITHFUL
- 原生跑步原语 0x6BC0B8 `call sub_76BECC`(0x3C,0x0A) → +0x10-=60、+0x14-=10（0x76BEE7 clamp +0x14>=0）；0x6BC0C4 `call sub_76BEC8`(=`sub byte [eax+0x20],dl`,1) → +0x20-=1。
- C# `ClientRunXY`（`TPlayObject.Attack.cs:859-862`）：`m_nHealthTick-=60; m_nSpellTick-=10; m_nSpellTick=MAX(0,...)`(=+0x14 clamp)；`DecreaseHealthSpellRecoveryStep(1)`(=+0x20 减 1)。逐项对上；资源消耗（非 tick 间隔）为原生限速闸，已复刻。

### MOVE-47 GetNextDirection 符号分桶 + 对角抑制 — FAITHFUL
- 原生 `sub_764A90`：X 抑制 0x764AC8 `cmp eax,2/jle`(距离>=3 起) + 0x764AD0/0x764AD7 `jl/jg`(sx 在 [dx-1,dx+1] 含端)；**Y 抑制不对称** 0x764B12/0x764B17 `jle/jge`(实为 sy==dy)；无匹配落 0x764BB6 `xor eax,eax`(=DR_UP)。
- C# `M2Share.cs:1871`：X `sx>=dx-1 && sx<=dx+1`；Y `sy>dy-1 && sy<dy+1`（`Message` 注释 1909-1916 显式记录该不对称）；default = DR_UP。(sgnX,sgnY)→dir 映射逐格对上。

### MOVE-48 GetRandomXY 常量（step/margin/retry 31）— FAITHFUL
- 原生 `sub_7782D0`：0x77830A step=3、Width<0x32→2；Height>=0xFA→margin 50、>=0x1E→20、else 2；0x778346 `mov esi,0x1F`(retry=31)；X 播种先于 Y（0x7782E4..，`cmp [edi],0/jg` 即 <=0 才 Random(Width)+1）；每轮 `call 0x777EF8`(=CanWalk)，推进或重播 `Random(Dim/2)+margin`。
- C# `TBaseObject.cs:2062` `NativeGetRandomXY`：step/margin/retry(31)/播种序/推进/重播逐条对上（含 `Random(wWidth/2)+nMargin`）。

### MOVE-49 GetRandomXY 失败回退 LinkPoint 列表（受 flag 门控）— FAITHFUL
- 原生 0x7783C1 `mov al,[ebp-5]` → 0x7783C4 `xor al,1` → 0x7783C6 `and al,[ebp+8]` → 0x7783C9 `je`：回退仅当(31 次全败 AND flag!=0)；Envir+0x10 列表、count=[eax+8]、X=word[+0]、Y=word[+2](movzx 零扩展)；空表或 flag=0 才 FALSE。
- C# `NativeGetRandomXY:2102` 用 `m_PointList` 无条件回退。**安全**：C# 侧 `NativeGetRandomXY` 仅被传送路径调用（`SpaceMove_GetRandXY`/`TryResolveNativeUserMoveCoordinates`，均 flag=1，见 `sub_6BD294` 0x6BD336 `push 1 push 1`）；怪物刷新走独立搜索（`UsrEngn.cs` `RegenMonsters`，自带"五连败后置 ignoreCellBlockers"逻辑），**不**经此函数。故丢弃 flag 参数对现有调用者行为等价。

### MOVE-50 只读探针 boIgnoreOccupancy 短路 — FAITHFUL
- 原生 `sub_777EF8`：0x777F4E GetCell → 0x777F53 je 失败 → 0x777F5A 地形 cmp byte[eax],0 → 0x777F6C canWalk=TRUE → 0x777F70 `cmp byte [ebp+8],0 / jne`(boIgnore 非零立即返 TRUE)。
- C# `CanWalk`（`Envirnoment.cs:536`）：GetCell → `mapCell && Valid`(地形) → `if(boFlag) return true`(短路) → 占用扫描。次序一致。

### MOVE-51 同图随机传送旗标（fallback 1 / boIgnore 1）— FAITHFUL
- 原生 `sub_6BD294` 同图臂：0x6BD336 `push 1 push 1` → 0x6BD346 GetRandomXY(fallback=1,boIgnore=1)；0x6BD35E `push 1` → 0x6BD374 MoveToMovingObject(boFlag=1)。落点压在占用者之上、不查占用。
- C# `TrySpaceMoveToEnvironment`（`TBaseObject.cs:2132`）：`SpaceMove_GetRandXY`→`NativeGetRandomXY`(用 `CanWalk(...,true)` boIgnore ✓、总回退 ✓)；落点用 `AddToMap`(恒放置=忽略占用 ✓)。行为等价。
- 备注：同图 C# 用 `DeleteFromMap+AddToMap`(尾插)统一同/跨图，原生同图用 `MoveToMovingObject`(头插)。传送后格内迭代顺序 头 vs 尾 有极细偏差（影响"先取哪个对象"），因统一函数改动风险 > 收益，仅报。

---

## 四、修复决策（为何 0 处代码改动）

按"只补外科式真缺口 / 大缺口·热点只报不改 / 有据不臆造"三重约束逐个评估三处缺口：

1. **MOVE-31（陈旧节点自愈）**：托管内存无悬垂指针，死/幽灵对象经 `IsNativeCellBlocking` 天然不挡路，可观测行为已等价；缺的自愈+日志在 C# 无必要。→ 不改。
2. **MOVE-34（cell[2] 传送点怪物不可走）**：真缺口，但须补"每格 cell+2 字节 + LinkPoint 入格加载器 + +0x178 字段映射"整块子系统，且 cell[2] 的地图来源无逐字节证据。→ 大缺口，仅报。
3. **MOVE-39（人形 mover 尾部三副作用）**：英雄跟随属子系统（C# 大概率经英雄 AI）；清 bodyState 0x17 / sub_778EC0 无字段映射证据。COSMETIC。→ 仅报。

另 3 处次要/潜伏偏差（MOVE-36 谓词 obj+0x34 vs m_nDamage 未逐字节证但行为等价、MOVE-41 玩家侧 m_boHolySeize 闸潜伏、MOVE-43 未接 +0x3FE 穿透）实战不触发或行为已等价，且进一步修法均需未证据化的字段/函数映射，硬改即违"有据不臆造"。→ 仅报。

主体（20 条核心原语）已忠实，无隐藏 bug。故本片交付以**核验报告**为主，无代码提交。

---

## 五、诚实覆盖率

- **判定完整性**：26/26 条逐条给出判定 + C# 位置 + 底本 VA 证据，无 BLOCKED、无跳过。
- **取证深度**：对 MoveToMovingObject、GetCell、两个 mover(含人形序言)、走路原语、GetNextDirection、GetRandomXY(含回退尾)、随机传送、代价 helper、map-event 探针、cell[2] 读写点均做了逐字节反汇编取证。
- **未尽之处（诚实声明）**：
  - MOVE-36 探针 `sub_779204` 的 `obj+0x34!=0` 与 C# `CanSafeWalk` 的 `m_nDamage>0` 未做字段对齐（行为等价、门控一致）。
  - MOVE-44 到达块辅助 `sub_6E37C4`/`sub_6F2EC8` 仅定性（节流类），未逐指令映射到 C#。
  - MOVE-34 cell[2] 的**地图文件来源**未定位（仅确证由 LinkPoint 入格函数 sub_779354 写 1）；若后续要移植须先取该来源证据。
  - MOVE-43 `+0x3FE` 的产生逻辑 `sub_768454` 未展开反汇编。
  - MOVE-39 `sub_778EC0` 未展开；英雄跟随是否等价依赖英雄 AI 侧（跨片）。
  - `+0x178`（MOVE-34）仅知"玩家 0 / 生物非零"，未定位对应 C# 字段名。

---

## 六、分支 / 提交

- 分支：`w/eqv-21`（worktree `D:\loym2\.claude\wt2\eqv-21`，基线 `00841828`）
- 代码改动：无（见第四节）
- 提交：本报告 `docs/eqv_shard21_20260814.md`
- 辅助脚本（未纳入 worktree）：`D:\loym2\staging\_disasm_shard21.py`、`_scan_cell2b.py`、`_scan_3fe.py`
