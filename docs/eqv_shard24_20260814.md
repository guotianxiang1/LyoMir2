# 分片 24（SPAWN / DROP）等价核验报告

- 日期：2026-08-14
- 工作树：`D:\loym2\.claude\wt2\eqv-24`　分支：`w/eqv-24`
- 基线提交：`00841828`（Merge branch 'w/trade-storage'）
- 底本：`D:\loym2\staging\_reunpack_work\flat_image.bin`（ImageBase=0x400000）
- 反汇编：capstone 5.0.7（脚本在 `%TEMP%`，未污染工作树）
- 分片文件：`staging/_shards/shard_24.tsv`（SPWN-06 … SPWN-31，共 26 行，原 csharp_status 全 UNKNOWN、csharp_ref 全 EMPTY）

## 铁律声明

所有判定均先在底本对给定地址/字节做**独立**反汇编核实，再对当前 master 的 C# 逐点比对。二进制与契约文字矛盾处以二进制为准并注明。无确凿证据一律 fail-closed。**本轮未改动任何 C# 源码**（见「为何不改」一节），只产出本报告；因此未触发 `dotnet build`。

## 涉及的三大原生函数与 C# 落点

| 原生 | 身份 | C# 落点（当前 master） |
|---|---|---|
| Phase-B 循环 `sub_67C150` @0x67C245-0x67C2C1 + worker `sub_67C9E0` | 刷怪生成器扫描/补怪 | `GameSvr/UsrSystem/UsrEngn.cs`（`ProcessMonsters` 循环、`RegenMonsters`、`CreateGeneratedMonster`、`GetGenMonCount`）＋ `Monsters/MonGenInfo.cs` |
| `sub_71FA20` = `@AfterScatterItems` | 怪物死亡散落主函数（段1-4＋脚本回调） | `Actors/TBaseObject.Base.cs`（Die 阶梯 1224-1271）＋ `Actors/TBaseObject.cs`（`ScatterGolds`、`m_boNativeScatterConsumed`）＋ `UsrSystem/UserEngine.MonItemsTree.cs`（段1）＋ `UsrSystem/UsrEngn.cs`（`MonGetRandomItems` 段2）＋ `Maps/NativeDropControlRuntime.cs`（段3-4）＋ `ScriptSystem/PasEngine/PasScriptHost.cs`（脚本回调） |
| `sub_71EC88` + 唯一调用者 `sub_71ED80` | 采集/收获型怪物：把怪物掉落表条目**直接塞进采集者背包**（receiver VMT+0x248 = AddItemToBag） | **未实现**（仅在注释中作为共享哨兵的旁证被提及） |

## 逐行结论

图例：FAITHFUL＝C# 与二进制等价；MISSING＝原生行为 C# 未实现；FAIL-CLOSED＝原生细节不可证、C# 有意不复刻；BLOCKED＝契约本身在二进制不可坐实。

| id | 二进制复核 | 判定 | 依据 / C# 落点 |
|---|---|---|---|
| SPWN-06 | ✔ 0x67C288 `cmp dword[esi+0x1C],0`/`je`；0x67CA33 `mov ecx,[ebx+0x1C]`（工厂第3参 ecx） | **FAITHFUL** | 闸三 = `UsrEngn.cs:1729 MonGen.nRace>0`；race 记录经 `AddBaseObject(nRace)` 传入工厂。注释 1720-1725 已坐实 [gen+0x1C]=已解析种族记录 |
| SPWN-07 | ✔ 0x67C291 `sub eax,[esi+0x34]`；0x67C294 `cmp eax,[esi+0x30]`；0x67C297 `jbe`（严格大于才刷） | **FAITHFUL** | `UsrEngn.cs:1731-1732 dwCurrentTick-dwStartTick > dwZenTime`（`dwStartTick==0` 短路等价于原生 now-0 恒大）。间隔取自记录 dwZenTime，非常量 |
| SPWN-08 | ✔ worker 0x67C9F8/`mov ebx,eax`(=edx 第2参)；Phase-B 0x67C278 `mov esi,eax`；无 esi=rec+8 偏置 | **FAITHFUL**（二进制内部） | C# 以托管对象 `MonGenInfo MonGen` 传递，天然无偏置别名问题 |
| SPWN-09 | ✔ 0x67CA12 `mov eax,[ebx+0x3C]`；0x67CA15 `cmp dword[eax+edi*4],0`/`jne` 跳占用槽 | **FAITHFUL** | `UsrEngn.cs:3462-3505` + `3471-3475`：以 `CertList.Count>=nCount` 封顶＋逐次 Add 表达"填空槽"。数据结构（定长带洞 vs 压实 List）不同但可观测等价，已注释 |
| SPWN-10 | ✔ 0x67CA3B `call 0x679F8C` | **FAITHFUL** | `UsrEngn.cs:3492 CreateGeneratedMonster`→`3358 AddBaseObject`（怪物工厂封装） |
| SPWN-11 | ✔ 0x67CA9B `inc [ebx+0x2C]`；0x67CAA1 `inc [eax+0x34]`(self) | **FAITHFUL** | `UsrEngn.cs:3375 nActiveCount++`、`3376 nMonsterCount++`（注明 0x67CA9E..0x67CAA1） |
| SPWN-12 | ✔ 0x67CA47 `je`→0x67CAB8 `inc[ebx+0x38]`；成功侧 0x67CAA7-A9 清零 | **FAITHFUL** | `UsrEngn.cs:3495 nFailCount++`（失败）/`3498 nFailCount=0`（成功）逐点一致 |
| SPWN-13 | ✔ 0x67CA49 `cmp dword[ebx+0x28],0`；0x67CA52 `mov dx,word[ebx+0x28]`；0x67CA56 `mov word[eax+0x38],dx` | **MISSING** | worker 把可选生成器字段 [gen+0x28] 低字写入新怪 [mon+0x38]。`CreateGeneratedMonster`(UsrEngn.cs:3345-3413) 无此字段与写入。`MonGenInfo` 未建模 [gen+0x28]。**在禁改文件内，只报不改**（见台账建议） |
| SPWN-14 | ✔ 0x67CA00 `call 0x406A90`(High,后 `inc esi`)；0x67CA5D `mov eax,[eax+0x40]`＋0x67CA60 `call 0x406A88`(Length,直接当计数) | **MISSING**（身份 INFERRED 已证：0x406A90=`call 0x406A88;dec eax`=High，0x406A88=`mov eax,[eax-4]`=Length） | 生成成功且 [gen+0x40] 播报串数组非空时 0x67CA69-8D 播报（color 0x38FF, dx=0x64）。C# 刷怪路径无此播报。**禁改文件内，只报不改** |
| SPWN-15 | ✔ 0x67D00A `mov eax,0x3C`＋`call 0x402FA0`；0x67CF75/7C/85 `[ebx+0x28]=5 / [ebx+0x2C]=1 / [ebx+0x30]=0`；+0x18 为 ShortString[14](0x67CF6E cl=0xE) | **FAITHFUL**（二进制侧核实）；C# 不单独建模该配置记录 | 但契约"+0x10 与 +0x14 与 +0x18 均 ShortString len 0x0E"**部分与二进制不符**：+0x10(0x67CF1B)、+0x14(0x67CF32) 接收的是虚方法 `call[esi+8]`(分词器) 的返回值 eax（整型/AnsiString 指针），非 ShortString[14]；仅 +0x18 为 ShortString[14]。**建议修台账**（见下） |
| SPWN-16 | 契约自述 UNPROVEN（配置记录↔运行期 generator record 字段对应未坐实） | **BLOCKED** | 维持未证；C# 用单一 `MonGenInfo` 承接 MonGen 语义，不复刻配置记录内部布局 |
| SPWN-17 | ✔ 0x71EC94 `mov ebx,eax`(self)；0x71EC91 `mov[ebp-4],edx`(接收方)；0x71ED76 `mov al,[ebp-5]`(是否至少掉一件) | **MISSING** | `sub_71EC88` 采集直交付函数 C# 无实现（仅 `TBaseObject.cs:282` 注释提及） |
| SPWN-18 | ✔ 0x71ECA4 `cmp dword[ebx+0x474],0`；0x71ECB1 `cmp byte[ebx+0x47F],0`；0x71ECBE `mov byte[ebx+0x47F],1`（先置哨兵后掉落） | **FAITHFUL**（字段与哨兵语义已建模） | [self+0x474] 掉落表 = 怪物 `ItemList`；[self+0x47F] 一次性哨兵 = `m_boNativeScatterConsumed`（`TBaseObject.cs:296`）＋`TryEnterNativeScatter()`(顶端无条件置位)。注：sub_71EC88 消费者本身缺失 |
| SPWN-19 | ✔ 0x71ECC5 `mov eax,[ebx+0x474]`；0x71ECCB `mov esi,[eax+8]`(count@+8)；0x71ECE8 `call 0x424D4C`(TList.Get)；[ebp-0xC] 单调递增 | **MISSING** | sub_71EC88 的 TList 遍历 C# 无实现 |
| SPWN-20 | ✔ 0x71ED04 `cmp eax,[edx+0x10]`(门限)；0x71ECF9 `[eax+0x14]`(模数)；0x71ED0C `[eax+0x18]`(名指针)，为0则 nil continue | **MISSING** | 条目偏移与 `TMonItem` 语义一致，但 sub_71EC88 的取用 C# 无实现 |
| SPWN-21 | ✔ 0x71ED1E `call 0x751BBC`（eax=[[0x7D5D6C]]、edx=名、ecx=0）；0x751BBC=`test edx;je;call 0x74C338(类工厂)` | **MISSING** | 采集侧按名造物 C# 无实现 |
| SPWN-22 | 契约自述 UNPROVEN（交付走 receiver VMT+0x248，无 VMT dump） | **BLOCKED**（+C# MISSING） | 旁证：+0x248 在 C# 台账多处坐实为 AddItemToBag（`NativeItemAcquisitionStamp.cs:8` 等，68 处 xref）。但采集机制本身 C# 缺失 |
| SPWN-23 | ✔ 0x71ECFC `call 0x403B4C` 模数[rec+0x14]；0x71ED04 cmp；0x71ED07 `jg`→continue（门 ≤，每条恒定1抽、无 imul） | **MISSING** | sub_71EC88 的 1 抽无倍率逻辑 C# 无实现（与 sub_71FA20 段2 唯一差别=无 imul，已在段2侧确认） |
| SPWN-24 | ✔ 0x720158 AnsiString len=0x12="@AfterScatterItems"；0x720062 `push 0x720158`（函数体内真 xref） | **FAITHFUL** | sub_71FA20 身份确认；C# 经 `TBaseObject.Base.cs:1270 TryCallAfterScatterItems` + Die 阶梯落地，段1-4 齐备 |
| SPWN-25 | ✔ 0x71FA3C/39/36 = eax/edx/ecx(self/killer/anchor)；0x71FA7B-84 anchor==0→self；0x71FFB7 `[ebp+8]`(金币上限)；0x71FBD8 `[ebp+0xC]`(分支) | **FAITHFUL** | 参数语义在 Die 阶梯与 `ScatterGolds(nativeMonsterScatter)` 复现；金币上限开关=1 对应 monster 路径（注释 5162-5171 坐实两参来源） |
| SPWN-26 | ✔ 0x71FA50 `cmp byte[eax+0x47F],0`；0x71FA57 `jne` 整函数退；0x71FA6C 置1 | **FAITHFUL** | 与 sub_71EC88 共用 `m_boNativeScatterConsumed`；`TryEnterNativeScatter()` 最左置位（`TBaseObject.Base.cs:758`、Die 阶梯 1224） |
| SPWN-27 | ✔ 0x71FA62 `[ebp-0x2C]=1`；0x71FB1E `cmp byte[ebx+0x1828],2`；0x71FB27 `[ebp-0x2C]=2`（ebx=killer @0x71FAD7） | **FAITHFUL** | 倍率=`m_btNativeFatigueTier==2 ? 2 : 1`：段2 `UsrEngn.cs:2468`、段4 `TBaseObject.cs:5189`。tier 字节=[self+0x1828]（`TPlayObject.Base.cs:461`） |
| SPWN-28 | ✔ 0x71FD3A `imul dword[ebp-0x2C]`(放大模数[rec+0x14])；0x71FFD1 `idiv dword[ebp-0x2C]`(缩小金额，先0x71FFC6封顶0xBB8后除) | **FAITHFUL** | 段2 `Random(MaxPoint*penalty) <= SelPoint`(`UsrEngn.cs:2472`)；段4 `ScatterGolds` 先 `Cap 3000`(5184-5186) 后 `/goldDivisor`(5190)，顺序与 0x71FFC6<0x71FFD1 一致 |
| SPWN-29 | ✔ 0x7200D0 GBK16"怪物爆出被防沉迷"、0x7200EC GBK8"被防沉迷"；0x71FB08 `dx=0xA2`；0x71FB0F `call 0x768BE0` | **FAIL-CLOSED** | 防沉迷**拦截**已实现（`NativeAfterScatterItemsBlocked`, tier3 阻断散落）；但该分支的日志/播报 `call 0x768BE0` 因参数不可证（见 SPWN-30/31）有意不复刻，注释 798-803 明示 |
| SPWN-30 | ✔ 0x71FB03 `mov ecx,0x7200EC`（ecx 装的是串指针，非颜色常量） | **BLOCKED** | 颜色字未捕获属实；C# 未复刻该播报（fail-closed），三色对照不适用 |
| SPWN-31 | ✔ 0x76ADF2 字节 `02 00 00 0F 00 33 C0 89 83 9C`（`33 C0`=xor eax,eax、`89 83 9C..`=mov[ebx+0x9C],eax，实为代码非串） | **BLOCKED** | push 0x76ADF2 参数身份不可判定属实（该地址落在代码区，非合法串窗）；C# 未复刻 |

## 状态计数（26 行）

- **FAITHFUL：14** — SPWN-06, 07, 08, 09, 10, 11, 12, 15, 18, 24, 25, 26, 27, 28
- **MISSING：7** — SPWN-13, 14, 17, 19, 20, 21, 23
- **BLOCKED：4** — SPWN-16, 22, 30, 31
- **FAIL-CLOSED：1** — SPWN-29

（原分片 26 行 csharp_status 全为 UNKNOWN，本轮全部落定。）

## 主要缺口（只报不改的依据）

### 1. 采集/收获直交付机制（sub_71EC88 + sub_71ED80）— SPWN-17,19,20,21,23（＋22）
- `sub_71ED80` 是**怪物虚方法**（全镜像 xref：115 个怪物 VMT 槽引用），入口 `call 0x772DA8` 通过后按 [self+0x47E] 门、[self+0x2EC] 已办标志把 [self+0x4A0]（`-= Random(0xC9)+0x64`）、[self+0x4A4]（`-= Random(0x10)+5`）两个产量计数递减；race∈[0x32,0x50) 触发变身（置 [self+0x2EC]=1，`call [vmt+0xD8]` dx=0x2728）；随后 `call sub_71EC88` 把怪物掉落表条目逐条 1 抽（门 ≤、无防沉迷倍率）并经 **receiver VMT+0x248 = AddItemToBag** 直接塞进采集者背包；一件都没掉时播报 GBK"没有获得任何东西"(0x71EF94)。
- C# 当前掉落路径只有 @AfterScatterItems（散落到地面），**无任何采集直交付逻辑与触发器**。属**整机制缺失**，非单点，按规则只报不改；建议单开分片/波次移植（需先定位该虚方法的间接调用点=玩家攻击/交互臂）。

### 2. 生成器可选字段 — SPWN-13 [gen+0x28]→[mon+0x38]、SPWN-14 [gen+0x40] 播报
- 两者均在 worker `sub_67C9E0`/`CreateGeneratedMonster` 内，落点在**禁改文件** `UsrEngn.cs`，且需在 worker 主流程内插桩（无法仅靠新 partial 文件外挂调用点），故本轮不改。
- 观测：`MonGenInfo` 未建模 [gen+0x28]（低字→怪 [mon+0x38]，疑为固定外观/事件标签）与 [gen+0x40]（播报串动态数组）。若标准 MonGen 配置从不填充这两字段，则 C# 省略与原生"字段为 0/空即跳过"可观测等价（此点取决于配置装载，未坐实）。

## 建议修台账（契约文字 vs 二进制）

1. **SPWN-15**：将"字段含 +0x10 与 +0x14 与 +0x18 的 ShortString 长度 0x0E"更正为"仅 +0x18 为 ShortString[14]；+0x10、+0x14 接收分词器虚方法 `call[esi+8]` 的返回值（0x67CF1B / 0x67CF32，整型或 AnsiString 指针），非 ShortString"。其余（0x3C 分配、+0x28=5/+0x2C=1/+0x30=0）属实。
2. **SPWN-22**：可补旁证——receiver VMT+0x248 在 C# 侧台账多处已坐实为 AddItemToBag（`sub_6B7378`，68 处原生 xref），虽仍缺 VMT dump 的自校验，但身份高度可信，可从纯 UNPROVEN 降级为"待 VMT dump 确认的 AddItemToBag"。
3. **SPWN-13/14/17/19/20/21/23** 的 csharp_status 应由 UNKNOWN 落定为 MISSING；**SPWN-06…12/15/18/24…28** 落定为 FAITHFUL；**SPWN-29** 为 FAIL-CLOSED；**SPWN-16/30/31** 维持 BLOCKED。

## 旁证观察（非本分片契约，供参考）

- Phase-B 逐生成器门序里 C# 多出一道 `!M2Share.g_Config.boVentureServer`（`UsrEngn.cs:1730`），在原生 0x67C27D-0x67C297 的四道门（nCount、名长、[gen+0x1C]、间隔）中**无对应**。属旁证，在禁改文件内，未处理；建议后续核该 venture 门是否有更粗粒度的原生依据或应移除。

## 复核方法

- 反汇编脚本：`%TEMP%\disx.py`（capstone，`d`=反汇编/`r`=裸字节）、`%TEMP%\xref.py`（e8/e9 相对调用 + 绝对 dword 引用扫描）。均在工作树外，未提交。
- 关键 xref 结论：`sub_71EC88` 唯一调用者 0x71EE5C（在 `sub_71ED80` 内）；`sub_71ED80` 115 处 VMT dword 引用；`0x71EF94`=GBK"没有获得任何东西"；`0x751BBC`=按名造物（→0x74C338）。
