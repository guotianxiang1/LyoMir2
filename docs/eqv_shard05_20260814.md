# 等价核验报告 — 分片 05（TRADE 尾 + CRAFT 头）

- 契约清单：`staging/_shards/shard_05.tsv`（26 条：TRADE-40..61 + CRAFT-01..04）
- 底本：`D:/loym2/staging/_reunpack_work/flat_image.bin`，ImageBase `0x400000`
- 反汇编：capstone x86-32（`_reunpack_work/disasm_mb.py`），python 3.11
- 工作树：`w/eqv-05` @ `00841828`（`.claude/wt2/eqv-05`）
- 判定基线：当前 master C# + 二进制对照；不 dotnet build。

## 状态统计

| 判定 | 条数 | 契约 |
|---|---|---|
| FAITHFUL | 18 | TRADE-40,41,42,43,45,46,48,49,51,53,56,57,58,59；CRAFT-01,02,03,04 |
| FAIL-CLOSED | 2 | TRADE-44,47 |
| MISSING（内部/无害） | 1 | TRADE-50 |
| BLOCKED（依赖） | 1 | TRADE-61 |
| VERIFIED + 备注（只报不改） | 4 | TRADE-52,54,55,60 |

**修复提交：0（无外科式真缺口）。** 所有缺口均落在①依赖阻塞、②交易成交/日志热区（他代理在改，只报不改）、③内部只写统计三类，均非外科可补。另发现 **3 处契约描述与二进制不符**（TRADE-49、TRADE-53、TRADE-54），C# 侧均已按二进制真相实现或不受影响。

---

## 仓库存取核心（TRADE-40..46）

底本：存入 `sub_6C2A34`（统一 type 分派，`[ebp+8]`=1/2/3），取出 `sub_6C2D7C`，容器名串确认：
`0x6c2d48`="普通仓库"→`[+0x6d0]`(type1)，`0x6c2d5c`="账号仓库"→`[+0x6d4]`(type2)，`0x6c2d70`="药品仓库"→`[+0x6d8]`(type3)。
C# 拆成三法：`ClientStorageItem`(type1, Operate.cs)、`ClientNativeAccountStorageItem`(type2)、药品仓 fail-closed。

- **TRADE-40 FAITHFUL** — 存入先加仓再删包。原生 `0x6C2B79`(add via `0x74a2e4`) 早于 `0x6C2C08`(`bag.Delete`)。C# `ClientStorageItem` 2306-2307 `m_StorageItemList.Add` → `m_ItemList.RemoveAt`；账号仓 Operations 115-117。崩溃期复制窗口=原生同款（契约明示是复制窗口非丢失窗口），未"改良"→忠实。
- **TRADE-41 FAITHFUL** — 取出先加包再删仓。原生 `0x6C2ED5`(AddItem via VMT+0x248) 早于 `0x6C2EFA/0F0A/0F1A`(仓库 remove)。C# 2400-2403 `AddItemToBag` → `RemoveAt`；账号仓 179-189。
- **TRADE-42 FAITHFUL** — 绑定拒收。`0x784060`=`[eax+0x1c]`→`test byte[+2],0x80`(std[+2]&0x80，类型无关，在分派前 `0x6C2B36`)；账号仓额外 `0x78406c`→`0x784710`→`cmp ax,1`(bindType==1)。C# 个人仓 2299 `NativeReserved02 & 0x0080`；账号仓 `IsDepositRestricted` 51-59 同时含 `&0x80` 与 `btValue[10..12]==1`。
- **TRADE-43 FAITHFUL** — 存入降序、托管升序。原生存入 `[ebp-8]=count-1` downto（`0x6C2CE7 dec/jne`）；托管 `sub_6C41xx` `[ebp-0xc]=0` 升序（`0x6C41FB`）。C# 存入 `for(i=Count-1;i>=0;i--)`(2277)，托管 `for(i=0;...)`(1680)。
- **TRADE-44 FAIL-CLOSED** — 药品仓(type3)存入 `0x6C2CBF call 0x404690`(TObject.Free，虚拟仓不存对象)。C# 药品仓整体未落地（DrugStore 需 DBSvr 后端，见 TRADE-35/`m_trade35_drugstore`），`RejectUnsupportedStorageItem` 静默拒收 = 原生"未加载态"`0x6C2A99 call 0x74a854 / je 0x6c2d15`（静默 ret 不发包）。释放对象行为落在未实现路径内→N/A，fail-closed 不丢不复制。
- **TRADE-45 FAITHFUL** — 取出 AddItem 失败留仓+独立消息。原生 `0x6C2FAC` SM `0x2C3`(FULLBAG, AddItem 假)；`0x6C2FF1` SM `0x2C2`(FAIL, ecx=esi 错误码 -2/-3)。C# 2417-2420 `SM_TAKEBACKSTORAGEITEM_FULLBAG`(=707=0x2C3)且 `RemoveAt` 仅在成功臂；2368 dealing 门发 `SM_TAKEBACKSTORAGEITEM_FAIL`(706=0x2C2) 带 -2。
- **TRADE-46 FAITHFUL** — 容器选择器 `nParam3` 0..2→type1..3，≥3 静默丢。原生 `0x6D91C2 sub ax,3 / 0x6D91C6 jae 0x6dbc2c / 0x6D91D3 inc eax`(0..2→1..3)。C# `TPlayObject.Message.cs` 1376-1403：`switch(wParam){0→个人仓;1→账号仓;2→药品仓(fail-closed)}`，≥3 落空=丢弃。

## 交易生命周期（TRADE-47..50,60）

- **TRADE-48 FAITHFUL** — 登出/Run 只在 partner 空/前方不符/自指时取消。原生 `sub_6B2D38`(TPlayer Run, VMT `0x6AC8C8`+0x88)`0x6B2E79..0x6B2EAF`。C# `Run()`(Message.cs 287-293)：`if(m_boDealing){ if(GetPoseCreate()!=m_DealCreat || m_DealCreat==this || m_DealCreat==null) DealCancel(); }` — 逐条对应。
- **TRADE-47 FAIL-CLOSED** — 原生 Run 内 30s 节流"半清扫"仅清 partner 指针 `[eax+0xBAC]=0`（`0x6B3B4B..0x6B3B87`，`now-[self+0x73C]>=0x7530`），留 dealing 标志与押金悬挂（登出满足空指针析取故仍可退押金=可恢复）。C# **未复刻此部分清扫**，改由 TRADE-48 的每 tick 全量取消覆盖同一触发面（ghost/离开前方→`GetPoseCreate()!=partner`→`DealCancel` 退押金），押金永不悬挂→更安全的 fail-closed 收敛。属交易生命周期热区，只报不改。
- **TRADE-49 FAITHFUL（契约标签有误，C# 已按真相实现）** — 契约称"受伤取消交易"，**实为被推挤移动取消**。`TPlayer VMT+0xA4 = [0x6AC96C] = 0x6BFD1C`（已验），`0x6BFD1C` 调 `0x76834C`（该函数更新 `[obj+0x128/12c/130]` 坐标、`0x7797cc` 可行走判定、返回**移动格数**，非伤害），`0x6BFD48 jle / 0x6BFD4C call 0x6C43C4`(DealCancel)。即"被推动 ≥1 格才取消"。C# `TBaseObject.CharPushed` 1816-1818 `if(result>0 && RC_PLAYOBJECT) DealCancel()`——已正确捕获真实语义并在注释中记录该纠正。
- **TRADE-50 MISSING（内部只写统计，无害）** — 全局成交计数器 `0x6C4758 inc dword[0x7D3A90]`（成交校验后、变更块前）已确认。全镜像对 `0x7D3A90` **仅 1 处引用即该 inc 本身**（只写不读），纯统计，C# 省略零 gameplay 影响；且处于成交提交热区。只报不改。
- **TRADE-60 UNPROVEN-CONFIRMED** — `sub_6B2C7C`(DealCancelA) 两调用点已验：`0x6B1C17`(存档定时器) + `0x6518C8`(位于 `0x651604`，其外层生命周期事件仍未定名——大栈帧函数，入口 `call 0x408340`，含 `[ebp-0x18]` 阶段标记 4/5，`0x6518D7 call 0x772da8`)。C# 在所有存档前调 `DealCancelA()`（UsrEngn 1533 周期、1414/1544 ghost、GameServer 64 关服），为安全超集。只报。

## 日志（TRADE-51..53，均交易成交/仓库日志热区，验证后只报不改）

- **TRADE-51 FAITHFUL** — 类型码：存入 `1`(原生 `0x6C2C8E/CB2 mov dx,1`；C# 2314 `'1'`)、取出 `2`(`0x6C2F70/F98 mov dx,2`；C# 2414 `'2'`)、成交双向及金币 `8`(`0x6C47E6/0x6C4879 mov dx,8`；C# 1962/2000/2014/1980 `'8'`)。金币行描述列=金币(`sSTRING_GOLDNAME`)，方向靠 actor/counterparty 列区分。
- **TRADE-52 VERIFIED（原生格式）/ C# 架构性替代** — 记录 196(0xC4) 字节：`0x79D40D` magic `0x33AABB77`、`+4` ver=1、`+6` word `0xBC`、`0x4A0684` len `0xC4` 发送。C# 全仓库**无 `0x33AABB77`**，改用文本 `AddGameDataLog`（TAB 分隔），属既定架构差异（无 LogServer 二进制协议）。
  - **CONFLICT（TRADE-52"账号名" vs CRAFT-32"地图名"）本报告证据倾向地图名**：`rec+0x08`(20 字节串)由 `sub_768BE0`→`sub_79D3D8` 的 `ecx` 参喂入，`ecx` = `player+0x115` 经 `0x405774` 转串；`player+0x106` 是字符名 ShortString(≤14 字符占 15 字节，止于 +0x114)，紧邻其后的 `+0x115` 与地图名字段布局吻合；记录另经 `0x768C1C` 单独携带 `+0x106` 字符名。未见 account 名进入本记录。→ 支持 CRAFT-32(map name)，不支持本行"account name"标签。
- **TRADE-53 FAITHFUL（契约误读，C# 已按真相实现）** — 契约称"限时物品谓词为真时整跳日志，`0x783984` 读 item[+0xF4]/[+0xF8] 对 `0x1D4C0`"。**实测 `0x783984` = `xor eax,eax; ret`（恒 0 桩）**；带 +0xF4/+0xF8/0x1D4C0 的真谓词在**相邻另一函数 `0x783988`**。两成交物品循环调的是桩：`0x6C47A9 call 0x783984` 与 `0x6C48D1 call 0x783984`，`test al,al / jne` 恒不成立→**日志必发、从不跳过**。C# 无条件发日志(1960-1963/1998-2001)=忠实。（数量字段硬编码 1 属另一 BLOCKED：item+0x14 无 C# 字段映射，取 `!=7` 分支 fail-closed。）

## 普查/死代码/参数忽略（TRADE-54..56）

- **TRADE-54 主张成立、副主张不准** — 全镜像 `66 BA lo hi`(mov dx,imm) 普查：**主张 VERIFIED** — `0x2A5-0x2A8`(677-680) 各 0 站点→逐件删除消息族不存在；`0x2AB`、`0x2B0`、`0x2B2-0x2BB` 亦 0。**副主张"0x2B0-0x2BC 皆缺"不准** — `0x2B1`(689) 有 2 真实站点(`0x6B4B98`、`0x6D1427`，均随 `call [.+0x250]`)、`0x2BC`(700=`SM_SENDUSERSTORAGEITEM`) 1 站点(`0x6B55BF`，紧邻开箱 `0x74a868`)。对 C# 无影响（缺失消息本就无需实现）。
- **TRADE-55 VERIFIED（二进制）/ C# config 门控扩展，默认对齐** — `Self+0x683` 三写点 `0x6B0A80`/`0x6B0AC0`(`C6 80 83 06 00 00 00`) 与 `0x6D1349`(`C6 86 83 06 00 00 00`) **全存 0**，无处 =1→原生取仓 +0x683 门(-3 出口 `0x6C2FDB` 及 `0x6C8E6F` 块)死代码。C# `m_boCanGetBackItem` 默认 false，但登录 `Base.cs:1450 = !boLockGetBackItemAction`（配置默认 false→置 true→放行=原生行为）；`@LOCK/@UNLOCK` 为选配。默认态 1:1，锁仓为 opt-in 扩展族（含 SETPASSWORD 命令），删除会破坏整族→只报不改。
- **TRADE-56 FAITHFUL** — 资格 helper `sub_78389C`：`0x7838A3 mov [ebp-1],cl` 存 Self+0x4B7；**仅 mode 3 的 `0x78392A cmp byte[ebp-1],0` 读它**；托管走 mode 2(`0x6C4229 mov edx,2`)，其体 `0x783911` 从不读→该参在交易路径被忽略。C# `NativeItemDropDestroy.CheckTransferPermission` mode 2 分支(118-123) 只测 `Reserved02&0x0200`，不引用 +0x4B7。

## 未证/门/表（TRADE-57..59,61）

- **TRADE-57 已定名为 CM_WANTMINIMAP、FAITHFUL** — opcode 1033 = `CM_WANTMINIMAP`(0x409)。`sub_6C3070`：`[edx+0x128]`(map)→`[eax+0xc]`(map+0xC)，`>0`→SM `0x2C6`(=710 `SM_READMINIMAP_OK`)否则 `0x2C7`(=711 `SM_READMINIMAP_FAIL`)。**触碰任何仓库容器为假、仅读 map+0xC 二选一消息为真**。C# `ClientGetMinMap` 2069-2080：`m_PEnvir.nMinMap>0 ? OK(带 nMinMap) : FAIL`。契约"未证"可升级为已证。
- **TRADE-58 FAITHFUL** — DealTry 门 `0x6C3F5E cmp byte[esi+0x178],0 / jne 拒绝`。+0x178 为生物种类(race)标签（怪/英雄/NPC 各写不同非零常量），语义="对端须为纯玩家"。C# `ClientDealTry` 1617 `TargetPlayObject.m_btRaceServer == RC_PLAYOBJECT`（=+0x178==0）。字段可识别为 race 码。
- **TRADE-59 FAITHFUL** — std 位测试在 `sub_78389C` 体 `0x783901..0x78396B`：std[+2]&0x80、&0x20，std[+3]&0x02、&0x04、&0x08(前置阶) 等，返回码 1-6。C# `CheckTransferPermission` 用原始 `NativeReserved02` 掩码 0x0080/0x0200/0x0400（+ 前置阶），不臆造人类可读名。
- **TRADE-61 BLOCKED** — 仓储层级/金币上限配置表 `[0x7D6608]`、`[0x7D6080]` 为运行期加载的容器指针（镜像内 0x7B45C4/0x7B45BC，内容不在静态镜像），仅索引可证。属 config/DBSvr 依赖，无法从镜像补内容。

## 物品打造 opcode 1034（CRAFT-01..04）

C#：`Message.cs:1408 CM_USERMAKEDRUGITEM → ClientMakeDrugItem`(Operate.cs 2082) → `Merchant.ClientMakeDrugItem`(Merchant.cs 2294)。

- **CRAFT-01 FAITHFUL** — 1034 非独立制药动词：NPC 须已备货，包内串对 NPC 货架名匹配。`sub_63FE2C` 遍历 `npc+0x56C`(货架)，取 `stock[0]` 内首项 `0x784568`(GetItemName) 对包串 `0x40591c` 比较；无备货则结果保持 1(失败)。C# `Merchant.ClientMakeDrugItem` 2309-2321 遍历 `m_GoodsList`，`List1C[0]` 的 `StdItem.Name == sItemName`。配方表单独不足=成立。
- **CRAFT-02 FAITHFUL/confirmed** — 1034 唯一到达其 case 标签。跳表标签 `0x6D924A`(`test si,si/jbe` 后)→`0x6D9262 call 0x6c4b14`(handler)；相邻 `0x6D9240 call 0x6c3070` 是 1033。C# CM_USERMAKEDRUGITEM 单臂分派。
- **CRAFT-03 FAITHFUL** — 1034 路径全程无 RNG，成功全确定。call 普查：`sub_6C4B14`{`0x649a58,0x64a844`(NPC 查),`0x7743e0`(距离),`0x405708`,`0x63fe2c`}；`sub_63FE2C`{`0x424d4c,0x784568,0x40591c,0x6d7960`(金币≥100 检),`0x6c4bcc`,`0x74c2fc`,VMT+0x248,`0x6c7d64`(DecGold),`0x768be0`(log),`0x404690`}；`sub_6C4BCC`{`0x74e0f8,0x74089c,0x403b2c,0x74c1e0,0x424d4c,0x424b30,0x404690,0x765f6c`}——**无 Random**。C# 确定性（金币 + 材料检查）。
  - 附带核实（金币成本，非本片契约）：原生成本硬编码 100（`0x63FF3A mov edx,0x64` DecGold + 预检 `0x6d7960` edx=0x64）；C# `nMakeDurgPrice` 唯一赋值 `GameSvrConfig.cs:1166 = 100`、无 ini 覆盖→恒 100=原生字面量，一致。
- **CRAFT-04 FAITHFUL** — NPC 目标是**指针非 id**：`0x6C4B33 cmp [ebx+0xCD8],0`(非空) + `0x6C4B3C cmp esi,[ebx+0xcd8]`(包 Recog==当前 NPC)+ 双查找器拒 ghost(`byte[+0x73]`)+ 同图(`0x6C4B74`)+ 半径 15 闭区间(`0x6C4B7C`)。C# 2100-2110：`m_NPC==null || m_NPC.ObjectId!=NPC || m_boGhost` → return；`m_PEnvir` 同图 + `Abs(dx/dy)<=15`。旧 C#"全局查商人 + m_boMakeDrug(恒不 true→通道死)"已修为自持 `m_NPC` 指针。

---

## 覆盖率

- 26/26 契约逐条对二进制 + 当前 master C# 核验，均附证据地址。
- 反汇编取证函数：`sub_6C2A34`(存入)、`sub_6C2D7C`(取出)、`sub_6D91xx`(选择器/1033/1034 分派)、`sub_6B2D38`(Run/TRADE-47/48)、`0x6BFD1C`(CharPushed)、`sub_6C4580`(成交/50/51/53)、`sub_79D3D8`+`sub_768BE0`(日志记录)、`sub_78389C`(资格/56/59)、`0x6C3F00`(DealTry/58)、`sub_6C3070`(1033)、`sub_6C4B14`+`sub_63FE2C`+`sub_6C4BCC`(打造)。
- 脚本取证：`mov dx` 全镜像普查(TRADE-54)、`Self+0x683` 三写点(TRADE-55)、`0x7D3A90` 引用扫描(TRADE-50)。

## 未改动理由（0 修复提交）

- **依赖阻塞**：TRADE-44（DrugStore 需 DBSvr）、TRADE-61（配置表运行期加载）。
- **成交/日志热区（他代理在改，铁律只报不改）**：TRADE-47,50,51,52,53。
- **内部只写统计、无 gameplay 影响**：TRADE-50。
- **config 门控、默认对齐原生的既有扩展族**：TRADE-55。
- **契约描述与二进制不符，C# 已按真相实现或不受影响**：TRADE-49（推挤非受伤）、TRADE-53（恒 0 桩非限时谓词）、TRADE-54（0x2B1/0x2BC 实存）。
- 其余 FAITHFUL 项 C# 已忠实落地，无外科缺口。

## 分支/提交

- 分支：`w/eqv-05`（基 `00841828`）
- 提交：本报告 `docs/eqv_shard05_20260814.md`（无代码修改）
