# 分片 23 等价核验报告（MOVEMENT/TELEPORT + SPAWN/DROP）

- 工作树 / 分支: `D:\loym2\.claude\wt2\eqv-23` @ `w/eqv-23`
- 底本: `staging\_reunpack_work\flat_image.bin`（ImageBase=0x400000），capstone 5.0.7 独立反汇编
- 分片: `staging\_shards\shard_23.tsv`（27 行；原 TSV 全部 `csharp_status=UNKNOWN`、`csharp_ref` 空——已对当前 master 重核）
- 日期: 2026-08-14
- 本次提交:
  - `c7506293` MOVE-83 NORIDE 拒绝改硬编码 0xFCFF 原始发送
  - `2fe4da73` MOVE-78 定点石 setter 的 map+0x67 门补齐 `boLIMITITEMMOVE`
  - （本报告提交见文末）

## 结论计数（27 行）

| 判定 | 计数 | 行 |
|---|---|---|
| FAITHFUL（native 证实 + C# 等价） | 11 | MOVE-78*, 80, 81, 83*, 84, 88, 91, 92, 93 ; SPWN-01, 02 |
| FAITHFUL（负向断言，C# 未在移动路径接线，符合原生） | 3 | MOVE-86, 87, 98 |
| FAITHFUL（SPWN 消费者在 UsrEngn.cs，只读核对） | 3 | SPWN-03, 04, 05 |
| MISSING（native TIER1 证实、C# 消费者/消息未移植，热点/大函数只报不改） | 5 | MOVE-79, 82, 85, 89, 90 |
| BLOCKED / UNPROVEN（native 本身未证，C# fail-closed 未臆造） | 4 | MOVE-94, 95, 96, 97 |

`*` = 本次外科修复后达 FAITHFUL。

> 台账建议：MOVE-90/92/93 的契约文字系旧快照，与二进制矛盾；SPWN-05 语义标签需细化。详见文末「台账修正建议」。

## 逐行结论

### MOVEMENT / WALKABILITY / TELEPORT

| id | native 复核（独立反汇编） | csharp_ref | 判定 |
|---|---|---|---|
| MOVE-78 | sub_6E9BAC：`[esi+0x67]==0` 门 + `[esi+0x44]` 取图名调 vmt+0x54 + 失败发 str@0x6E9DBC(len30,cx=0x38FF,vmt+0xD4)。全镜像证实 map+0x67 由 NORECALL 与 LimitItemMove **两 token 两池**置位（0x7751BB/0x776382、0x775A5C/0x7769E8）。 | `Players/TPlayObject.NativeFixedCoordStone.cs` (`ClientSetFixedCoord`) | **FAITHFUL（已修）** |
| MOVE-79 | sub_7274B4：`[ebx+0xBA4]≠0` && `[Envir+0x67]==0` && `[Envir+5]==0`→sub_6BF458；否则 `无法对 `(str@0x7275A4 len7)+name+` 使用天地合一`(str@0x7275B4 len13)，cx=0x38FF vmt+0xD4。 | 字段: `Players/TPlayObject.NativeUnmappedScalars.cs`（+0xBA4=TianDiHeYi 已建模）；技能消费者未移植 | **MISSING**（消息/技能） |
| MOVE-80 | sub_6CE400 @0x6CE452 `cmp [eax+0x6b],0`(NOPOSITIONMOVE)，拒绝 @0x6CE4FE。set-1 0x77528B `c6436b01`；复合 LimitItemMove 0x775A64 同置 +0x6B。 | `Command/Commands/UserMoveXYCommand.cs`；`Maps/Maps.cs` | **FAITHFUL** |
| MOVE-81 | sub_6CE400：`[+0x675]>=2`→sub_6D06C0；`[+0x1BC]==0` 退出；`cmp eax,0x2710/jbe` 冷却（=10000 仍拒）；成功写 `[+0x72C]=tick`、`[+0x480]=Envir`；冷却文案 `IntToStr(10-秒)`+` 秒后方可使用`(str@0x6CE540 len13)。 | `UserMoveXYCommand.cs` + `Players/TPlayObject.NativeUserMove.cs`（`>10000U`、写 `m_dwTeleportTick`/`m_NativeUserMoveEnvir`） | **FAITHFUL** |
| MOVE-82 | 双串仅"您/你"之差：NOPOSITIONMOVE `在这里您无法使用`(str@0x6CE558 len16, send 0x6CE4FE)；NORANDOMMOVE 兄弟 `在这里你无法使用`(str@0x785864 len16, send 0x7856F0，属**随机传送魔法** sub_7855F8)。皆 cx=0x38FF vmt+0xD4。 | 您-变体: `UserMoveXYCommand.cs`(FAITHFUL)；你-变体缺失（C# `EatUseItems` case2 在 boNORANDOMMOVE 时静默 no-op，且魔法路径未含该串） | **MISSING**（你-变体） |
| MOVE-83 | sub_6EE174 @0x6EE197 `cmp [eax+0x85],0`(NORIDE)，拒绝 @0x6EE1A0 硬编码 cx=0xFCFF + str@0x6EE248(len22) + vmt+0xD4。第二读点 0x70F75B。 | `Players/TPlayObject.NativeRun3Horse.cs` | **FAITHFUL（已修）** |
| MOVE-84 | sub_6EE174 @0x6EE1B8 `sub_75EC20(bag,0x0F)`；==0 时 @0x6EE1C9 硬编码 cx=0xFCFF + str@0x6EE268(len29) + vmt+0xD4。 | `NativeRun3Horse.cs`（`SendNativeHorseSystemMessage` 0xFF/0xFC；mount==null||wIndex==0） | **FAITHFUL** |
| MOVE-85 | 超负重跑动通告在 sub_6B6BEC（进图状态位函数）@0x6B6C45 `cmp [Envir+0xB0],0` → str@0x6B6D10(len38) cx=0xFCFF。+0xB0=RUNFLAG。 | 旗标侧: `Maps/Maps.cs`(RUNFLAG→boRUNFLAG→`NativeCanRunWhileOverweight`)。通告消息未移植（sub_6B6BEC 含战神状态通告"巅峰战神状态"整函数未移植，全镜像 0 命中） | **MISSING**（进图通告） |
| MOVE-86 | DARK set-1 0x774E50 `c6435a01`(+0x5A)；NORECONNECT set-1 0x775072 `c6436401`(+0x64)。均非移动门（DARK 读点在状态/显示；NORECONNECT 无 test 点）。 | `Maps/Maps.cs`(boDarkness/boNORECONNECT)；C# 未在移动路径消费 | **FAITHFUL**（负向） |
| MOVE-87 | FIGHT set-1 0x774E7F `c6435d01`(+0x5D)；FIGHT3 0x774EAE `c6435e01`(+0x5E)。旁证：+0x5D 读点在 sub_6B6BEC（状态位 esi|1），非移动器。 | `Maps/Maps.cs`(boFightZone/boFight3Zone)；不 gate 移动 | **FAITHFUL**（负向） |
| MOVE-88 | MINE @0x6EC0FE `cmp [eax+0x6a],0` + 武器非空 + StdItem.Shape==0x13(19)。set-1 0x775257 `c6436a01`。 | `Players/TPlayObject.Attack.cs`(CM_HEAVYHIT+boMINE+武器+Shape19 挖矿)；`Plugins/YanshenTriggerDispatch.cs` 标注 0x6EC0FE | **FAITHFUL** |
| MOVE-89 | TRIGGERBOMB @0x789752 `cmp [eax+0x83],0`（sub_7896FC，单调用者 0x7896BD，移动时每 300ms 触发）。set-1 0x776894 `c6838300000001`。 | 旗标侧: `Maps/Maps.cs`(boTRIGGERBOMB)；消费者 sub_7896FC 未移植 | **MISSING**（消费者） |
| MOVE-90 | +0x81 token 字面串 **= NOMAGIC**（str@0x776E58 len7），set-1 0x77684E `c6838100000001`；test @0x6DA12B 在移动派发器 sub_6D7D68 gate 一个魔法 CM 分支。 | 旗标侧: `Maps/Maps.cs`(boNOMAGIC)；消费者未接线 | **MISSING**（消费者） |
| MOVE-91 | NOHUMNOMON 全镜像（GBK/ASCII/UTF-16LE）0 字节命中，确认缺席。 | `Maps/Maps.cs` §INVENTED 裁定：26 个凭空 token（含 NOHUMNOMON）解析臂已删、字段保留惰性默认 → 消费点不可达 | **FAITHFUL**（缺席对齐） |
| MOVE-92 | PAODIAN @0x77685F 匹配后 `call sub_77BEDC`。**独立复核 sub_77BEDC**：@0x77BEE2 `mov byte [ebx+0x91],1`（无视入参）+ @0x77BEF2 `[+0x94]==0` 惰性 new 管理器(call 0x77CD18, classref 0x774800) 存 +0x94。 | `Maps/Maps.cs`(boPAODIAN 注册；注释"不要凭 boPAODIAN 接线消费者") | **FAITHFUL**（fail-closed，符合"效果未证"；台账应升级） |
| MOVE-93 | GuildPK @0x776969（str@0x776EF0 len7）匹配后 `mov eax,[0x7D660C]/[eax]` 取全局管理器、`call sub_698484` 把 (图名→Envir) 注册进 manager[+0x54]，**不写 Envir 字段**；强制消费者未定位。 | `Maps/Maps.cs`(识别 token、无字段、fail-closed BLOCKED) | **FAITHFUL**（识别+fail-closed；台账应升级） |
| MOVE-94 | cell[2] 消费点 @0x7799D5 `cmp byte [eax+2],0` + creature[+0x178]；地图加载器（填 Envir[+0x38] cell 数组）**未定位**→生产者缺。 | 无（生产者侧原生即缺） | **BLOCKED**（沿用；C# 未臆造） |
| MOVE-95 | +0x2E6/+0x2E3/+0x2E2/+0x74/+0x73 creature-block 合取；+0x2E6 写点 4 处（0x684B33 存 0=`c683e602000000`；0x684BC2/0x764E66/0x764E97 存 1）。命名未证。 | — | **UNPROVEN**（沿用；成员为 TIER1，命名不臆造） |
| MOVE-96 | 占用探针 sub_777EF8 尾部（cell[3] 之后）未解；已证 boIgnoreOccupancy 短路 @0x777F70（`cmp byte [ebp+8],0/jne`，见 `Monsters/MonGenInfo.cs` nFailCount 注释）。 | 部分（占用扫描尾未移植） | **BLOCKED**（沿用） |
| MOVE-97 | opcode 3012 @0x6D9C7D 调 vmt+0x40(arg=1) 再 sub_6BBF9C；后者 @0x6BBFAF `mov cx,0x2719`(10009) 仅带 Dir 广播（无 X/Y）。名（SITDOWN）未证。 | — | **UNPROVEN**（沿用；功能标签非符号） |
| MOVE-98 | 移动间隔缺席（scan-scoped 负向）；扫描域 [0x600000,0x790000) 内唯一 tick delta @0x6BC94C 在**魔法路径**（`sub eax,[esi+0x6c]/cmp 0x1f4`=500ms）。GameGate 明确出域。 | — | **FAITHFUL**（负向，有界；C# 无每动作移动节流） |

### SPAWN / DROP（ProcessMon = sub_67C150；身份由异常串 "[Exception]: ProcessMon" @0x67C218/0x67C2DB/0x67C769 坐实）

| id | native 复核 | csharp_ref | 判定 |
|---|---|---|---|
| SPWN-01 | @0x67C198 `call 0x408340`(GetTickCount) → @0x67C19D `mov [ebp-0x14],eax`；全程复用（0x67C28E/0x67C2B4）。 | `UsrSystem/UsrEngn.cs` `ProcessMonsters`（注释 0x67C198 单 tick 复用） | **FAITHFUL** |
| SPWN-02 | Phase-A 尸体延迟队列挂 `[self+0x20]` 单链表；节点 12 字节（+0x00 对象/+0x04 入队tick/+0x08 next），持有 0x493E0=300000ms；`[self+0x28]` 计数；释放 `mov edx,0xc/call 0x402FD0`。 | `UsrEngn.cs`（只读核对） | **FAITHFUL** |
| SPWN-03 | Phase-B 每 tick `mov edi,0x50`(80) 次游标扫描；游标 `[self+0x48]`，越界归零并停(0x67C264/0x67C267)；`dec edi/jne 0x67C257`。 | `UsrEngn.cs` `for(nGenScan<NativeMonGenScanPerTick)`；`m_nCurrMonGen` | **FAITHFUL** |
| SPWN-04 | 四闸顺序：目标数 `[esi+0x24]>[esi+0x2c]`(0x67C27A) → 字节 `[esi+0xc]`(0x67C282) → `[esi+0x1c]≠0`(0x67C288) → 间隔 `tick-[esi+0x34]>[esi+0x30]`(0x67C28E)；汇合/游标++ @0x67C2BA；worker=false→不进游标即停。 | `UsrEngn.cs`（nCount>nActiveCount / sMonName / nRace / dwZenTime；worker false→break 不 ++） | **FAITHFUL**（另见下方 boVentureServer 备注） |
| SPWN-05 | 闸二 @0x67C282 `cmp byte [esi+0xC],0` 为 byte 宽度、`je 0x67C2BA`。 | `UsrEngn.cs`（解读为 sMonName 长度字节=名非空） | **FAITHFUL**（语义标签见台账建议） |

## 本次外科修复（2 处，均 build 通过 0 error）

1. **MOVE-83**（`c7506293`，`NativeRun3Horse.cs`）：NORIDE 召唤坐骑拒绝原用 `SysMsg(MsgColor.Blue,MsgType.Hint)`——会改用配置色 `btBlueMsgFColor/BColor` 且在 `boShowPreFixMsg` 时前置 `sHintMsgPreFix`；原生 @0x6EE1A0 为硬编码 cx=0xFCFF、无前缀、vmt+0xD4，且与同函数马牌拒绝(MOVE-84)字节同构。改走既有 `SendNativeHorseSystemMessage`(SendMsg 0xFF/0xFC)。

2. **MOVE-78**（`2fe4da73`，`NativeFixedCoordStone.cs`）：定点石 setter 的 `[esi+0x67]==0` 门原仅查 `boNORECALL`。全镜像证实 map+0x67 由 NORECALL 与复合 LimitItemMove **两 token**置位；补上 `|| boLIMITITEMMOVE`，否则 LimitItemMove 图上会漏发 0x6E9C18 拒绝消息并多留定点态（原生在 setter 即拒绝、不记录定点）。

> 其余 MISSING 行（MOVE-79/82/85/89/90）均落在**未移植的大函数或热点**（天地合一技能、随机传送魔法 sub_7855F8、进图状态函数 sub_6B6BEC、炸弹触发 sub_7896FC、移动派发器 sub_6D7D68），按"大缺口/热点只报不改、fail-closed 不臆造"处理。

## 台账修正建议（契约文字与二进制矛盾，以二进制为准）

- **MOVE-90**：`token whose name the spec does not give` 已定名——字面串 `NOMAGIC`（str@0x776E58 len7），store +0x81 @0x77684E。建议改 strength 说明并补 token 名。
- **MOVE-92**：`sub_77BEDC was never analysed / UNPROVEN` 过时——已证 @0x77BEE2 `mov byte [ebx+0x91],1`（set-only 无视入参）+ @0x77BEF4 惰性建管理器存 +0x94。效果层仍 BLOCKED（管理器下游消费者未证），但 +0x91 置位已 TIER1。
- **MOVE-93**：`store instruction and any test site were never located / BLOCKED` 过时——parser B @0x776969 识别 GuildPK 后注册 Envir 进全局 manager[+0x54]（sub_698484/sub_49F128，读 Envir[+0x44]），**不写 Envir 字段**；强制消费者仍未定位（效果层 BLOCKED）。
- **SPWN-05**：`[gen+0x0C] 的 byte 布尔开关` 标签欠准——C# 深析认定为 `sMonName` 长度字节（名非空判定）。二进制仅证 `cmp byte [esi+0xC],0`（两解读均容纳）；建议标注为"生成器名长度字节（=名非空）"。
- **SPWN-04 备注**：C# Phase-B 在四原生闸之外多插 `!boVentureServer` 条件（位于 nRace>0 与间隔之间）。未在 sub_67C150 的 0x67C27A–0x67C28E 四闸内找到对应指令；建议核实其是否为循环外的全局模式门或为 C# 附加（`UsrEngn.cs` 属禁改文件，仅报不改）。

## 方法与工具

- 反汇编脚本置于 `%TEMP%\eqv23_tool.py`（capstone 5.0.7），未污染工作树；提交信息经 `git commit -F` 临时文件。
- Delphi 字符串按 EA-8 refcount / EA-4 len / EA chars 解码并逐一核长度；旗标 set-1 以 `c6 43 <off> 01` / `c6 83 <off32> 01` 字节形态全镜像枚举写点。
