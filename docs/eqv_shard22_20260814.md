# 等价核验报告 — 分片 22 (MOVE-52..MOVE-77)

- 子系统：MOVEMENT / WALKABILITY / TELEPORT
- 底本：`staging/_reunpack_work/flat_image.bin`（base `0x400000`，17,661,952 B）
- 工具：capstone 5.0.7 / Python 3.11；反汇编脚本 `staging/_reunpack_work/_dis22.py`
- 工作树：`D:\loym2\.claude\wt2\eqv-22`（分支 `w/eqv-22`，起点 `00841828`）
- 方法：逐条对**二进制 VA 证据**与**当前 master C# 代码**双向核对；不 dotnet build。

## 状态统计

| 判定 | 数量 | 行 |
|---|---|---|
| FAITHFUL | 19 | 52,53,55,56,58,59,60,61,62,63,64,65,66,67,68,69,70,76,77 |
| FAIL-CLOSED / 有据分歧（热点，只报不改） | 2 | 54, 57 |
| 归属他代理（NOTHROUGH，只报不改） | 5 | 71,72,73,74,75 |

- 覆盖率：26/26 = 100% 逐条判定。
- 外科式修复：**0**（本分片所有真实分歧均落在**热点**——传送原语 `TrySpaceMoveToEnvironment` 与登录放置流 `UsrEngn` ——或属**他子系统**；依铁律"大缺口/热点只报不改"，不动刀）。
- MOVE-65 / MOVE-67 记为 FAITHFUL：核心契约已 1:1，残余为**他子系统/fail-closed**（见下）。

---

## 逐条判定与证据

### 传送原语族 MOVE-52..60

**MOVE-52 · FAITHFUL（COSMETIC）** — TPlayer 传送 `sub_6BD294`（VMT+0x1C0 覆写）。
- 二进制：flush `call [ecx+0x1D8]`(edx=0) @0x6BD2AD；InBodyState 拆解 0x33→`sub_6EE458`@0x6BD2E5 / 0x34→`sub_6EEB98`@0x6BD2F9；`sub_6CCA1C` gate @0x6BD2FE；config 0x80 & Envir[+0xB4]→`sub_77B97C`@0x6BD331；成功臂 `call [eax+8]`@0x6BD39B、`0x2785→sub_765E68`@0x6BD3B2、`0x2786→sub_765F6C`@0x6BD3DB；广播臂 @0x6BD3F5 `cmp [ebp+0xc],1 → si=0x2860 / else 0x285B`，`call [esi+0xD8]`@0x6BD427。
- C#：`TBaseObject.TrySpaceMoveToEnvironment`（`Actors/TBaseObject.cs:2132`）内部消息 `RM_NATIVE_CLEAROBJECTS=10117(0x2785)` / `RM_NATIVE_CHANGEMAP=10118(0x2786)`，广播 `showMode==1?RM_SPACEMOVE_SHOW2(10336=0x2860):RM_SPACEMOVE_SHOW(10331=0x285B)`（常量见 `Grobal2.cs:1535/1536/1671/1672`，AuditTool `PacketRoundTripCheck` 亦断言）。马/英雄拆解经 `CleanupNativeHorseBeforeSpaceMove`（`Players/TPlayObject.NativeHorseDismount.cs:34`）：mounted(0x33)→`ClientNativeHorseDismount`=`sub_6EE458`，blocked(0x34)→`NativeHorseRiderDownCore`=`sub_6EEB98`；env 变更钩子 `OnEnvirnomentChanged`。
- 备注：`sub_6CCA1C`、`sub_77B97C` 两个前置消费未接线；后者读 Envir[+0xB4]=FLYDROPITEM，`TMapFlag.FlyDropItemNames` 明确标 **BLOCKED（未接线，fail-closed）**——一致。

**MOVE-53 · FAITHFUL（INTERNAL）** — 同图臂失败净效果=不动、无消息。
- 二进制：`sub_6BD294` 同图快臂（0x6BD336 起）`GetRandomXY` 失败 @0x6BD34D、`sub_7797CC`(MoveToMovingObject) 失败 @0x6BD37B 均 `je 0x6BD446`（稳健臂），该臂搜失败再做整卷回滚。
- C#：`TrySpaceMoveToEnvironment` 统一走"先 `DeleteFromMap`→`GetRandXY`→`AddToMap`，失败于 finally 回滚重挂原格"（`TBaseObject.cs:2166,2252-2298`）。净效果一致：失败不移动、不发玩家消息（仅服务器日志）。

**MOVE-54 · FAIL-CLOSED / 有据分歧（INTERNAL，热点·只报不改）** — 创物 cross-map `sub_768D78` 同图臂占位策略。
- 二进制：门 `sub_78FE84(byte[esi+0x54])`@0x768D90；同图测 @0x768D9D；`GetRandomXY(1,1)`@0x768DB5；**`push 0`**@0x768DC9 → `sub_7797CC`(MoveToMovingObject, boIgnoreOccupancy=**0**)@0x768DDF —— **创物同图传送复检占位**；对比玩家 `sub_6BD294` @0x6BD35E `push 1`（忽略占位）。
- C#：`TrySpaceMoveToEnvironment` 对**所有对象**统一 `GetRandXY`(CanWalk flag=true 忽略占位) + `AddToMap`(无占位复检)，等同**玩家策略**；创物的 `boIgnoreOccupancy=0` 终态复检**未复刻**。
- 定性：传送原语热点（`GetRandomXY` 11 caller / `MoveToMovingObject` 17 caller，MOVE-63），改动波及全体怪物/召唤物传送 → **只报不改**，建议传送原语归属代理按对象类型补终态占位复检。

**MOVE-55 · FAITHFUL（INTERNAL）** — cross-map 存档/回滚。
- 二进制：存旧 Envir/X/Y @0x768E11-E26；`sub_7794A8` 解链；地图名拷入 `[+0x115]` max 0x0F；`GetRandomXY` 原地；VMT+0x28 插入；失败整卷回滚重插原格。
- C#：`TrySpaceMoveToEnvironment` 存 `oldEnvironment/oldX/oldY/old*Visible*`（`TBaseObject.cs:2141-2159`），失败 finally 重挂并复原可见列表（2252-2298）。语义一致。

**MOVE-56 · FAITHFUL（INTERNAL）** — 登录/重入放置 `sub_6B9A2C` jitter。
- 二进制：按名解析 `[+0x115]`@0x6B9C09、无解硬败 @0x6B9C12；`edi=5`@0x6B9C18；探测 `sub_777EF8`(boIgnoreOccupancy=1)@0x6B9C2D；X += `Random(6)-3`@0x6B9C36-4B、Y 同 @0x6B9C51-66；`dec edi`@0x6B9C6C。范围 **-3..+2**。
- C#：`UsrEngn.cs:684-691` `while` 循环：`CanWalk(X,Y,true)` break；`X = X-3+Random(6)`；`Y = Y-3+Random(6)`；`nC>=5` break。retry 5 与 -3..+2 **逐位一致**。

**MOVE-57 · 有据分歧（INTERNAL，热点·只报不改）** — 登录 jitter 耗尽后的 `GetRandomXY` 回退缺失。
- 二进制：jitter 尽 → `GetRandomXY([+0x12C/130], 1,1)`@0x6B9C81，成功即定（@0x6B9D05）；失败才记 `[Error]: Can not Get Vaild Cell`（串 @0x6B9F64，console cl=1）、覆写 `[+0x115]`=默认图 globals[0x7D5F24]、X/Y=[[0x7D693C]]/[[0x7D6744]]；仍 nil 记 `[Error]: Player.PMap = nil, Goto back Proc`、`[+0x4BB]=1`；否则清 `[+0xD2C]`。
- C#：`UsrEngn.cs:692-700` jitter 失败**直接**回退 `sHomeMap`+`nHomeX/nHomeY`（最终目标与原生默认图一致），**跳过了中间的 `GetRandomXY(当前图)` 尝试**，且日志用 `sChangeServerFail2` 而非 `'Vaild'` 串。
- 影响：出生点周围 6 格全阻塞时，原生会在**当前图**全图搜有效格（31 步 + LinkPoint 兜底）再放弃，C# 直接踢回城图 → 罕见但可致玩家落错图。
- 定性：登录放置流为**热点**（200+ 行，含 castle/fight3/PK/switch 交织，高爆破半径）→ **只报不改**，建议登录归属代理在 `UsrEngn.cs:692` 前插 `TBaseObject.NativeGetRandomXY(Envir, ...)` 尝试（`NativeGetRandomXY` 已 `internal static` 可用）。

**MOVE-58 · FAITHFUL（INTERNAL）** — 命名图传送 `sub_6CE1B8` 未知图静默空操作。
- 二进制：空名→默认当前图（`[esi+0x128]`@0x6CE1C6，`test edi,edi/je`@0x6CE1CC）；`sub_696228` 对 `[0x7D660C]` 解析 @0x6CE1D9；未解 `je 0x6CE20A` **静默返回** @0x6CE1E0；异图清 `[+0xBA8]`@0x6CE1EC；`call [+0x1C0]`(Envir,X,1,0,Y)@0x6CE204；`ret 4`。
- C#：`TBaseObject.MapRandomMove`（`TBaseObject.cs:2400`）`FindMap==null` **无 else 分支→静默空操作**；`BaseObjectMove`（`TPlayObject.cs:3035`）空名→`m_sMapName`。契约一致。
- 观察（非本行 gap）：`BaseObjectMove` @`TPlayObject.cs:3051-3055` `var envir=m_PEnvir; if(envir!=m_PEnvir ...)` 恒假（先取后比同一字段），`m_boTimeRecall` 清零永不触发；原生 `[+0xBA8]` 清零在低层 `sub_6CE1B8`。字段映射未定，留观察，建议低层传送处清 recall 标志。

**MOVE-59 · FAITHFUL（INTERNAL）** — VMT+0x1C0 多态：TPlayer=`sub_6BD294`，其余=`sub_768D78`。
- C#：`TrySpaceMoveToEnvironment` 以 `this is TPlayObject` 分派（马匹清理、地图掉落物释放等）。占位策略差异见 MOVE-54（唯一未复刻点）。

**MOVE-60 · FAITHFUL（INTERNAL）** — TPlayer cross-map 臂拆解+回滚。
- 二进制：存旧 Envir/X/Y、`call [+8]`、`sub_7794A8` 解链、`sub_76B3B4/sub_6D7610/sub_6D75F8`、赋新 Envir、名拷 max 0x0F、`GetRandomXY(1,1)`、失败回滚 0x6BD5D1。
- C#：cross-map 分支复用 `TrySpaceMoveToEnvironment` 的存档/解链/`OnEnvirnomentChanged`（env 变更钩子 ≈ `sub_76B3B4/6D7610/6D75F8`）/回滚。语义一致（细节子调用归传送原语代理）。

### 零坐标哨兵 + 搜索原语 MOVE-61..64

**MOVE-61 · FAITHFUL（INTERNAL，二进制逐位验）** — 零/负哨兵，双轴独立。
- 二进制 `sub_7782D0`：`cmp [edi],0 / jg`@0x7782E4（有符号，负数亦哨兵）→ `Random([+0x3C]=Width)+1`；`cmp [eax],0 / jg`@0x7782F7 → `Random([+0x40]=Height)+1`。
- C#：`TBaseObject.NativeGetRandomXY`（`TBaseObject.cs:2062`）`if(nX<=0) nX=Random(wWidth)+1; if(nY<=0) nY=Random(wHeight)+1;`。`<=0` 覆盖全负数，X 先于 Y。逐位一致。

**MOVE-62 · FAITHFUL（INTERNAL，二进制逐位验）** — `Random(N)+1`，含 N（越界种子）。
- 二进制：`inc eax`@0x7782F1（1..Width，X==Width 可能）；循环 `esi=0x1F`(31) @0x778346；步 3 / Width<0x32 时 2；margin Height≥0xFA→0x32 / ≥0x1E→0x14 / else 2；前进/重种子 `Random(N/2)+margin`@0x778373/0x77839F；LinkPoint 兜底 @0x7783C4 门于 caller flag。
- C#：`+1` 保留；`nStep=Width<50?2:3`；`nMargin=Height<30?2:Height<250?20:50`；`for(nRetry<31)`；越界 `nX<Width-nMargin-1` 前进否则 `Random(Width/2)+margin`；`m_PointList` 兜底。逐位一致。

**MOVE-63 · FAITHFUL（INTERNAL）** — 全 11 caller 共一 body。
- C#：`NativeGetRandomXY` 为唯一权威，`SpaceMove_GetRandXY`（`TBaseObject.cs:2115`）、`TryResolveNativeUserMoveCoordinates` 皆为其适配器（代码注释已锚 MOVE-63）。

**MOVE-64 · FAITHFUL（INTERNAL）** — NORANDOMMOVE 守卫消费者 `sub_7855F8`。
- 二进制：`cmp byte [eax+0x68],0 / jne` 拒绝 @0x785710-14；`dx=0x285A`@0x785722、`call [+0xD8]`@0x78572A；`TeleportToMap(Self, 空名, 0, 0)`@0x785738(`sub_6CE1B8`)。
- C#：`TPlayObject.EatUseItems` case 2（`TPlayObject.cs:2962`）`if(!Flag.boNORANDOMMOVE){ SendRefMsg(RM_SPACEMOVE_FIRE=10330=0x285A); BaseObjectMove(m_sMapName,0,0); }`（→ `MapRandomMove` 当前图随机点）。一致。

### 地图旗标 MOVE-65..70

**MOVE-65 · FAITHFUL（INTERNAL）** — NORANDOMMOVE 字节 Envir[+0x68]，两测点。
- 二进制：parser 写 `c6 43 68 01`@0x7751EF；测点 0x785710（EatUseItems case2）与 0x786E54。
- C#：解析 `Maps.cs:188 boNORANDOMMOVE=true`；测点1（EatUseItems case2）FAITHFUL（见 MOVE-64）。
- 测点2（0x786E54）：位于 VMT 方法 `sub_786DEC`（无直接 caller），限地图名 `"0150"`(=皇宫 `UserCastle.cs:127`)、`SceneType!=2`、Dura≥1000 的**皇宫专用物品效果**，属**城堡/物品子系统**，非移动原语；本分片不接线，建议对应子系统核验其 NORANDOMMOVE 守卫。

**MOVE-66 · FAITHFUL（INTERNAL）** — 两解析器：B 权威(地图加载)、A(GM 运行时)。
- C#：地图加载解析器 = `Maps.cs`（对应 pool B `sub_776008`，权威）；GM 设置器 = `Command/Commands/TempSetMapParamCommand.cs`（对应 pool A `sub_774D98`+wrapper）。两者分离、各自 token 集，符合"A 不可假设覆盖 B"。

**MOVE-67 · FAITHFUL（COSMETIC）** — parser A 单 caller 为 GM 命令体。
- 二进制：解图名 `sub_6962D0`@0x62994E、未知图 `je`@0x629957、`StrToIntDef`@0x629962、调 wrapper@0x629970、Blue 回 `取消地图属性=` / `，操作成功`。
- C#：`TempSetMapParamCommand`：`FindMap`→未知图红字提示；`Str_ToInt`(=StrToIntDef) 转 state；`state==1?"增加地图属性=":"取消地图属性="` + `"，操作成功"` Blue。命令形态与回串 1:1。
- 说明：C# GM 设置器仅接线 `pickup` 属性，其余返回"不支持此地图属性"（**fail-closed**）。原生 A 覆盖 ~52 token（多与 B 重复），全量移植属**大缺口**且 A 非权威 → 记 fail-closed，不动刀。

**MOVE-68 · FAITHFUL（INTERNAL）** — B 独有 PAODIAN/NORIDE/GuildPK。
- C#：`Maps.cs` 解析 `PAODIAN`(:702)、`NORIDE`(:279)、`GuildPK`(:716) —— 均在地图加载解析器(B)；GM 设置器(A)不覆盖，符合"GM setter cannot reach"。`boPAODIAN` 消费按 `TMapFlag` 文档 **BLOCKED**（+0x94 管理器类未逆），一致。

**MOVE-69 · FAITHFUL（INTERNAL）** — 两解析器同形：匹配后存单字节 1/0。
- C#：均以 `bool` 字段表达（语义等价字节 1/0）。

**MOVE-70 · FAITHFUL（INFERRED→验证）** — SAFE 字节 Envir[+0x5C]，读于击退/推挤/图态广播，**不在行走/可走性**。
- 二进制：读点 0x7684F6(`sub_7684DC`)、0x768598、0x6B6C05 等；`sub_741224/sub_6BBCD8/sub_6BBFBC/sub_7797CC` 无。
- C#：`Envirnoment.CanWalk`（`Envirnoment.cs:536-576`）**不引用 boSAFE**（已通读确认）；boSAFE 读于 `InSafeZone`(`TBaseObject.cs:5641`)、安全区判定等，非 movers。一致。

### NOTHROUGH MOVE-71..75（归他代理 `w/move-nothrough`，只报不改）

现状快照（**未改动**）：
- 解析：`Maps.cs:538 boNOTHROUGH=true`；字段 `TMapFlag.boNOTHROUGH`（`SystemModule/Data/TMapFlag.cs:213`）。
- 缓存/穿越判定在建：`TBaseObject.cs:651/666 HasNativeCellPassThroughGrant()`（对应 `sub_772EB8` 无条件放行，MOVE-75）；`TPlayObject.Message.cs:427` 锚 `call 0x768454`（=`sub_768454`，MOVE-72/73）；`SM_2821`(0xB05) @`:994`（MOVE-74 缓存翻转发包 Series6/Param1|0）。
- 结论：与 MOVE-71..75 契约（Envir[+0x84] 唯一读点、Obj[+0x3FE] 缓存、玩家 tick 刷新、0xB05 发包）方向一致，属在建工作；本分片**不评级不改动**，交 NOTHROUGH 代理收口。

### 负重跑/回城 MOVE-76..77

**MOVE-76 · FAITHFUL（COMBAT，二进制定夺极性）** — RUNFLAG 字节 Envir[+0xB0]，**反极性=豁免**。
- 二进制：**构造默认** `mov byte [esi+0xB0],1`@0x774A5C（=EXEMPT）；解析 `StrToIntDef`@0x77558A→非零存1@0x77559F/零存0@0x775593/裸存0@0x7755B3；运行门 `cmp byte [+0xB0],0 / jne 跳过负重比较`@0x6BBFDC、@0x6BC0F4。
- C#：`TMapFlag.boRUNFLAG=true`（字段初始化=默认豁免，`TMapFlag.cs:195`）；`Envirnoment.NativeCanRunWhileOverweight=true`（`Envirnoment.cs:26`）；解析 `Maps.cs:301 boRUNFLAG=Str_ToInt(s38,0)!=0`（裸/0→false、非零→true）；消费 `IsNativeRunLadderAllowed`（`TPlayObject.NativeRun3Horse.cs:165`）`(!switch0x80 || NativeCanRunWhileOverweight || Weight<MaxWeight) && !state67 && !state13`。默认、解析、消费三处**均对齐二进制**。
- 注：分片契约文字"absent token means restriction is ON"与二进制**相反**（构造默认 [+0xB0]=1=豁免，缺 token→豁免→限制 OFF）；本报告以二进制为准，判 C# FAITHFUL。

**MOVE-77 · FAITHFUL（INTERNAL）** — NORECALL 字节 Envir[+0x67]，三测点均非行走路径。
- 二进制：parser `c6 43 67 01`@0x7751BB；测点 0x6C7B47(`sub_6C7B28` 回城倒计时)、0x6E9C00(`sub_6E9BAC` 定位石)、0x72751B(`sub_7274B4`)。
- C#：解析 `Maps.cs:178 boNORECALL=true`；消费 `NativeFixedCoordStone.cs:153`(=定位石 `sub_6E9BAC`)、`MirrorMessage.cs:618`(夫妻回城 `MsgGetRequestLoverRecall`)——均在**回城/召回路径**，`CanWalk` 不引用。核心契约一致。
- 注：`sub_6C7B28` 的 `(GetTickCount-Self[+0x728])/1000 vs byte Self[+0xBA3]` 倒计时属**回城子系统**细节，本分片仅确认 NORECALL 标志正确接线于回城消费点、离行走路径。

---

## 结论

分片 22 的移动/可走性/传送子系统在 master 中还原度极高：**19 FAITHFUL**。仅两处有据分歧（MOVE-54 创物占位复检、MOVE-57 登录 GetRandomXY 回退），二者均落在**热点**（统一传送原语 / 登录放置流），依铁律**只报不改**并已给出接线建议。NOTHROUGH（MOVE-71..75）归 `w/move-nothrough` 代理，仅记现状。MOVE-65 测点2 / MOVE-67 全量 token 属**他子系统/fail-closed**，非本分片外科缺口。

**本分片无外科式修复提交（0 fix）——所有真实缺口皆为热点或跨子系统，强行改动违反"大缺口/热点只报不改"与"有据不臆造"。**
