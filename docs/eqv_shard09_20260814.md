# 等价核验报告 · 分片 09（DURA 尾 + POIS 头）

- 日期：2026-08-14
- 契约来源：`staging/_shards/shard_09.tsv`（26 条：DURA-36..45 共 10 条，POIS-01..16 共 16 条）
- 底本：`D:/loym2/staging/_reunpack_work/flat_image.bin`，ImageBase `0x400000`，capstone 5.0.7 x86-32（大小 17,661,952 = `0x10D9000`）
- C#（只读核对）：worktree `w/eqv-09` @ `00841828`（`Merge branch 'w/trade-storage'`）
- **未执行任何编译命令**（无 dotnet build / run / msbuild）。纯反汇编 + 只读比对。
- 反汇编方式：capstone `Cs(CS_ARCH_X86, CS_MODE_32)` 直读 flat_image.bin 的 `[VA-0x400000 : end-0x400000]` 片段（与 `staging/_*_disasm.py` 系列同法）。

---

## 0. 状态统计

| 判定 | DURA | POIS | 合计 |
|---|---:|---:|---:|
| FAITHFUL | 2 | 14 | **16** |
| DIVERGENT（已桥接·毒代理域） | 0 | 1 | **1** |
| FAIL-CLOSED（有据不臆造） | 0 | 1 | **1** |
| BLOCKED | 8 | 0 | **8** |
| MISSING | 0 | 0 | **0** |
| 合计 | 10 | 16 | **26** |

**本分片新增外科修复：0 处。** 所有条目在当前 master 已属 FAITHFUL、合理 FAIL-CLOSED、或大缺口 BLOCKED（只报不改）。
无一条需要「臆造公式」或「补空调用点」——按铁律不动。

### 相对台账（`equivalence_ledger` / `reconcile_CRAFT_DURA_20260813.md`）的关键增量

当前 master 已由**毒代理**把台账里 4 条非 FAITHFUL 条目全部落地，本次复核予以确认并升级：

| 契约 | 台账/契约旧状态 | 当前 master 复核 | 依据 |
|---|---|---|---|
| POIS-05 | DIVERGENT（C# 用严格 `>`，漏边界 tick） | **FAITHFUL** | `TBaseObject.Base.cs:686` 已改 `>=`，对上 `0x76BD3E jb`（反极性 = elapsed≥2500） |
| POIS-08 | MISSING（C# 只实现四档之一） | **FAITHFUL** | `TBaseObject.NativePoisonTick.cs:72-89` 实现 if/else-if 链 0x06>0x01>0x1C>0x1F，一 tick 一档 |
| POIS-09 | MISSING（MaxHP 派生两档缺失，byte 装不下） | **FAITHFUL** | 同文件 `:60-99`，`node.Value` 为 `int`（`TimedAbility.cs:20`），上限钳更正为 `0x4C4B40`=5,000,000 |
| POIS-12 | MISSING（档位存在但 level 恒 0，×1.25 打不到） | **FAITHFUL** | `MakePosion` 补记 level（`TBaseObject.cs:6199 RecordNativeRedPoisonLevel`），`NativeMagicMidStates.cs:252` 读取 |

---

## 1. DURA-36..45 逐条

耐久尾部本分片 10 条中，8 条为大分析缺口（BLOCKED，只报不改），2 条 FAITHFUL（其中 DURA-45 可关闭台账 UNPROVEN）。

| id | 判定 | 一句话 + 字节证据 |
|---|---|---|
| DURA-36 | **FAITHFUL**（分析产物，无 C# 影响） | `0x73E394` 确为 `ret`（`0x73E38F call 0x405524` / `0x73E394 ret` / `0x73E395 jmp 0x404e38` SEH），被当成「独立 recalc 函数」的内部地址实为这一个 recalc 例程的内段，两份 dump 共尾同一 ret。无 C# 需镜像。 |
| DURA-37 | **BLOCKED** | 字节事实已复核：`0x73EA5D mov [ebp-0xC],eax`（GetItem(9)）→ 循环内 `0x73EAB9 mov [ebp-0xC],eax` **无条件覆写** → `0x73EB0A sub word[eax+0x26],dx` 用它。首检命中会 `0x73EA99 jne 0x73EAF7` 跳过循环（此时指针=GetItem(9)）；契约自述「可达性 UNPROVEN，因入口缺失」。C# 无对应函数。 |
| DURA-38 | **BLOCKED** | `0x73EAFA and al, byte[ebp-5]`；`0x73EA44`-`0x73EB1B` 捕获区内**无** `[ebp-5]` 写者，其值继承自未捕获的 prologue。C# 无对应。 |
| DURA-39 | **BLOCKED**（大缺口·只报不改） | 全镜像仅证到 slot 1（武器）与 slot 9 有 `+0x26` 掉耐久点；slot 0/2/3/7/8/10/12/15 的 `GetItem` 立即数存在，但其调用点在捕获集内不写 `+0x26`。契约明令「不得臆造公式」——不动。 |
| DURA-40 | **BLOCKED**（大缺口·只报不改） | 装/卸的 CM 跳表入口与 opcode 常量未捕获。调用方已推进（台账：`0x6B7F8F call 0x75F044` 装备、`0x6B81F0 call [ecx+0x244]` 卸下预门），但缺 dispatcher→case 体的整链。 |
| DURA-41 | **BLOCKED**（超重后果未捕获；台账 EA 抄错已勘正） | 台账写的裸谓词 `0x73C92C` 是 15B ShortString 拷贝（`0x73C94E ret`）；真正谓词是 **`0x73C950`**（`mov edx,[eax+0x2C4]` / `cmp edx,[eax+0x2C8]` / `setl` = Weight<MaxWeight）。C# `TPlayObject.cs:348 IsAddWeightAvailable` 的注释与实现均已按 `0x73C950` 写对。缺的是原生「超重后果」的整链捕获，非 C# 缺口。 |
| DURA-42 | **BLOCKED** | 两个 slot-9 掉耐久体入口 `0x73E951` / `0x73EA38` 都从 SEH prologue 中段起（`push 0x73E9F9` / `push fs:[eax]`），零直接调用方；`esi=player`、`[ebp-4]=amount` 由使用推断。算术与销毁判定本身已证。 |
| DURA-43 | **BLOCKED（证据推进：edi 来源已定位）** | 契约称 `0x788511 dec word[edi+0x26]` 的 edi「在捕获范围内无加载」。**回溯 prologue 发现**：函数真正起点是 `0x788418`，`0x788430 mov edi,eax` 把首参（eax=物品指针）装入 edi；`0x78842E mov ebx,edx`（次参=player）。故减耐久目标 = 传入的物品。字节级 blocker 已解除；但该函数身份/触发/调用方与 C# 端口仍未定，归入 DURA-44 群。 |
| DURA-44 | **BLOCKED**（大缺口·只报不改） | 约 20 个 `+0x26` 写者（`0x68686E`/`0x68C9DE`/`0x68F4CA`/`0x6A0B11`/`0x6A0BB4`/`0x6A0C64`/`0x6CF2B2`/`0x6D5F4E`(add=修理)/`0x6DF96D`/`0x6E9D0B`/`0x740C8D`/`0x788DAD`/`0x78B1FE`）的触发/槽/算术均未定性。 |
| DURA-45 | **FAITHFUL（台账 UNPROVEN 可关闭）** | 打包器 `0x743D99..0x743DD6` 依序拷贝 `+0x2C4/+0x2C8/+0x2CC/+0x2D0/+0x2D4/+0x2D8`（+`+0x2DC` 单列）；谓词 `0x73C950 cmp [+0x2C4],[+0x2C8]/setl` 决定性证明 `+0x2C8` 是 cap、`+0x2C4` 是 load。C# `TAbility.cs:50-75` 六字段 Weight/MaxWeight、WearWeight/MaxWearWeight、HandWeight/MaxHandWeight 顺序与 cap/load 归属全对。 |

---

## 2. POIS-01..16 逐条

### 2.1 POIS-01..07（本代理主责）—— 全部 FAITHFUL

| id | 判定 | 原生字节 | C# |
|---|---|---|---|
| POIS-01 | **FAITHFUL** | Run=`0x76B6F0`（TCreature VMT+0x10）；平砍掉血 `0x76B801`、毒 tick `0x76BD33` 同在此一函数 | 两个掉血点同在单一 `Run()`（`TBaseObject.Base.cs:12`）：平砍 `:215`、毒 tick `:686` |
| POIS-02 | **FAITHFUL** | `0x76B7EF cmp [esi+0x10],0xFFFFFED4`(−300) / `0x76B7F6 jge skip`；无 HasState 门 | `Base.cs:215 m_nHealthTick < -nHealthFillTime`（严格 `<`），无状态门 |
| POIS-03 | **FAITHFUL** | `0x76B7F8 cmp [esi+0x2AC],1` / `0x76B7FF jle skip`（HP≥2 才 dec） | `Base.cs:215 m_WAbil.HP > 1` |
| POIS-04 | **FAITHFUL** | `0x76B807 add [esi+0x10],0x12C`（+300 累加，非硬重置）；门 `jge` 严格低于 −300 | `Base.cs:218 m_nHealthTick += nHealthFillTime`；`nHealthFillTime=300`（`GameSvrConfig.cs:1033`） |
| POIS-05 | **FAITHFUL**（台账 DIVERGENT 已修） | `0x76BD39 cmp eax,0x9C4`(2500) / `0x76BD3E jb skip` = 反极性，elapsed≥2500 触发 | `Base.cs:686` 已用 `>=`；`dwPosionDecHealthTime=2500`（`GameSvrConfig.cs:1466`） |
| POIS-06 | **FAITHFUL** | `0x76BD44 mov eax,[ebp-4]` / `0x76BD47 mov [esi+0x28],eax` = MOV 硬重置（丢余数） | `Base.cs:688 m_dwPoisoningTick = GetTickCount()` |
| POIS-07 | **FAITHFUL** | `0x76BDFE mov edi,[eax+0x0A]` / `0x76BE01 inc edi` / `test/jle` 非正跳过 | `NativePoisonTick.cs:97 damage = node.Value + 1` |

### 2.2 POIS-08..16（毒系统·毒代理已核验，本次复核确认，未改动）

| id | 判定 | 复核要点 |
|---|---|---|
| POIS-08 | **FAITHFUL**（曾 MISSING） | 四档 if/else-if：`0x76BD53`(0x06)>`0x76BD8C`(0x01)>`0x76BDC5`(0x1C)>`0x76BDE0`(0x1F)，三跳全汇合 `0x76BDF5`，`0x76BE0C call [ecx+0x1B0]` 每 tick 仅一次伤害。C# `NativePoisonTick.cs:72-89` 同构。 |
| POIS-09 | **FAITHFUL**（曾 MISSING） | 0x06/0x01 两档从 MaxHP 派生并覆写 `rec+0x0A`：`0x76BD68 mov edx,0x4C4B40`(=5,000,000) / `call 0x4C700C`(MIN，`cmp edx,eax;jg;mov eax,edx`) / `idiv 0x64`(0x06) 或 `idiv 0x1E`(0x01)。C# `node.Value=Math.Min(MaxHP,0x4C4B40)/divisor`，`node.Value` 为 `int`。**账本写 1,000,000 是错的，字节是 0x4C4B40=5,000,000。** |
| POIS-10 | **FAITHFUL** | `idiv 0x64` / `idiv 0x1E` 有符号截断，附近无 `call 0x403574`(@ROUND)。C# 用整除，无取整助手。 |
| POIS-11 | **DIVERGENT（已桥接·毒代理域）** | 原生 `0x1F`=DoT 毒（在掉血块 `0x76BDE0`）、`0x1E`=防御毒（在入伤缩放 `0x767A94`），确为两种不同效果。C# 索引空间是另一支 fork（`Grobal2.cs:103 POISON_DECHEALTH=0…`），毒代理用 `MakePosion` 的 `bodyState = 31 - nType` 在边界桥接（`TBaseObject.cs:6174`），行为侧已对齐；结构 fork 仍存，属已知追踪项。 |
| POIS-12 | **FAITHFUL**（曾 MISSING/inert） | `0x767AAA cmp eax,4`；`0x767AB5 fmul [0x767B3C]`=1.25（`0000A03F`）、`0x767ACA fld [0x767B40]`=1.2，均 `call 0x403574`。三常量字节复核：1.25/1.2（外加 `0x767B30`=1.3 对应 state53 分支）。C# `NativeMagicMidStates.cs:246-261`：state53→1.3、state30→level==4?1.25:1.2；**关键修复**：`MakePosion` 现对红毒 `RecordNativeRedPoisonLevel(nPoint)`（`TBaseObject.cs:6199`），level 落地后 ×1.25 档可达。 |
| POIS-13 | **FAITHFUL** | level 仅在 `0x767AA5 call 0x773BEC` 取一次、只与 4 比（`0x767AAA`），恰两档无逐级曲线。C# `?1.25:1.2`。 |
| POIS-14 | **FAITHFUL** | `0x767A9F je 0x767AD9`：未中防御毒则整段缩放跳过。C# `else if(HasNativeActiveState(30))…else return`。 |
| POIS-15 | **FAITHFUL**（结构判别） | `0x674F83/0x674F96` HasState(0x1E)/(0x1F) → `[ebp-9]` 存 0x1E/0x1F/0 → `0x67502F mov dl,[ebp-9]` / `0x675037 call [edi+0xC8]`（AddState），`cx=0x12C`、level 1。证明 0x1E/0x1F 是一个毒族变量经 AddState 复投。C# `MakePosion`→`AddTimedAbilityInternal` 用同一权威统一两毒（`bodyState=31-nType`）。（证据站点 `sub_674F58` 本身是冰霜之王低血特技，其触发随 POIS-16 fail-closed。） |
| POIS-16 | **FAIL-CLOSED（有据不臆造）** | 家族清除 `0x76B4DC` 循环 `bl:0x18→0x1F`（`cmp bl,0x20/jne`）逐个 `call 0x76B4D0→0x7731C0`(RemoveState)。唯一调用者 `0x674FB8`（在 `sub_674F58`，`[self+0x564]==4` 门），而 `sub_674F58` 是 TKingOfIceMon/BB（race 146/154，VMT slot-34 低血特技）独占分支。该类链（TSearchMon→TKingOfIceMon）在 C# 未移植、race 146/154 既定 fail-closed，故补空调用即死代码。毒代理已在 `TBaseObject.NativePoisonFamilyClear.cs` 全字节留档 + 移植后接线方案。判定：合理 fail-closed。 |

---

## 3. 浮点/常量字节校核（本分片涉及项）

| 常量 VA | 字节 | 解码 | 用途 | 判定 |
|---|---|---|---|---|
| `0x4C4B40`（立即数） | — | 5,000,000 | POIS-09 MaxHP 上限钳 | C# 已更正为 5,000,000（账本 1,000,000 错） |
| `0x767B30` | ext80 | 1.3 | POIS-12 state53 分支 | 对上 C# 1.3d |
| `0x767B3C` | `00 00 A0 3F` | float32 1.25 | POIS-12 level==4 | 对上 C# 1.25d |
| `0x767B40` | ext80 | 1.2 | POIS-12 else | 对上 C# 1.2d |
| `0x12C`（立即数） | — | 300 | POIS-04 血回填时间 / POIS-15 AddState 时长 | `nHealthFillTime=300` |
| `0x9C4`（立即数） | — | 2500 | POIS-05 毒 tick 间隔 | `dwPosionDecHealthTime=2500` |

取整助手：POIS-10 明确**不用** `0x403574`（@ROUND），POIS-12 两档**都用** `0x403574`（半偶入）——C# 分别用整除 vs `RoundNativeX87`，一一对应。

---

## 4. 覆盖率

- **契约条目**：26 / 26 全部核对（DURA-36..45、POIS-01..16）。
- **原生字节**：本分片全部关键 EA 均以 capstone 亲验（POIS 块 `0x76B6F0`/`0x76BD33`/`0x767A80`/`0x674F60`/`0x76B4D0`；DURA 块 `0x73E370`/`0x73E930`/`0x73EA40`/`0x788418`/`0x743D90`；clamp `0x4C700C`）。
- **C# 侧**：POIS-01..16 引用文件全部读证（`TBaseObject.Base.cs`、`NativePoisonTick.cs`、`NativeMagicMidStates.cs`、`NativeRedPoisonLevel.cs`、`NativePoisonFamilyClear.cs`、`TimedAbility.cs`、`TBaseObject.cs MakePosion`、`GameSvrConfig.cs`、`Grobal2.cs`）；DURA 侧读证 `TAbility.cs`、`TPlayObject.cs IsAddWeightAvailable`。
- **BLOCKED 8 条**（DURA-37..44）均为原生「入口/调用方/写者普查」类大缺口，按铁律只报不改；其中 DURA-43 的 edi 来源本次已定位（证据推进，未改代码）。

---

## 5. 修复项

**无。** 本分片当前 master 无外科式真缺口：

- POIS-01..07：全 FAITHFUL（其中 POIS-05 的边界 tick 分歧已在 master 修复）。
- POIS-08..16：毒代理已核验并落地，本次复核确认（含账本 3 条 MISSING → 已 FAITHFUL）；按指令「复核即可勿重复改」，未改动。
- DURA-36/45：FAITHFUL；DURA-37..44：BLOCKED 大缺口/分析产物，按「有据不臆造 / 大缺口只报不改」不动。

刻意不做的三处（若做即制造背离，违反铁律）：
1. POIS-16 家族清除——补空调用点 = 死代码（唯一调用者随 TKingOfIceMon 类链一并 fail-closed）。
2. DURA-39/44——为未证槽位臆造掉耐久公式。
3. DURA-40/42——在缺 CM 跳表/函数入口时臆造装卸与 slot-9 体的触发。

---

## 6. 分支 / 提交

- 分支：`w/eqv-09`（自 `00841828` 切出）
- 提交：本报告 `docs/eqv_shard09_20260814.md`（无代码改动，故仅文档提交）
