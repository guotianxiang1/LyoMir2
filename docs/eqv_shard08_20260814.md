# 等价核验报告 — 分片 08（EQUIPMENT / DURABILITY）

- 工作树：`D:\loym2\.claude\wt2\eqv-08`
- 分支：`w/eqv-08`（base `00841828`，`git merge-base HEAD master` = HEAD，工作树已含 master）
- 底本：`D:\loym2\staging\_reunpack_work\flat_image.bin`（ImageBase=0x400000）
- 反汇编：capstone 5.0.7（脚本置于 `%TEMP%\eqv08_disasm.py`，未入库）
- 分片：`D:\loym2\staging\_shards\shard_08.tsv`（26 行 DURA-10..DURA-35，均 TIER1）

## 核心结论

C# 移植用**扁平 `m_UseItems[16]`（HUMAN_EQUIPPED_ITEM_COUNT）+ 单个 `RecalcAbilitys()`** 抽象掉了
战神引擎的「带 VMT 的装备容器对象（player+0x4C0，16 槽，每类装备各自 VMT+0x60 槽位谓词）+ 两级重算
（容器 `sub_75EE78` + 角色 `sub_73D500`）」。因此多数「内存布局/OOP 派发」类契约在 C# 中是**结构性抽象**，
判定以**可观测行为等价**为准；行为类契约逐条对底本反汇编独立复核。

分片 TSV 的 `csharp_status` 为旧快照（除 DURA-26 外全标 UNKNOWN）——经复核，前序移植波已把绝大多数升级为
名副其实的 FAITHFUL，且相关 C# 已带精确到地址/字节的原生证据注释。**建议修台账**（见文末）。

**状态计数：FAITHFUL 24 / BLOCKED 2 / FAIL-CLOSED 0 / MISSING 0（合计 26）。**
本波**未改动任何 C# 代码**：所核验的冷/热路径均已 FAITHFUL，2 处 BLOCKED 属跨分片（MAGIC）或证据本身 UNPROVEN，
按铁律 fail-closed 只报不改（不臆造修复）。

---

## 逐行判定

| id | 新状态 | 依据（二进制 ↔ C#） |
|----|--------|----------------------|
| DURA-10 | **FAITHFUL** | `sub_73E93C`@0x73E9A2 `jge`→销毁分支，0x73E9DE `call 0x75F27C` dl=9；销毁器 0x75F2BB 清槽、0x75F348 `call 0x404690` 释放。C# `Magic.UseAmulet`：`if(dura<charm.Dura) 扣减 else {slot=null;RecalcAbilitys();SendDelItems;AddGameDataLog}` — `>=` 语义（scaled==dura 也销毁）一致 |
| DURA-11 | **BLOCKED** | 第二例程 `sub_73EA20`（装备槽9 **或** 背包扫描 TBujuk，扣**原始量**无×100，装备槽余量 `cmp ax,0x64/jb`<100 才 `call 0x75F27C` 销毁，否则仅 0x278D 包；cl=0 仅测试 / cl=1 消耗）。18 个调用者位于 MAGIC 子系统（0x688xxx/0x68Exxx/0x694xxx/0x6EDC90）。C# 施毒术走的是**相邻的内联路径** `0x6ED945`（MagicManager.cs:280-344，已 FAITHFUL），**未在装备代码中定位 0x73EA20 的 C# 对应**。跨分片，建议在 MAGIC 分片核验各调用者是否复刻「原始量扣减 + 阈值100销毁」 |
| DURA-12 | **FAITHFUL** | 0x73E989 `imul eax,[ebp-4],0x64` + 0x73E9A4 `sub word[edi+0x26],ax`，无除法。C# `UseAmulet`：`dura=(ushort)(nCount*100); charm.Dura-=dura`；`CheckAmulet` 门 `nCount*100<=charm.Dura+50` |
| DURA-13 | **FAITHFUL** | 0x73E997 `cmp eax,ecx`(ecx=Dura+0x32) / 0x73E999 `jg` 拒绝。C# `CheckAmulet`：`nCount*100 <= charm.Dura + 50`（拒绝时 result=false，UseAmulet 不执行） |
| DURA-14 | **FAITHFUL** | 0x75EC48 `sub dl,0x10 / setb al`（slot<16）；0x75EC20 索引 `[esi+eax*4+8]`（slot*4+8）。C# `m_UseItems = new TUserItem[Grobal2.HUMAN_EQUIPPED_ITEM_COUNT]`(=16)，越界由数组保证 |
| DURA-15 | **FAITHFUL**(结构) | player+0x4C0 装备容器 ↔ C# `m_UseItems` 字段；纯内部布局，行为经数组忠实建模，无可观测发散 |
| DURA-16 | **FAITHFUL** | 15 个 per-class VMT+0x60 谓词族（slot9 已亲验 0x762C64 `cmp dl,9/sete`）。C# `M2Share.CheckUserItems`(M2Share.cs:2011) 是其中心化转置，注释逐条列出谓词地址与 slot→StdMode 映射（U_BUJUK→StdMode 25 等），精确等价 |
| DURA-17 | **FAITHFUL** | 裸基类 TEquipItem VMT+0x60 = 0x75FE18 `33 C0 C3`(xor eax,eax/ret) 恒 false。C# `CheckUserItems` 起始 `result=false` 且**无 default 臂**，未列 StdMode 对所有槽保持 false = fail-closed「永不可穿」 |
| DURA-18 | **FAITHFUL** | 装备-带卸下：0x75F066 `call [ecx+0x60]`(资格) je 拒 / `call 0x75E9EC`(卸下) jl 拒 / 0x75F085 写槽。C# `ClientTakeOnItems`(Operate.cs:567-650)：CheckUserItems+CheckTakeOnItems→可卸检查→630 写槽(注释标 0x75F085)→DelBagItem→AddItemToBag 失败**回滚**，带详尽 0x6B7E9C/sub_75F044 证据 |
| DURA-19 | **FAITHFUL**(行为) | 空槽变体 0x75F1B7 `cmp [esi+eax*4+8],0/jne` 拒非空。C# 客户端装备走「带卸下」变体（占用则卸旧），空槽专用变体属内部（DB 加载等），可观测行为一致 |
| DURA-20 | **FAITHFUL** | 可卸门 0x75F6E0 返回 signed 1/-4（0x75F6F9、0x75F707 `mov ecx,0xFFFFFFFC`），消费者 0x75EA1C `test/jle`。C# 拒绝码 `n18=-4`，语义按有符号判 |
| DURA-21 | **FAITHFUL** | 0x75F6EE `test si,2`+0x75F6F5 `test dl,dl`（bit2 仅 mode==0 拒）；0x75F700 `test si,4`（bit4 无条件拒）。C# Operate.cs:590 `!m_boUserUnLockDurg && (Reserved&2)`、597 `(Reserved&4)` 无条件——极性一致（mode!=0 即 UnLock 时 bit2 不拦） |
| DURA-22 | **FAITHFUL**(行为) | 0x75EA11 `mov dl,byte[esi+0x374]`：mode 来自容器字段而非调用者。C# 以玩家标志 `m_boUserUnLockDurg` 承载并门控 bit2；字段语义 UNPROVEN（分片自述），但门控行为忠实 |
| DURA-23 | **BLOCKED**(COSMETIC) | 辅助槽过滤 0x75F1D8 收 {0,1,4,13} 并门控 equip/unequip/destroy 的 `[edx+0x1cc]` 调用（销毁器 0x75F332→0x75F340 亲验）。**+0x1CC 方法身份 UNPROVEN**（分片自述）；C# 用 `FeatureChanged` 无条件刷新外观，无法证明与「按 {0,1,4,13} 成员门控 +0x1CC」精确等价。仅外观影响 |
| DURA-24 | **FAITHFUL** | 容器聚合序 0x75EE78：0x75EE82 清零→0x75EE89..AC 循环 16 槽（0x75EE98 `test ax/jbe` **仅 Dura>0** 槽入 0x75EE04）→0x75EEAE 套装→0x75EEC5 收尾→0x75EED6 `+0x32`/0x75EEE3 `+2`（byte==7 尾条件）。C# RecalcAbilitys：清 aggregate→循环(2210，2216 跳 Dura<=0)→套装段(3061+)，序一致 |
| DURA-25 | **FAITHFUL** | 0x75F4F8 清 word[+0x370]/[+0x372] 两权重词 + 三块 FillChar(0x403B2C)：+0x48/0x1B0、+0x1F8/0x36、+0x22E/0x13E。C# 于重算开头 `m_AddAbil=new()`、`WearWeight=0/HandWeight=0` 全清后重累加 |
| DURA-26 | **FAITHFUL** | 0x7845B0 `mov eax,[eax+0x1c]`(模板) / `movzx ax,word[eax+0x1a]`(权重词)。C# 用 `StdItem.Weight`，无实例权重字段（B.3 结论：不会走错） |
| DURA-27 | **FAITHFUL** | per-slot 0x75EE04：0x75EE4A `cmp byte[ebp-1],1`；槽1 0x75EE67 `mov word[ebx+0x372],ax`(赋值)，其余槽 0x75EE57 `add word[ebx+0x370],ax`(累加)。C# TBaseObject.Base.cs:2241-2247(WEAPON→HandWeight 赋值 / 其余 3 特例槽→WearWeight+=) + 2441-2443(**所有其它槽**→WearWeight+=)，全槽覆盖且槽1赋值一致 |
| DURA-28 | **FAITHFUL** | 0x75EE57 word 目的加法无钳制，65536 回绕。C# `m_WAbil.WearWeight`(TAbility.cs:60 `ushort`)，`ushort +=` 默认 unchecked 截断→同样 65536 回绕 |
| DURA-29 | **FAITHFUL**(行为) | 容器权重词经 movzx 桥接到玩家 dword：+0x2CC 赋值、+0x2D4 赋值、+0x2D0 累加。C# 合并两级重算：WearWeight 循环内 `+=`(累加)、HandWeight `=`(赋值)，赋值/累加不对称保留 |
| DURA-30 | **FAITHFUL** | 0x6BE195 先 `call 0x73D500`(重算) 再 `cmp byte[ebx+0x1c2]`→翻倍 +0x2C8/+0x2D0/+0x2D8（三 Max 权重），兄弟 +0x2CC/+0x2D4/+0x2DC 不翻倍。C# TBaseObject.Base.cs:3059-3064 `if(m_boMuscleRing){MaxWeight+=MaxWeight;MaxWearWeight+=…;MaxHandWeight+=…}`。**注**：native 翻倍在 wrapper（重算之后），C# 内联于重算末尾（3059）但 HwanDevil 套装权重加成在 3087+（其后）——若同时 muscle ring+幻魔套，native 会连套装加成一起翻倍而 C# 不会（量级仅 ~20 负重，需全反 0x73D500 才能定论；已标注供可选深审） |
| DURA-31 | **FAITHFUL**(N/A) | 0x73D52E `lea edi,[ebp-0x1b8]`+`rep movsd` 把容器+0x48 拷进栈缓冲——纯 Delphi 实现技巧（edi-偏移=容器偏移的陷阱）。C# 扁平模型无此偏移混淆，直接算等价聚合，无可观测行为 |
| DURA-32 | **FAITHFUL** | 玩家侧字段清零(0x73D569..0x73D5FC) 先于拉入容器聚合(0x73D621+)。C# 于 2104-2122 清字段，循环 2210 才聚合，序一致 |
| DURA-33 | **FAITHFUL** | 0x278D 包 0x73E86B 推 `movzx word[ebx+0x26]`(Dura)+`movzx word[ebx+0x28]`(**实例** max，0x7845A8 `mov ax,[eax+0x28]` 佐证)，非模板+0x1C。C# `RM_DURACHANGE` 全用实例 `.DuraMax`（Operate/TBaseObject 各 SendMsg 点均一致） |
| DURA-34 | **FAITHFUL**(行为) | 0x73E9CB `push 0xa`(channel) 经栈参 [ebp+0xc]，销毁器 0x75F322 `mov dl,byte[ebp+0xc]`→0x768BE0（channel 字节，非 cx 颜色词）。C# `UseAmulet` 销毁分支写 `AddGameDataLog("持久耗尽"…)`（字符串 0x73EA14）。channel 0xA 具体含义 UNPROVEN（分片自述） |
| DURA-35 | **FAITHFUL**(行为) | 0x404828 是 Delphi `is` 走父链。0x73E97A 用类引用 [0x75E3F8] 测 `is`（接受子类）。C# 以 `StdMode==25`(+CheckAmulet 分 Shape 5=TBujuk / Shape1,2=TPoisons) 判定，等价于「接受该类族全部后代」——物品工厂 0x74C338 派发保证同 StdMode 所有 Shape 子类落同一类族 |

---

## BLOCKED / 跨分片明细

- **DURA-11（跨分片→MAGIC）**：`sub_73EA20` 是独立于 `sub_73E93C` 的第二护身符消耗例程，语义不同（背包+装备双查、原始量扣减、装备槽余量<100 才销毁、cl 决定测试/消耗），被 ~18 个魔法/技能处理器调用。C# 已忠实复刻**相邻**内联路径 `0x6ED945`（施毒术 wMagicID 6，MagicManager.cs:280-344，含 `ConsumeSpentPoisonCharm` 对应 `sub_73CC18` 销毁尾），但 `0x73EA20` 本体的 C# 对应未在装备代码中定位。**建议**：在 MAGIC 分片枚举 0x73EA20 全部调用者，核验各自 C# 处理器是否复刻其耐久语义。
- **DURA-23（COSMETIC，证据 UNPROVEN）**：`+0x1CC` 方法身份分片本身标 UNPROVEN，无法证明 C# `FeatureChanged` 与「按 {0,1,4,13} 槽成员门控 +0x1CC」精确等价。仅外观影响，需先证 +0x1CC 语义方可定级。

## 台账修正建议（shard_08.tsv，位于工作树外 `staging/_shards/`，未由本波提交）

- 24 行（DURA-10,12~22,24~35）`csharp_status` UNKNOWN → **FAITHFUL**，补 `csharp_ref`：
  - DURA-10/12/13/34/35 → `GameSvr/Spells/Magic.cs`（CheckAmulet/UseAmulet）
  - DURA-16/17 → `GameSvr/M2Share.cs:2011`（CheckUserItems）
  - DURA-18/19/20/21/22 → `GameSvr/Players/TPlayObject.Operate.cs:537`（ClientTakeOnItems）
  - DURA-24/25/27/28/29/30/32/33 → `GameSvr/Actors/TBaseObject.Base.cs:2083`（RecalcAbilitys）；DURA-30 另涉 :3059-3064
  - DURA-26 `csharp_ref` 旧值 `TBaseObject.Base.cs:2172` 为**过期行号**，实指「无实例 Weight 字段」，建议改指模板 `StdItem.Weight` 用点
  - DURA-14/15/31 标注为「结构性抽象（扁平数组），行为等价」
- DURA-11、DURA-23 保持非-FAITHFUL，标 BLOCKED 并记跨分片/UNPROVEN 缘由。
