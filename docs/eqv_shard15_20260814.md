# 等价核验报告 — 分片 15（MINE-57..61 / GILD-01..21）

- 日期：2026-08-14
- 分支：`w/eqv-15`（基线 `00841828` master）
- 修复提交：`6e52dbdc`（MINE-61）
- 底本：`D:/loym2/staging/_reunpack_work/flat_image.bin`（ImageBase 0x400000）
- 反汇编：`tools/m2_disasm.py`（capstone x86-32）
- 判据：`FAITHFUL` 忠实 / `FIXED` 本次外科修复 / `BLOCKED` 原生证据不足 / `MISSING` / `FAIL-CLOSED`

铁律遵循：全部结论有反汇编字节或源码行据；未臆造。**未执行 dotnet build**（按要求）。

---

## 一、状态统计

| 判定 | 数量 | 条目 |
|------|------|------|
| FAITHFUL | 23 | MINE-59, MINE-60, GILD-01..21（21 条） |
| FIXED | 1 | MINE-61 |
| BLOCKED | 2 | MINE-57, MINE-58 |
| MISSING | 0 | — |
| FAIL-CLOSED | 0 | — |
| **合计** | **26** | |

> 公会写操作（宣战/申请联盟/解盟/关注/退会/副会长）均门控于 `NativeCorpsService.SupportsGildWrites`（`_gildStore != null`）：注入 MySQL 行会库时 live 且忠实，未注入时 fail-closed 回 1000（与原版无库前行为一致，属**刻意设计**，非缺口）。本分片的 21 条 GILD 契约（关系语义/错误码/排序/结构）本身已忠实实现，故不逐条记 FAIL-CLOSED。

---

## 二、修复项（外科式）

### MINE-61 — 挖矿产出率正常档改回硬编码（提交 `6e52dbdc`）
- **缺口**：原生产出率**硬编码**，C# 正常档误用配置 `g_Config.nMakeMineRate`（TIER1 DIVERGENT，MONEY_ITEMS）。默认值 12 虽与原生同值，但配置化即偏离——运营下发非默认值会改变矿产经济。
- **原生证据**（`flat_image.bin`）：
  - `0x717715 B8 04000000 mov eax,4` → `0x71771A call 0x403B4C` ＝ 命中率 `Random(4)`（C# 早已硬编码 ✓）
  - `0x6BC2A3 80BB2818000002 cmp byte[ebx+0x1828],2`（疲劳档）
  - `0x6BC2AC B818000000 mov eax,0x18` → `Random(24)`（tier2，C# 早已硬编码 24 ✓）
  - `0x6BC2C3 B80C000000 mov eax,0xC` → `0x6BC2C8 call 0x403B4C` ＝ 正常档 `Random(12)`
- **改动**：
  - `GameSvr/Players/TPlayObject.cs`：正常档 `nMakeMineRate` → 硬编码 `12`（与已硬编码的 tier2 `24` 对齐，1:1 原生）
  - `GameSvr/Configs/GameSvrConfig.cs`：删除随之成为死配置的 `nMakeMineRate` 声明+初始化（全仓仅此 3 处引用，无外部/反射加载），避免 MINE-60 式无原生对应死字段
- **回归风险**：极低。默认值不变（12/24），仅移除偏离原生的配置旁路。

---

## 三、逐条核验

### 挖矿 / 采集 MINE-57..61

| ID | 判定 | 依据 |
|----|------|------|
| MINE-57 | BLOCKED | 契约自述 UNPROVEN（两门字节名 + 三个每动作成本计数器语义未证）。C# 已忠实建模两门字节：`m_btNativeFatigueTier`(+0x1828)、`m_btNativeCheatPenaltyTier`(+0x1829)；「三计数器」语义无原生证据可证，属研究限制而非 C# 缺口。 |
| MINE-58 | BLOCKED | 契约自述 UNPROVEN（三挖矿 ident 线格式仅 push-order 证据，slot 交换未证）。COSMETIC。C# `SendDefMessage(SM_MINESUCCESS,…)` 对齐已证据部分（`0x6BC2F8 mov dx,0x274`）；交换细节无据不改。 |
| MINE-59 | FAITHFUL | 自纠正已验证：反汇编确认 `0x6BC202 80BB2818000003 cmp byte[ebx+0x1828],3`（80 形，真实访问点），非中段误解码。C# `TPlayObject.cs:2266` `m_btNativeFatigueTier == 3` 硬阻断忠实。 |
| MINE-60 | FAITHFUL | 死配置「矿种最小值」已删——`GameSvrConfig.cs:514-516` 仅留说明注释，`= 56` 字段已不存在（全仓 0 命中）。无原生对应死字段已清除。 |
| MINE-61 | **FIXED** | 见上「修复项」。命中率 `Random(4)` 早已硬编码；产出率本次硬编码 `12/24`；死配置 `nMakeMineRate` 清除。 |

### 公会关系 GILD-01..21

C# 关系表复刻主体：`NativeCorpsService`（live）+ `NativeGildSaveRelation`/`NativeGild*Transaction`（dormant 契约模型，带字节证据）+ `NativeCorpsWireCodec`（线格式）+ `TPlayObject.NativeAttackMode`（战斗判定）。

| ID | 判定 | 依据（C# ↔ 原生） |
|----|------|------|
| GILD-01 | FAITHFUL | `NativeGildSaveRelation.IsRelationTypeInRange` `(rt&0xFF)<4`，越界→14。反汇编 `0x5E6E6F sub al,4 / 0x5E6E71 jb / 0x5E6E73 mov eax,0x0E` 确认。（公会代理已核，复核通过） |
| GILD-02 | FAITHFUL | `NativeCorpsDataSnapshot.GildRelationKey` 高位**有符号**、低位**无符号**比较，小者在前。反汇编 `0x5E6E8F cmp / 0x5E6E92 jne / 0x5E6E97 jae / 0x5E6E9B jge` 一致。 |
| GILD-03 | FAITHFUL | `NativeGildSaveRelation.Evaluate`：任一行会未解析→12（`0x5E6EF6`）。 |
| GILD-04 | FAITHFUL | 既存 1/2/3 关系→15（`0x5E6F0D dec/sub al,3/jae`）。`Evaluate` `existing∈[1,3]→RelationAlreadyPresent`。 |
| GILD-05 | FAITHFUL | 关系 1 写入：`NativeCorpsService` 接受申请 `_gildRelations[key]=(GildUnion,…)`（行 2279）；盟友列表由 `GetGildRelationPage` 按关系值派生（等价原生 gild+0x30 双向表）。 |
| GILD-06 | FAITHFUL | 关系 2 写入：`ApplyGildDeclareWarLocked` `_gildRelations[key]=(GildHostile,…)`+INSERT（行 1116-1117）；战争时长 3h 见 GILD-10。 |
| GILD-07 | FAITHFUL | 0/3 仅写表不改列表：loader `0x5E8D80` 证据（0..3 全入表，仅 1/2 另入 union/hostile 列表）—— `NativeCorpsStore.cs:476-487` 注释与逻辑一致。 |
| GILD-08 | FAITHFUL | 幂等对称：`NativeGildConcernSet.TryAdd` 先 `Contains` 后 `Add`（原生 sub_706A5C+sub_424AB8）；关系表按配对键唯一，叠加即覆盖，且 GILD-04 门阻止重建。 |
| GILD-09 | FAITHFUL | 关系 1＝盟友：`NativeCorpsService.GildUnion = 1`（生产者 sub_708168 `mov dl,1`）。 |
| GILD-10 | FAITHFUL | 关系 2＝战争 3 小时：`NativeGildWarExpiry`（CreateTime+dwGuildWarTime，默认 3h＝10800000ms）到期删关系；生产者 sub_703F74 `mov dl,2`。 |
| GILD-11 | FAITHFUL | 关系 3＝联盟申请挂起：`NativeGildRequestUnionTransaction` 建请求（Relation=3 INSERT 先于去重探测），入请求台账（`NativeGildRequestLedger`），无列表无计时（生产者 sub_704494 `mov dl,3` / TUnionGildRequest）。 |
| GILD-12 | FAITHFUL | 关系 0＝中立且未命中：`GetCombatRelation` 默认 `gildRelation=0`，`_gildRelations.TryGetValue` 未命中留 0（getter `0x5E78FB xor eax,eax`）。 |
| GILD-13 | FAITHFUL | 解盟仅限关系恰为 1：`NativeGildUnionConcernTransaction.BreakUnion` `!Allied→27(0x1B)`（`0x703D77 cmp al,1` / edi=0x1B）。 |
| GILD-14 | FAITHFUL | 盟友谓词仅关系 1：`IsNativePlayerAttackTarget` 行会档 `!SameGild && GildRelation != GildUnion(1)`；`IsNativePlayerFriend` 同（`0x683245 call getter / cmp al,1 / sete`）。挂起（3）非盟友。 |
| GILD-15 | FAITHFUL | 独立错误码：宣战侧 `RelationBusyState1=32(0x20)`/`State2=15(0x0F)`；联盟侧 `AlreadyAllied=15(0x0F)`/`AtWar=33(0x21)`。四码与 `0x70402C/0x704036/0x70454A/0x704554` 一一对应。 |
| GILD-16 | FAITHFUL | 关系以原始字节达客户端：`EncodeGildConcernSummaries` 记录 [24] 写关系字节（`0x6F6750 call 0x5E7890 / 0x6F6755 88 46 18 mov [esi+0x18],al`），不过滤 0/1/2/3。 |
| GILD-17 | FAITHFUL | 原生未证「关系 2 是否开 PK」——C# 忠实对齐原生**只测 ==1、绝不测 ==2**：`IsNativeHostileAttackTarget` 仅盟友(1)免战，战争(2)不额外开 PK（`0x683204`/`0x6846B0` 均仅测 al==1）。 |
| GILD-18 | FAITHFUL | TGild∶TObject，InstanceSize 0x38。C# `NativeGildSnapshot` 以托管对象语义建模（无需字节偏移复刻）。 |
| GILD-19 | FAITHFUL | 六个 TList：析构 `0x70639A..0x7063C5` 六次 `call 0x404690` 于 +0x2C/+0x34/+0x30/+0x20/+0x24/+0x0C——反汇编逐一确认。C# 六语义列表齐备（成员/入会/联盟请求/关注/盟友/战争）。 |
| GILD-20 | FAITHFUL | 字段布局：构造 `0x7062D0` 反汇编确认 id +0x18/+0x1C、name +0x10、members +0x0C、join +0x20、union +0x24、allies +0x30、war +0x34。C# 字段角色对齐。 |
| GILD-21 | FAITHFUL | 标志 +0x28 默认 1：`0x70633A C6 47 28 01 mov byte[edi+0x28],1` 精确确认。C# `NativeGildSnapshot.UnionEnabled=true` 与 `NativeGildUnionFlagCell._enabled=true`（`NativeGildConcernState.cs:211`）——补丁已在基线落地。 |

---

## 四、覆盖率

- 覆盖：26/26 条 = **100%**（全部给出源码行 + 反汇编字节双据）。
- 反汇编交叉验证地址（本次实跑）：`0x5E6E60`（GILD-01/02/03/04）、`0x717700`/`0x6BC2A0`/`0x6BC1FC`（MINE-59/61）、`0x7062D0`（GILD-20/21）、`0x706390`（GILD-19）。
- 未决（研究限制，非 C# 缺口）：MINE-57 三成本计数器语义、MINE-58 挖矿 ident 交换细节——均原生 UNPROVEN，无据不补。

## 五、结论

分片 15 的公会关系子系统（GILD-01..21）在 C# 侧已达高保真 1:1 复刻，关系语义、配对规范化、错误码阶梯、结构语义、战斗联盟判定、线格式均与原生字节证据吻合；写操作以 `SupportsGildWrites` 刻意 fail-closed。挖矿侧仅 MINE-61 一处配置化偏离，已外科修复并对齐原生硬编码。无遗留 MISSING/DIVERGENT。
