# 分片 07 等价核验报告 — CRAFT 尾 (opcode 1034 / 钻石熔炼) + DURA 头 (装备耐久)

- 日期：2026-08-14
- 分支：`w/eqv-07`，基点 `00841828`（Merge branch 'w/trade-storage'）
- 底本：`D:\loym2\staging\_reunpack_work\flat_image.bin`，ImageBase `0x400000`，capstone 5.0.7
- 契约：`staging\_shards\shard_07.tsv`（26 条：CRAFT-31~47、DURA-01~09）
- 判定口径：`FAITHFUL` / `FAIL-CLOSED` / `MISSING` / `BLOCKED`（有据不臆造）

## 结论摘要

| 状态 | 计数 | 条目 |
|---|---|---|
| FAITHFUL | 24 | CRAFT-31~45、DURA-01~09 |
| BLOCKED | 1 | CRAFT-46（config 依赖） |
| RESOLVED（原 BLOCKED→今 FAITHFUL） | 1 | CRAFT-47（原"survey 无输出"，C# 熔炼现已存在且忠实） |
| MISSING / FAIL-CLOSED | 0 | — |

**外科式修复：0 处（无真缺口）。** 分片内原被标 `DIVERGENT` 的 3 条（DURA-03/04/07）在当前 master 已被前序修复；`UNPROVEN` 的 CRAFT-45 之 populator 已定位，本轮升级为 FAITHFUL。覆盖率 26/26。

---

## CRAFT（opcode 1034 / 钻石熔炼 MakeItemUseDiam）

钻石熔炼 = PAS 命令 `MakeItemUseDiam`（native handler `sub_64DF3C` @0x738F77 注册）。
C# 完整复刻并已接线：`NativeMakeItemUseDiamTransaction.Execute` + `NativeDiamondForgeHost`
（`GameSvr/Services/NativeMakeItemUseDiam*.cs`、`GameSvr/Players/TPlayObject.NativeDiamondFoundry.cs`），
经 `PasApiBridge`（`diapeif`→选配方、`MakeDiamondItem`→熔炼、`FOUNDRYLIST`→列表）触发。

| ID | 判定 | 二进制证据（已反汇编核对） | C# 对应 |
|---|---|---|---|
| CRAFT-31 | **FAITHFUL** | 0x768BE0 先 push `word[+0x12C]`(=CurrX) 再 push `word[+0x130]`(=CurrY)；sink 0x79D3D8 存 `rec+0x20=[ebp+0x20]=CurrX`、`rec+0x24=[ebp+0x1c]=CurrY`。**实为自然序 X,Y，无交换**（`m_trade52_logserver` L27-28 亦标 rec+0x20=X/rec+0x24=Y；`+0x12C=CurrX/+0x130=CurrY` 见 m_sm_b/c/a_r2 多处佐证） | `TBaseObject.cs:1037` / 熔炼日志 `…\t m_nCurrX\t m_nCurrY\t…` 先 X 后 Y，字段→值映射与 native 一致。196 字节二进制 LogServer 记录本身按 `77BBAA33协议规范` L204-205 **明确 out-of-scope**，C# 以等价 tab 文本日志建模 |
| CRAFT-32 | **FAITHFUL** | 0x768C30 `lea edx,[ebx+0x115]`→rec+0x08；写入者 0x6B1A6A `mov word[ebx+0x115],0x3301`(len-1 '3' 地图名) + 随机 spawn X/Y 入 +0x12C/+0x130 | 熔炼/掉落日志均用 `m_sMapName` 于该位。**跨分片冲突**：TRADE-52 家族把 rec+0x08 标为账号名——本分片二进制证据（写入者写地图名）+ 通用位置日志语义支持"地图名"；建议 doc 侧统一，非本仓代码缺口 |
| CRAFT-33 | **FAITHFUL** | 发送器 0x765E68/0x765F6C：`0x765E71 mov [ebp-6],cx` → `0x765E96 mov word[ebx],ax`（cx=ident/opcode 存 rec+0）；1034 路径无 GBK 字面量/颜色字 | C# `SendMsg(...,int wIdent,...)` 以 `wIdent` 传 opcode |
| CRAFT-34 | **FAITHFUL** | 两发送器均 `cmp byte[esi+0x73],0 / jne 尾`（0x765E7D、0x765F81）；+0x73=m_boGhost（MarkDelete 0x7680EF 唯一写） | `TBaseObject.cs:3624 SendMsg`/`3664 SendDelayMsg`：`if (m_boGhost) return;`（含 CRAFT-34 注释、引原生 EA） |
| CRAFT-35 | **FAITHFUL** | 0x6C4C7B `mov edx,0xC0 / call 0x403b2c`（清 48 dword，[ebp-0xE0]..[ebp-0x24]，索引 48 落 [ebp-0x20] 循环计数器，无边界检查）；cap 0x6D0AF9 `cmp eax,0x30 / setle`（=48） | C# 用托管 `m_ItemList`（无定长栈缓冲，native 脆弱性天然消除）；`MaximumBagItems=48` 封顶等价 |
| CRAFT-36 | **FAITHFUL** | handler 0x64DF3C @0x738F77 注册（name ptr 0x73A0B4="MakeItemUseDiam"）；配方独立 TList | 独立类 `NativeDiamondFoundry`；`ExecuteNativeDiamondForge` 经 PAS `MakeDiamondItem` 触发 |
| CRAFT-37 | **FAITHFUL** | 0x64DF6A 取 `[+0x1818]`、0x64DF70 `dec`、0x64DF78 索引；0x64DF83/85 `xor edx,edx / mov [+0x1818],edx`（用后立即清零） | `Transaction.Execute` L117-118：`ResolveRecipe(selector)` 后 `host.FoundrySelector=0`；`ResolveRecipe` 用 `selector-1` |
| CRAFT-38 | **FAITHFUL** | rate `movzx word[rec+0x10]`(0x64E00F)、cost `word[rec+0x12]`(0x64E01E)、9 槽步长 17（0x64DFCA `shl 4`+`add`）自 rec+0x14、count `byte[slot+0x10]`(0x64DFE1) | `Recipe{SuccessRate,DiamondCost}`+`Materials`(≤`MaximumMaterials=9`，`NameMaximumGbkBytes=15`→ShortString[15]+count=步长17)、`Material.Count` |
| CRAFT-39 | **FAITHFUL** | consume(loop) 0x64DFE9 `call 0x74054c` → 单次 Random 0x64E007 `call 0x403b4c(100)` → 判定 → diamond charge 0x64E027 `call 0x6b6b3c`；失败串 0x64E20C="很遗憾锻造失败，除金刚石外其他物品均被消耗" | L142-159 先耗材再 `NextNativeRandom(100)` 判定，再 L161-168 扣钻；`MaterialOrChanceFailureMessage` 逐字吻合 |
| CRAFT-40 | **FAITHFUL** | 0x64E013 `cmp eax,word[rec+0x10] / jg 失败`（rand≤rate 成功，含端点；Random 返回 0..99） | `host.NextNativeRandom(100) > recipe.SuccessRate`→失败（>rate 才失败，即 ≤ 成功） |
| CRAFT-41 | **FAITHFUL** | 0x64DFD6 `cmp byte[edi],0 / je 0x64DFFA`（首个空槽跳出循环）；0x64DFF4 `inc / cmp esi,9 / jne`（0..8） | L148 `if (material.ItemNameGbkBytes.IsEmpty) break;`；循环上界 `MaximumMaterials` |
| CRAFT-42 | **FAITHFUL** | 0x64DF9D `test byte[[0x7D7038]],8 / je 关闭`；关闭串 0x64E194="实物锻造功能暂时关闭，请留意最新官方主页新闻。" | `IsServerSwitchBitSet(0,0x08)`；`DefaultMessage` 逐字吻合 |
| CRAFT-43 | **FAITHFUL** | 0x64E0BA `mov cx,0x38ff`（颜色）→ VMT+0xD4=0x73C8F4（`0x73C8F7 push ecx` 存颜色→rec+2、`0x73C903 mov cx,0x2774` opcode）；0x2774=RM 10100 分发臂 0x6B4E4E 再发 client ident 100，Param=颜色 | `SendSuccessSystemMessage(msg,0xFF,0x38)`→`SendMsg(RM_SYSMESSAGE,0,0xFF,0x38,…)`：颜色 0x38FF(fg=FF/bg=38) 还原；client 侧系统消息语义等价（内部 RM 号 11004 vs 10100 不可观测） |
| CRAFT-44 | **FAITHFUL** | 0x64E0ED `mov dx,0x22`(=34) → 0x64E0F4 `call 0x768be0`（日志构建，类别串 0x64E1E4="金刚宝石"） | `FoundryLogType=34`、`FoundryLogCategory="金刚宝石"`（逐字吻合），`WriteFoundrySuccessLog(type=34,…)` |
| CRAFT-45 | **FAITHFUL**（原 UNPROVEN，本轮定位 populator 后升级） | populator **已定位** `sub_74F818`：入口 0x74F847/0x74F850 一次性清零名字/数量字段（跨行沿用 quirk）；配方 list 分配于 0x74B 区（TList） | `NativeDiamondFoundry.TryLoad`/`TryParseRecipe`（读 `Gifts.txt`，注释逐点引 0x74F847/0x74F850/0x74F9E1/0x74FA0B 建模 quirk）；`GameApp.ReloadDiamondFoundry` 接线→`M2Share.DiamondFoundry`。残留：整段解析未逐字节 diff，但关键锚点已核 |
| CRAFT-46 | **BLOCKED** | 分发表已解，但成品 StdMode 来自 StdItems 配置数据（非二进制）——pile 销毁缺陷是否触发取决于配置 | C# 成品经 `CopyToUserItemFromName` 生成，pile 行为随 StdItems.txt；与 native 同为 config 依赖，不可仅凭二进制判定 |
| CRAFT-47 | **RESOLVED→FAITHFUL** | 原 spec §10"无 C# 熔炼、survey 两次无输出"系工具受限 | C# 熔炼系统现已完整存在并核实（见 CRAFT-36~45）；1034/MakeDrug 主路径亦在（`SendMsg` 注释引 0x63FFCE/0x63FFED）。BLOCKED（工具）解除 |

---

## DURA（装备耐久）

核心扣耐久 `DoDamageWeapon` = native `sub_73E804`，在 `GameSvr/Actors/TBaseObject.cs:3305`。

| ID | 判定 | 二进制规则 | C# 对应（当前 master） |
|---|---|---|---|
| DURA-01 | **FAITHFUL** | 实例 +0x1C=模板指针（deref 0x75FE35 等；nil 检查作 dword 0x73E972），模板 +0x1C=word DuraMax（0x783E91）；两义不可合并 | 模板 `TStdItem.DuraMax`(int) 与实例 `TUserItem`(独立类)分离；"有无模板"用 `GetStdItem(wIndex)!=null`/`wIndex>0`，不与 DuraMax 混用 |
| DURA-02 | **FAITHFUL** | 实例 +0x26=当前、+0x28=本实例上限（accessor 0x7845A0/0x7845A8）；模板 +0x1C 仅 spawn 上限 | `TUserItem.Dura`/`TUserItem.DuraMax`；主耐久路径读实例（`DoDamageWeapon` 读 `m_UseItems[U_WEAPON].Dura`） |
| DURA-03 | **FAITHFUL**（原 DIVERGENT，前序已修） | 规则：live max=实例 +0x28（承 DURA-02）。native 精确 case 站点未钉（跳表分发；`m_ident_pk_death_drop` L384 记"未定位") | `TBaseObject.Base.cs` AniCount 180/181/182/183 四处已改读实例 `m_UseItems[i].DuraMax`（2431/2436/2619/2624 带 DURA-03 注释），符合 DURA-02 live-max 规则。残留：native 指令级地址未逐条钉，但语义规则为 TIER1 |
| DURA-04 | **FAITHFUL**（原 DIVERGENT 1.03，已修） | 耐久 1/1000 单位，显示=raw/1000.0 half-even（常量 0x73E8D0=float32 1000.0；0x73E83B/0x73E896 fdiv） | `3320`/`3352`：`HUtil32.Round(nDura/1000.0)`（非 1.03）；攻击路径 `Attack.cs:1147` 仅调 `DoDamageWeapon`（无独立除数） |
| DURA-05 | **FAITHFUL** | 两除点 @ROUND half-even（0x403574）非 @TRUNC；仅取结果低 dword | `HUtil32.Round(double)`=`(int)Math.Round(r,MidpointRounding.ToEven)`（`HUtil32.cs:223`），取 int |
| DURA-06 | **FAITHFUL** | 除-round 仅用于"显示值是否变化"测试；存储值不过除（0x73E849 sub；0x73E88C `mov word[+0x26],si`） | `nDura -= nWeaponDamage`(原始减)后 `…Dura=(ushort)nDura`；除法仅入 `nDuraPoint` 比较（3320/3352） |
| DURA-07 | **FAITHFUL**（原 DIVERGENT 删除，已修） | 槽1 零耐久：原地 clamp `word[+0x26]=0`+recalc+发包，既不销毁也不卸下（无清槽/无 delete） | `3322-3341`：`Dura=0` 原地 + `RecalcAbilitys()` + `SendMsg(RM_DURACHANGE…)` + `return`；**不** 置 wIndex=0、**不** 发 delete 包 |
| DURA-08 | **FAITHFUL** | recalc 跳过零耐久：0x75EE9B `jbe` 跳过 apply（当前耐久<=0）；blocks 每次 recalc 于 0x75F4F8 清零 | `TBaseObject.Base.cs:2216`：`if (wIndex<=0 || Dura<=0) continue;`（在 RecalcAbilitys 装备循环内跳过 stat 应用） |
| DURA-09 | **FAITHFUL** | 槽1 损耗用 32 位寄存器减后范围测试，不会 16 位回绕（0x73E849 sub esi; 0x73E84C test; 0x73E84E jg） | `int nDura`(32 位) `-= nWeaponDamage` 后 `if (nDura<=0)` 范围判定，无 16 位回绕 |

---

## 覆盖率与方法

- 覆盖：26/26。CRAFT 二进制事实全部经 capstone 反汇编逐点核对（0x64DF3C 熔炼主体、0x768BE0/0x79D3D8 物品日志、0x765E68/0x765F6C 发送器、0x73C8F4 SysMsg 包装、0x6C4C7B/0x6D0AF9 缓冲与 cap、0x74F818 populator、0x738F77 注册），Delphi 字面量以 pstr/dstr 解码逐字对照 C# 常量。DURA 规则与 C# 逐条比对（`DoDamageWeapon`、`RecalcAbilitys`、`HUtil32.Round`、`TStdItem`/`TUserItem`）。
- 底本关键地址：见上表逐条 EA。
- 未做 `dotnet build`（遵铁律）。

## 说明（报告不改项）

1. **DURA-03 残留**：native 的 AniCount 180/181 recalc 为 Delphi `case` 跳表分发（玩家代码区无 `cmp reg,180/182` 立即数），且既有 doc 已记"未定位"；当前 C# 读实例 max 与 TIER1 的 DURA-02 live-max 规则一致，判 FAITHFUL；如需指令级钉死原生 case 站点，属独立热点调查。
2. **CRAFT-32 跨分片冲突**：rec+0x08 在本分片（熔炼/位置日志）二进制证据为地图名（写入者 0x6B1A6A），TRADE-52 家族标账号名——建议在 doc 层统一口径；C# 现用 `m_sMapName`，与本分片证据一致，无代码改动。
3. **196 字节 LogServer 二进制记录 out-of-scope**：CRAFT-31/32 涉及的 native 二进制日志传输（CMD 0xC4/体 0xBC）按 `77BBAA33协议规范` 明确不在 C# 重写范围；C# 用等价 tab 文本日志，字段值/相对顺序一致。
