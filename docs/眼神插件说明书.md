# 眼神 2.0.8 插件本地说明书

> 整理日期：2026-08-21  
> 在线来源：[战神引擎眼神插件更新日志及使用教程（未完成）](https://docs.qq.com/doc/p/9e6e93645a1697578ec179f8e04b6cab138c81fe)  
> 适用目标：`yanshen2.0.8.dll`，不得用 2.07 二进制、配置或行为替代 2.08 结论。

## 1. 文档定位与证据等级

本文是用于本地部署、脚本开发和 C# 复刻验收的功能规格，不是对在线文档的逐字复制。

- **[2.08 实机]**：已在本机 2.08 部署目录或 2.08 文件中直接核对。
- **[说明书]**：在线说明书明确列出或描述，但尚未以 2.08 运行行为复验。
- **[待验收]**：在线页只给出功能名或函数签名，具体参数、边界和副作用仍需测试。

重要版本边界：在线页的“初次安装教程”截图仍显示 `眼神2.0.7(7.19内测)`。因此截图中的旧包目录只作为历史安装参考；本说明书的 2.08 安装清单以本机真实 2.08 目录为准。

## 2. 2.08 安装与启动

### 2.1 文件布局 [2.08 实机]

当前可工作的 2.08 部署位于：

```text
Mud2.0\Mir200\Gs1\
└─ ys\
   ├─ yanshen2.0.8.dll
   ├─ version.ini
   ├─ libmysql.dll
   └─ libmysqls.dll
```

文件指纹：

| 文件 | 字节数 | SHA-256 |
|---|---:|---|
| `yanshen2.0.8.dll` | 22,508,544 | `103C3DC2DB3CA7BF8B1617503BA2DF57E7DA6B33295CC6AA28B10A89891558D2` |
| `version.ini` | 16 | `960C4926C80B86CEDDB0E55BE54A6B11D6043E94B4AEC4F840B5DFD75E870B95` |
| `libmysql.dll` | 1,134,592 | `373E327DA02DFF23F896C397CE10F6D76DCCBBADF45FE2C37C17F138D386E369` |
| `libmysqls.dll` | 13,822,976 | `D81869D1874D8016B6B2308CF9B77A6181B7C80D2AF1D4CA37E9E5CB1ADC3558` |

### 2.2 `version.ini` 的硬性格式 [2.08 实机]

`version.ini` 不是普通的 INI 节文件。它必须恰好包含 DLL 文件名：

```text
yanshen2.0.8.dll
```

当前实机文件是精确的 16 个 ASCII 字节，末尾没有 CR/LF、空格、引号或 BOM。出现“请检查 ys 文件夹中 [version.ini] 保存的版本是否存在或损坏”时，逐项检查：

1. `version.ini` 位于 `Gs1\ys`，不是 `Gs1` 根目录。
2. 文件内容与 DLL 文件名逐字节一致。
3. DLL 确实位于同一个 `ys` 目录。
4. 不要把 `yanshen2.0.7.dll` 或旧包的 `version.ini` 混入 2.08。
5. `libmysql.dll` 与 `libmysqls.dll` 均存在；缺库会在插件初始化前失败。

可用 PowerShell 自检：

```powershell
$ys = 'D:\赤焰武易\Mud2.0\Mir200\Gs1\ys'
$name = [Text.Encoding]::ASCII.GetString(
  [IO.File]::ReadAllBytes((Join-Path $ys 'version.ini')))
$name
Test-Path -LiteralPath (Join-Path $ys $name)
Get-ChildItem -LiteralPath $ys
```

期望输出 DLL 名，并且 `Test-Path` 为 `True`。

### 2.3 脚本目录 [说明书，2.08 待验收]

在线旧版安装图给出的脚本结构是：

```text
Mud2.0\Mir200\Envir\
├─ CommonScripts\
│  └─ 眼神专用\
└─ PsMapQuest\
   ├─ RunQuest.pas
   └─ RunMailQuest.pas
```

说明书称 `CommonScripts\眼神专用` 与 `PsMapQuest` 中的脚本用于触发函数和 API 包装。对 2.08 复刻时应按“需要的触发函数最小引入”处理，不应整包复制 2.07 脚本后宣称等价。

### 2.4 面板与授权 [说明书]

- 插件 GUI 可通过 M2 的设置面板重新呼出。
- 面板含 GM 工具、旧版配置、第二季配置和更新/说明页。
- 授权、换绑和查看次数属于原插件发行机制；C# 复刻只需要复现游戏功能，不需要复制第三方授权限制。
- 原面板提示：修改有参数的选项时，通常应先取消勾选，改参数后重新勾选保存，下一次启动读取保存值。

## 3. 面板功能总览

### 3.1 配置 1 [说明书]

| 类别 | 功能 |
|---|---|
| 拾取与物品 | 全屏拾取、捡物触发、禁止装备自动绑定、`give` 极品、装备来源、读取英雄装备 |
| 伤害与属性 | 刀刀切割（新/旧）、永久属性、新永久属性、特殊属性、移动速度、攻击速度、永久攻速、攻击反伤、千分比免伤 |
| 复活 | 复活触发脚本、复活戒指改 CD、复活戒指触发概率 |
| 触发脚本 | 被击杀触发、攻击触发、高级物理攻击触发、魔法攻击触发、高级魔法攻击触发、新穿戴触发、`穿戴触发_plus`、上线触发 |
| 倍攻暴击 | 新倍攻和暴击、主号高级倍攻和暴击、英雄倍攻和暴击、英雄高级倍攻和暴击 |
| 英雄与宝宝 | 英雄穿戴触发、英雄攻速/移速/施法速度、BB 杀怪触发、BB 死亡触发、特殊宝宝 |
| 脚本兼容 | `AddLimLF` 函数修改、`IncActivePoint` 函数修改、眼神特殊函数 |

冲突规则：新版与旧版切割同时启用会重复计算；新永久属性与旧永久属性应择一启用。这两组都必须加入复刻验收用例。

### 3.2 配置 2 [说明书]

| 类别 | 功能 |
|---|---|
| 地狱雷光 | 系数、范围、主属性切换 |
| 爆裂火焰 | 主属性切换、范围及系数 |
| 激光电影 | 主属性切换、范围及系数、命中概率 |
| 火球术 | 主属性切换、自定义范围 |
| 雷电术 | 主属性切换、自定义范围 |
| 冰咆哮 | 主属性切换、范围、切割 |
| 流星火雨 | 主属性切换 |
| 其他技能 | 魔法盾修正、嗜血术倍数/范围、免毒符、野蛮等级、技能等级突破 |
| 群毒 | 群毒开关、自定义群毒公式、绿毒/红毒参数 |
| 提示控制 | 禁止发言不提示、中毒飘血、删除技能不提示、升级技能不提示 |

说明书面板给出的群毒公式为：

```text
绿毒伤害 = A × 道术 ÷ B，每 K 秒一次，最低值兜底
双毒时间 = A × 道术 ÷ B，每 K 秒增加 1 秒，最低 15 秒
```

截图示例值为绿毒 `A=1, B=10, 最低=5`，双毒时间 `A=1, B=1, 最低=5`。这些是面板默认示意值，不能替代 2.08 的运行边界测试。

### 3.3 第二季第一、二页 [说明书]

| 类别 | 功能 |
|---|---|
| 元素与装备 | 自定义元素、装备转生穿戴判定、装备多职业、角色多阵营、英雄读取极品、`give` 极品 |
| 主号技能 | 英雄自动开盾、诱惑之光触发、烈火/冰咆哮/火墙/火符增伤，冰咆哮/火墙/烈火/雷电术/火符切割 |
| 宝宝与英雄 | 宝宝自动叛变、新呼唤宝宝、英雄千分比免伤、英雄野蛮、指定英雄放技能 |
| 范围与控制 | 全屏吸怪、自定义伤害、自定义麻痹/施毒/击退/定身/治疗/临时属性、技能弹射或溅射 |
| 高级触发 | `伤害触发脚本_plus`、高级物理攻击触发、高级魔法攻击触发、英雄物理/魔法攻击触发、`super` 攻击触发 |
| 服务功能 | 毫秒级 CD、全局循环函数、高级回收、新怪物爆率、获取沙城归属 |

在线页明确标记“主号施法速度（待修复）”和“技能触发脚本（弃用）”。复刻时不能把这两项当成稳定的 2.08 承诺；应优先实现高级触发链。

### 3.4 面板截图中可见的其他开关 [说明书]

第二组配置还列出：地狱雷光/爆裂火焰/激光/火球/雷电/冰咆哮/火雨的属性或范围切换、嗜血术倍数、免毒符、野蛮等级、群毒、删除/升级技能不提示等。截图显示大多数开关处于“已启动”，这只说明作者的示例环境状态，不表示所有部署都默认启用。

## 4. 已明确的 S 变量

### 4.1 新永久属性 [说明书]

主号：

| S | 含义 | S | 含义 |
|---:|---|---:|---|
| 12 | 最大 HP | 13 | 最大 MP |
| 14 | 物防下限 | 19 | 物防上限 |
| 15 | 魔防下限 | 20 | 魔防上限 |
| 16 | 攻击下限 | 21 | 攻击上限 |
| 17 | 魔法下限 | 22 | 魔法上限 |
| 18 | 道术下限 | 23 | 道术上限 |
| 203 | 幸运 | 204 | 腕力 |
| 205 | 背包负重 | 206 | 穿戴负重 |
| 207 | 准确 |  |  |

英雄：

| S | 含义 | S | 含义 |
|---:|---|---:|---|
| 30 | 最大 HP | 31 | 最大 MP |
| 32 | 物防下限 | 37 | 物防上限 |
| 33 | 魔防下限 | 38 | 魔防上限 |
| 34 | 攻击下限 | 39 | 攻击上限 |
| 35 | 魔法下限 | 40 | 魔法上限 |
| 36 | 道术下限 | 41 | 道术上限 |
| 208 | 幸运 | 209 | 腕力 |
| 210 | 背包负重 | 211 | 穿戴负重 |
| 212 | 准确 |  |  |

### 4.2 千分比属性 [说明书]

主号：`151` 最大 HP，`152/153` 最低/最高攻击，`154/155` 最低/最高魔法，`156/157` 最低/最高道术，`158/159` 最低/最高物防，`160/161` 最低/最高魔防，`162` 最大 MP。

英雄：`163` 最大 HP，`164/165` 最低/最高攻击，`166/167` 最低/最高魔法，`168/169` 最低/最高道术，`170/171` 最低/最高物防，`172/173` 最低/最高魔防，`174` 最大 MP。

### 4.3 新版主号切割 [说明书]

| S | 含义 |
|---:|---|
| 9 | 被切割防护概率；示例中 100 表示不受切割，0 表示允许 |
| 10 | 对怪百分比切割，1..1000 按千分比；超过 1000 作为固定神圣伤害 |
| 11 | 对怪固定切割 |
| 50 | 对人百分比切割，按千分比，不能超过 1000 |
| 51 | 对人固定切割 |
| 52 | 对怪百分比切割概率 |
| 62 | 对怪固定切割概率 |
| 53 | 对人百分比切割概率 |
| 63 | 对人固定切割概率 |

概率字段的说明书规则：值为 `0` 或 `>=4095` 时必触发；否则概率为 `value / 4095`。所有溢出、负数和目标类型边界均需 2.08 运行验收。

### 4.4 其他已列变量 [说明书]

- 英雄切割：`SetS(2,1)` 千分比值，`SetS(2,2)` 固定值，`SetS(2,3)` 千分比概率。
- 月灵新伤害：`SetS(1,215)` 轻击千分比调整，`SetS(1,216)` 重击千分比调整。
- 主号分身术：`SetS(1,200)` CD，`SetS(1,201)` 数量。
- 气功波重定义：`SetS(1,202)` 贴身推动距离。
- 自定义野蛮：`SetS(1,213)` 冲撞距离。
- 主号技能加成：`SetS(7, magicId, 千分比)`。
- 英雄技能加成：`SetS(8, magicId, 千分比)`。
- 英雄技能额外点数：`SetS(9, magicId, 点数)`。
- 概率格挡：`SetS(1,100)` 千分比概率。
- 刺杀免伤：`SetS(1,101)` 百分比免伤。

## 5. 脚本触发点

### 5.1 `CommonScripts` 初始化 [说明书]

说明书截图给出的入口是无参数过程：

```pascal
procedure init_ys();
begin
  // 初始化眼神变量或上线复制逻辑
end;
```

示例用 `SetS(1,9,100)` 防止上线人物被切割，并演示了按行会名/城主行会名写变量。复刻验收要确认初始化调用次数、热重载行为和异常隔离，而不是照抄示例业务逻辑。

### 5.2 `RunQuest.pas` 触发签名 [说明书]

| 过程 | 触发时机 | 参数 |
|---|---|---|
| `OnDia()` | 复活戒指触发 | 无 |
| `MyKill(KillerName, MapDesc)` | 角色被击杀 | 击杀者、地图描述 |
| `pickpre(ItemName, MapDesc)` | 拾取前 | 物品名、地图描述 |
| `Myattacked(roleid, magicId, types, hp, Name)` | 主号被攻击 | 来源对象、技能/怪物 ID、来源类型、伤害、名称 |
| `NewCutting(roleid, types, hp, curhp, maxhp, Name)` | 主号新版切割 | 目标、类型、伤害、伤前 HP、最大 HP、名称 |
| `HeroCutting(roleid, types, hp, curhp, maxhp, Name)` | 英雄切割 | 同上 |
| `KillMonster(MonName, MapName)` | 杀怪 | 怪物名、怪物所在地图 |
| `MyBlocking()` | 概率格挡成功 | 无 |

说明书提示：按怪触发的切割与杀怪过程开销较大。复刻时要保证开关关闭时完全不调脚本，打开时也应把脚本异常隔离在单次事件内。

### 5.3 全局循环函数 [说明书]

`Ys_SetTimerByName(Player, timer, fucName)` 用于周期调用 `RunQuest.pas` 中的无参数函数：

- 最快间隔为 500 ms。
- `timer=0` 清理同名定时器。
- `fucName='ClearAll'` 用于清理全部定时器。
- 需要同时启用“眼神特殊函数”。

## 6. AllFuc.pas API 索引

本节保留在线页公开的签名和可确定用途；没有明确返回码的函数统一标为 [待验收]。

### 6.1 自定义伤害、控制和效果

```pascal
function ys_MyJn_plus(Player:TPlayer; magicLV,baseHP,round,TargetX,TargetY,Canl,types,cuttingV:integer):integer;
function ys_MyJn_plus2(Player:TPlayer; magicLV,baseHP,round,TargetX,TargetY,Canl,types,cuttingV,lei:integer):integer;
function ys_MyJn_effect(Player:TPlayer; magicLV,baseHP,round,TargetX,TargetY,Canl,types,cuttingV,lei,effect:integer):integer;
function ys_MyJn_undead(Player:TPlayer; magicLV,baseHP,round,TargetX,TargetY,Canl,types,cuttingV,lei,effect,undead:integer):integer;
function ys_MyJn_delay(Player:TPlayer; magicLV,baseHP,round,TargetX,TargetY,Canl,types,cuttingV,lei,effect,undead,MgId,AttactId,double,delay:integer):integer;
function ys_Cutting(Player:TPlayer; round,TargetX,TargetY,Canl,types,cuttingV,lei,effect,AttactId,delay:integer):integer;
function ys_JiTui(Player:TPlayer; juli,fangxiang,gailv,fanwei,TargetX,TargetY,Canl,isqun:integer):integer;
function ys_JiTui2(Player:TPlayer; juli,fangxiang,gailv,fanwei,TargetX,TargetY,Canl,isqun,roleid:integer):integer;
function ys_TuiTui2(Player:TPlayer; why,level,gailv,fanwei,TargetX,TargetY,Canl,isqun,roleid:integer):integer;
function Ys_Mymabi(Player:TPlayer; timer,rand,round,TargetX,TargetY,Canl:integer; isqun:boolean):integer;
function ys_ShiDu(Player:TPlayer; shijian,leix,hp,gailv,fanwei,TargetX,TargetY,Canl,isqun:integer):integer;
function ys_ShiDu_effect(Player:TPlayer; shijian,leix,hp,gailv,fanwei,TargetX,TargetY,Canl,isqun,effect:integer):integer;
function ys_DoEffect(Player:TPlayer; round,TargetX,TargetY,all,effect:integer):integer;
function ys_Healing(Player:TPlayer; round,TargetX,TargetY,onehp,allhp,isStack,roleid,effect:integer):integer;
function ys_SubShuxing(Player:TPlayer; round,TargetX,TargetY,value,time,pid,roleid,effect:integer):integer;
function ys_AddShuxing(Player:TPlayer; round,TargetX,TargetY,value,time,pid,roleid,effect,IsOther:integer):integer;
function ys_AddShuxing_pro(Player:TPlayer; round,TargetX,TargetY,value,time,pid,roleid,effect,IsOther,types:integer):integer;
function ys_XiXue(Player:TPlayer; hp,bf_hp:integer):integer;
```

`Canl=0` 表示不限制施法距离，其他值表示允许距离。其余字段的符号、单位、上限和返回码以 2.08 行为测试为准。

### 6.2 元素、装备和物品

```pascal
procedure Ys_GiveNewItem(Player:TPlayer; ItemName:string; isbind,ys1,ys2,ys3,ys4,ys5,ys6,ys7,ys8,ys9,ys10,ys11,ys12,ys13,ys14,ys15,ys16,ys17:integer);
procedure Ys_GiveItemYS_JP(Player:TPlayer; ItemName:string; isbind,ys1,ys2,ys3,ys4,ys5,ys6,ys7,ys8,ys9,ys10,ys11,ys12,ys13,ys14,ys15,ys16,ys17,jp1,jp2,jp3,jp4,jp5,jp6:integer);
procedure Ys_GiveItem_ly(Player:TPlayer; ItemName,pname,desc1,desc2:string; isbind:integer);
function ys_GetYs(Player:TPlayer; pis,pid,isHero:integer):integer;
function ys_GiveDuar(Player:TPlayer; pis,val,types:integer):integer;
function Ys_NpcGiveItemYs(Player:TPlayer; ClientItemID,ys1,ys2,ys3,ys4,ys5,ys6,ys7,ys8,ys9,ys10,ys11,ys12,ys13,ys14,ys15,ys16,ys17:integer):integer;
function Ys_GetOther(Player:TPlayer; itemid,id,val,types:integer):integer;
function Ys_GiveBind(Player:TPlayer; itemid,flag:integer):integer;
function ys_WupinMakeIndex(Player:TPlayer; isall:boolean):string;
function ys_WupinGetData2Take(Player:TPlayer; MakeIndex:integer):string;
function Ys_GetClientItemIDByItemid(Player:TPlayer; Itemid:integer):integer;
function Ys_GetItemid(Player:TPlayer; ClientItemID:integer):integer;
function ys_Change_ly(Player:TPlayer; ClientItemID:integer; pname,desc1,desc2:string):integer;
function Ys_RepairInBag(Player:TPlayer; stdmode,IsHero:integer):integer;
function Ys_DropItembyId(Player:TPlayer; id:integer):integer;
function Ys_DropItembyName(Player:TPlayer; name:string):integer;
function Ys_GetItemJp(Player:TPlayer; types,id,jid:integer):integer;
function Ys_SetItemJp(Player:TPlayer; types,id,jid,val:integer):integer;
function Ys_GetItemDBData(Player:TPlayer; itemid,pid:integer):integer;
```

### 6.3 宝宝、英雄和组队

```pascal
function ys_SetPetV(Player:TPlayer; MonName:string; id,Ac,Dc,DcMax,Mac,Mc,Sc,gs,ys,hp,Maxhp:integer):integer;
function ys_MakeSlaveEx(Player:TPlayer; MonName:string; num,lv,Ac,Dc,DcMax,Mac,Mc,Sc,gs,ys,hp,Maxhp:integer):integer;
function Ys_GiveBBSkill(Player:TPlayer; magicId,gailv,shanghai,del:integer; name:string):integer;
function Ys_GiveBB_SX(Player:TPlayer; key1,key2:integer; shuxing,name:string):integer;
function Ys_SetHeroCSkill(Player:TPlayer; magicid,isrun:integer):integer;
function ys_BBflowme(Player:TPlayer; roleid:integer):integer;
function Ys_Getshuxing(Player:TPlayer; roleid,types:integer):integer;
function Ys_GetsxByName(Player:TPlayer; name:string; types:integer):integer;
function Ys_KillBBbyName(Player:TPlayer; name:string):integer;
function ys_GetMemberCount(Player:TPlayer):integer;
function ys_GetMember_roleid(Player:TPlayer; id:integer):integer;
```

### 6.4 地图、拾取、数据库和杂项

```pascal
function ys_pick(Player:TPlayer; round,v1,gbv,ismy:integer):integer;
function ys_DropItem(Player:TPlayer; num,range:integer; name:string):integer;
function ys_CheckMapMonByName(Player:TPlayer; MapName,MonName:string):integer;
function Ys_NewXiGuai(Player:TPlayer; round,lv,num:integer):integer;
function Ys_HuiShou():integer;
function Ys_SetTimerByName(Player:TPlayer; timer:integer; fucName:string):integer;
function Ys_TanTanSkill(Player:TPlayer; MagicId,x,y,roleid,times,round,double,cutting,effectid,js:integer):integer;
function ys_DecExp(Player:TPlayer; value,downlevel,tips:integer):integer;
function ys_SetA(Player:TPlayer; key:string; id,value:integer):integer;
function ys_SqlDbInsert(Player:TPlayer; sql:string; fg:boolean):integer;
function ys_SqlDbSelect(Player:TPlayer; sql:string):string;
function ys_SendMsg(Player:TPlayer; roleid,id,hp,start_x,start_y,to_x,to_y:integer; rs,img1,img2:string):integer;
function ys_ReName(Player:TPlayer; NewName:string):integer;
```

说明书唯一明确的回收错误码：`Ys_HuiShou()` 返回 `-999` 表示回收配置语法错误。其他返回值不得凭函数名猜测。

### 6.5 NpcFuc / 高层对象 API [说明书，待验收]

在线页还列出 `YSGetItem`、`YSGetItemID`、`YSSafeZone`、`YSBindItem`、`YSGetOnlinePlayerNum`、`YSGetBodyItem`、`YSKillRole`、`YSChangeRole`、`YSKillMon`、`YSFindPlayerByName`、`YSSay`、`YSyeman`、`YSCreateMon`、`YsAttact`、`YsSetG/YsGetG`、`YsSetStr/YsGetStr`。这些接口只有名称或短签名，须单独建立 2.08 用例确认空对象、离线玩家、地图不存在、越界数值和失败返回。

## 7. 关键功能操作说明

### 7.1 全屏拾取

- 服务端开关：启用“全屏拾取”。
- 脚本入口：`ys_pick(Player, round, v1, gbv, ismy)`。
- `round` 是范围；`ismy` 用于限制只拾取自己的物品；`v1/gbv` 的准确门控仍需 2.08 验收。
- 在线页另有客户端补丁教程。未安装配套客户端时，服务端逻辑即使执行也可能没有完整交互或显示。
- 必测：超范围、非本人掉落、背包满、重量不足、地面对象已被他人取走、堆叠物品和掉落归属时效。

### 7.2 新版切割

- 新旧切割不能同时打开，否则说明书称伤害会叠加。
- 至少分别测试人物、英雄、怪物目标，以及百分比和固定伤害两条路径。
- 触发脚本必须在开关开启时才调用；大量怪物场景要测性能和脚本异常隔离。
- 切割伤害是否参与反伤由“切割也反伤”开关决定；火墙反伤另受“火墙不反伤”控制。

### 7.3 自定义伤害与技能弹射

- `ys_MyJn_*` 系列用于范围、属性、切割、特效、不死族加成、技能/攻击 ID、倍数和延迟组合。
- `Ys_TanTanSkill` 可给原生技能设置弹射或溅射；说明书建议范围不要过大。
- 设定在服务器重启前持续生效。复刻应明确生命周期，并测试重载配置是否清除。

### 7.4 高级回收

- `Ys_HuiShou()` 返回 `-999` 时表示配置语法错误。
- 要求原子处理：解析失败不能先删物品再报错；成功时物品、金币/货币、日志和提示只执行一次。

### 7.5 指定英雄技能

- `Ys_SetHeroCSkill(Player, magicid, isrun)`。
- 说明书称仅支持法师、道士技能。
- `isrun=0` 当前释放一次；`isrun=1` 持续释放。
- “英雄不自动释放技能”属于定制开关，不能作为通用 2.08 默认行为。

## 8. 复刻验收矩阵

功能说明可以定义“要有什么”，但不能证明精确公式。每个功能至少通过以下层级：

| 层级 | 验收内容 |
|---|---|
| 开关 | 关闭时无副作用；开启后只影响对应功能；重启后按设计持久化 |
| 参数 | 0、1、最大值、负数、溢出、缺省值、非法字符串 |
| 对象 | 主号、英雄、宝宝、怪物、离线/死亡/幽灵目标 |
| 地图 | 安全区、跨地图、边界坐标、对象移除并发 |
| 战斗 | 未命中、免疫、0 伤害、致死、反伤、盾、延迟伤害和重复击 |
| 脚本 | 过程不存在、参数错误、抛异常、递归触发和高频性能 |
| 协议 | 客户端补丁缺失、消息顺序、特效和飘字 |
| 持久化 | 重启、热重载、角色上下线、英雄召回 |

优先级建议：

1. **P0**：安装加载、开关持久化、切割/自定义伤害、全屏拾取、脚本触发、元素/物品、英雄与宝宝核心链。
2. **P1**：技能公式修改、群毒、倍攻暴击、免伤、复活戒、回收、数据库接口。
3. **P2**：提示屏蔽、面板工具、定制功能、已标记待修复或弃用的功能。

## 9. 已知限制与后续维护

- 在线原文标题本身标注“未完成”，不是完整 API 契约。
- 文档中存在重复条目、旧名称和新推荐名称；本地说明书按功能合并，但复刻层需保留脚本兼容别名。
- 旧安装截图属于 2.07，不能作为 2.08 二进制证据。
- 功能说明可用于补齐产品范围，精确伤害公式、随机数顺序、状态时长、消息顺序和返回码仍须由 2.08 实机/静态证据确认。
- 后续每闭合一个功能，应在本文件对应条目追加：实现文件、审计项目、证据地址或实机用例，并把 `[待验收]` 升级为 `[2.08 实机]`。

