# 等价核验 · 分片 04（TRADE 段 TRADE-14..39，共 26 条）

- 日期：2026-08-14
- 分支：`w/eqv-04`　工作树：`D:\loym2\.claude\wt2\eqv-04`　起点：`00841828`（Merge branch 'w/trade-storage'）
- 底本：`D:\loym2\staging\_reunpack_work\flat_image.bin`（17,661,952 B，ImageBase `0x400000`）
- 反汇编器：capstone 5.0.7（`_eqv04_dis.py`，工作区外），Python 3.11
- **未执行 dotnet build。** 逐条 native 字节 + C# 源双向核对。

## 判定统计

| 判定 | 条数 | 契约 |
|---|---|---|
| FAITHFUL | 26 | TRADE-14..39 全部 |
| FAIL-CLOSED | 0 | — |
| MISSING | 0 | — |
| BLOCKED | 0 | — |

其中 **3 条属他人代理在改，只报不改**：TRADE-22（成交安全组）、TRADE-29（析构组）、TRADE-35（仓库组）。
**外科修复：0**（全段已忠实，无真缺口；依「有据不臆造」不制造假修）。

覆盖率：**26/26 = 100%**，每条均有 native 反汇编证据 + C# 源行号对位。

---

## 一、交易成交核心 TRADE-14..27（全 FAITHFUL）

C# 主体：`GameSvr/Players/TPlayObject.Operate.cs`（`ClientChangeDealGold` / `ClientDealEnd`）、`TPlayObject.cs`（`DealCancel` / `GetBackDealItems` / `IncGold`）、`Actors/TBaseObject.cs`（`AddItemToBag`）。

### TRADE-14/15/16 押金金币（`ClientChangeDealGold` 1755-1798）
`sub_6C44xx` 反汇编（m_nGold=`[ebx+0x15C]`，押金=`[ebx+0x6E0]`）：
```
0x6C44BE cmp esi,eax          ; eax=[+0x15C]+[+0x6E0]=gold+escrow
0x6C44C0 jg  0x6C4547         ; nGold > gold+escrow → 失败（TRADE-16 上限）
0x6C44C6 mov eax,[+0x15C] / 0x6C44CC add eax,[+0x6E0] / 0x6C44D2 sub eax,esi
0x6C44D4 mov [+0x15C],eax     ; gold = gold+escrow-nGold（TRADE-15 先复位后扣；TRADE-14 全子系统唯一 sub）
0x6C44DC mov [+0x6E0],edi     ; escrow = nGold
```
C# 1782 `if (m_nGold + m_nDealGolds >= nGold)` / 1784 `m_nGold = m_nGold + m_nDealGolds - nGold` / 1785 `m_nDealGolds = nGold`。成交侧仅经 `IncGold`(+0x28C) 贷记对端、从不再 sub m_nGold（见 TRADE-25）。**FAITHFUL。**

### TRADE-17 `DealCancel`（`sub_6C43C4` → C# 1401-1427）
`0x6C43CB cmp[+0x461],0/je`(!m_boDealing 返回) → `0x6C43D4` m_boDealing=0 → `0x6C43E5 dx=0x2A9` SM_DEALCANCEL → `0x6C43FF mov[eax+0xBAC],0`（**先无条件清对端指针**）→ `0x6C4409 call 0x6C43C4`（递归）→ `0x6C4410 mov[ebx+0xBAC],0`（**自己最后**）。C# 1420 `dealRemote.m_DealCreat=null` → 1421 递归 → 1423 `m_DealCreat=null`。**FAITHFUL。**

### TRADE-18 `GetBackDealItems`（`sub_6C4114` → C# 1596-1634）
`0x6C4128 esi=Count-1` 降序循环 `0x6C4130`→`call 0x424D4C`(Get)→`call 0x424AB8`(Add 到 `[ebx+0x508]`=m_ItemList)，**全程无 `0x6D0AE8` 空间检查**；`0x6C4150` Clear；`0x6C4161 add[+0x15C],eax`（**裸加**金币，不走 IncGold）。C# 1625 `for(i=Count-1;i>=0;i--) m_ItemList.Add` + 1631 `m_nGold += m_nDealGolds`。**FAITHFUL**（native fact：无容量门，退还可致背包 >48）。

### TRADE-19 六前置门（`sub_6C4580` 0x6C45A2-0x6C45E9 → C# 1833-1856）
六道全部 `je/jne 0x6C49EB`（`xor eax,eax`+SEH 收尾 = **静默返回**）：`[+0x461]`自 dealing、`[+0xBAC]`m_DealCreat、`[+0x73]`自 ghost、对端 dealing、对端 ghost、`cmp ebx,[eax+0xBAC]`互指一致。C# 六条 early-return 一一对应。`0x6C45EF mov[+0x684],1`=m_boDealOK(1857)。**FAITHFUL。**

### TRADE-20 1000ms 双边门（0x6C45F6-0x6C4638 → C# 1858-1863）
`0x6C4603 cmp edx,0x3E8/jb`(自<1000)`0x6C4617 cmp eax,0x3E8/jae`(对端≥1000跳过) → 任一 <1000 落 `0x6C4633 call DealCancel`。C# `((now-selfTick)<dwDealOKTime)||((now-partnerTick)<dwDealOKTime)` → SysMsg+DealCancel。`dwDealOKTime` 默认 `1000`（GameSvrConfig.cs:1232 = 0x3E8）。**FAITHFUL。**

### TRADE-21 对端未确认（0x6C4643/0x6C49C1 → C# 1871/2062-2066）
`cmp[eax+0x684],0/je 0x6C49C1`：两条 `cx=0xFFDB` SysMsg（自 `0x6C49C5`、对端 `0x6C49D8`）后落 `0x6C49EB` 返回，**不 DealCancel**。C# else 分支两条 SysMsg、无取消。**FAITHFUL。**

### TRADE-22 权限门（0x6C4650/0x6C4693 → C# 1878-1898）【他人代理在改·只报】
`0x6179DC/0x617A38(cl=2)` 内首测 `cmp[cfg+8],0/je→ret TRUE`（=boAuthOpen 开关）；否则双轮 `bt[obj+esi+0x193C]`(esi=1,0)。失败且有押金 → `0x69B22C`(@PlayerActiveValidate)+DealCancel。C# `if(boAuthOpen){自门→脚本+DealCancel; 对端门→…}` 结构对位。**结构 FAITHFUL；隶属成交安全组，本轮不动。**

### TRADE-23 phase-D 四检查（0x6C46E4-0x6C4752 → C# 1908-1924）
四道各失败 `→0x6C49B8`(DealCancel)，**在任何 mutation 之前**：自收对物 `[vmt+0x244]`、自金溢出 `0x6D7948`、对端收自物、对端金溢出。C# 短路 `bo11 &&` 四段，任一 false→else DealCancel。**FAITHFUL**（fail-fast 与短路可观测等价，无 per-check 消息）。

### TRADE-24 溢出谓词同源（0x6D7948 vs 0x6D791C → C# 322/1913）
预检 `sub_6D7948`：`edx+=[+0x15C]; cmp [+0x68C]; jg→TRUE(拒)`。贷记 `sub_6D791C`(IncGold)：`ebx=[+0x15C]+edx; cmp[+0x68C]; jg→fail`。**同字段(+0x15C/+0x68C 每角色)同 `>` 谓词**。C# phase-D `m_nGoldMax-m_nGold < amt` ≡ `gold+amt>max` = IncGold 322 `m_nGold+tGold<=m_nGoldMax`。phase-D 过 ⇒ 贷记必成。**FAITHFUL。**

### TRADE-25 mutation 顺序（0x6C477B/4835/487E/4959/49AA/49B1 → C# 1927-2055）
自物→自金(`[+0x28C]`)→对物→对金→finalize 对端(`call 0x6C4A98`)→finalize 自。C# 五段顺序逐一对位，`0x6C475E` 后无 abort。**FAITHFUL。**

### TRADE-26 丢弃返回值 + 无条件 Clear（→ C# 1933/1977/1988/2011/2044/2052）
`AddItemToBag`/`IncGold` 返回值均弃；`0x6C4AE1` escrow.Clear 无条件。C# 四处调用弃返回、两处 `m_DealItemList.Clear()` 无条件。**FAITHFUL。**

### TRADE-27 失败分支不可达（0x73D078/0x6D0AE8 → C# 2481）
内层 `sub_73D078` 仅两出口：`0x73D084 dl=1;call[+0x244]`(容量复检)`/je` 与 `test esi/je`(空)。`sub_6D0AE8`=`Count+(edx&0xFF)<=0x30`；成交每次 dl=1，nulls 预跳(0x6C478B/0x6C48B7)，phase-D 已保 count+N≤48 ⇒ 逐件加 1 恒可满足。C# 内层 `if(m_ItemList.Count<BagCapacity.Of(this))` 同门，phase-D 保过、返回被弃。**FAITHFUL**（闭合旧疑点：destroy 分支证不可达）。

---

## 二、序列化 / 析构 / 兜底 TRADE-28..30

### TRADE-28 押金不序列化（→ FAITHFUL）
全仓库 `m_DealItemList`/`m_nDealGolds` 引用仅落在交易逻辑与字段定义，**不在** `UsrEngn.GetHumData`、`DBSvr/Core/NativeHumanDataCodec.cs` 任一编解码路径。native +0x6DC/+0x6E0 也不在编码器区 0x6B0FF0..0x6B1764。押金为运行期字段，不入档、不随 NativeHumanData 透传。**FAITHFUL。**

### TRADE-29 析构释放押金（→ FAITHFUL）【他人代理在改·只报】
native `0x6AFB49..0x6AFB76` 枚举押金逐件 `call 0x404690`(free) —— teardown 无前置 cancel 即销毁押金（native fact 确认）。C# 托管态下 dtor 不显式 free，押金列表随 GC 丢弃，语义等价（既不退背包、也不入档），且损失被 TRADE-30 兜底封顶。**隶属析构组，本轮不动。**

### TRADE-30 10 分钟兜底（0x6B1BFC → C# UsrEngn.cs:1530-1534/1544-1545）
`TPlayer.Run`：`0x6B1BFC cmp ecx,0x927C0`(600000)`/jbe skip` → `0x6B1C17 call 0x6B2C7C` → `0x6B1C2C call 0x6B6510`(save)。`sub_6B2C7C` = `mov[+0x230],[+0x2AC]`(Abil.HP←WAbil.HP) + `call 0x6C43C4` = **DealCancelA 无条件包装**。C# 周期存盘 1533 `DealCancelA()`→1534 `SaveHumanRcd`；登出 1544-1545 同序。`dwSaveHumanRcdTime` 默认 `10*60*1000`（GameSvrConfig.cs:1200 = 0x927C0）。**FAITHFUL。**

---

## 三、仓库序列化 / 容量 TRADE-31..38

C# 记录编解码：`DBSvr/Core/NativeHumanDataCodec.cs`（缓冲区基址 = native `rec_full+8` = THumInfoData Data 体；已由 encoder 复核，见下）。

**记录基址复核（消解偏移歧义）**：encoder `sub_6B0FF0` 中 `edx=[ebp-4]`=rec_full；`0x6B100C esi=rec_full+8`（cap 帧），`0x6B16CC [ebp-0x18]=rec_full+0xF70`（bag 帧）。故 cap 绝对 `rec_full+0x516`↔C# `0x050E`（差 8）；bag 绝对 `rec_full+0xF70+0x1C8E=0x2BFE`↔C# `0x2BF6`（差 8）。**C# 全部偏移在 rec_full+8 帧内自洽。**

### TRADE-31 背包 48×208（0x6B16E9-0x6B171B → codec BagItemBase）
`imul edx,edi,0x1A`/`lea edi,[ecx+edx*8+0x1C8E]`/`mov ecx,0x34;rep movsd`（52 dword=208 B）/`cmp edi,0x30`(48)，源 item+0x20。C# codec 48 槽 × ItemRecordSize，round-trip 由 `DbGateRegressionCheck` 守。**FAITHFUL。**

### TRADE-32 普通仓容量断言非截断（0x74A648 → codec）
`0x74A65F cmp ecx,eax`(destCap vs Count)`/0x74A661 jl 0x74A69E`：Count>destCap → **零写**（跳过整个 208B×N 复制）。C# codec save 段将 StorageSpaceCount clamp 至 [24,192]，且全消费点先 clamp 到 MAX=192，Count 不可能越 192，与断言等价。**FAITHFUL。**

### TRADE-33 仓容 WORD @rec+0x516（0x6B112F save / 0x6B0CBC load → codec 0x050E）
save `mov word[esi+0x50E],ax`(esi=rec+8 ⇒ abs 0x516)，源 `[+0x6D0].container+8`；load `mov ax,[eax+0x516]`。codec `StorageSpaceCountOffset=0x050E`，注释(158) 明标「== record 0x516」。**FAITHFUL。**

### TRADE-34 载入 ≤48 不覆盖（0x6B0CC3-0x6B0CE7 → codec 359-361）
`cmp ax,0x30/jbe`(≤48 跳过覆盖) → `>48` 才写 container+8；随后 `0xC0`(192) clamp。C# codec `storedSpaceCount > 48 ? storedSpaceCount : 48`。**FAITHFUL。**

### TRADE-35 三仓库容器（0x6AD8D5-0x6AD91A → 单列表模型）【他人代理在改·只报】
native ctor：`[+0x6D0]`普通仓、`[+0x6D4]`账号仓（皆 THumStorage VMT 0x74A278），`[+0x6D8]`药仓（TVirtualHumStorage VMT 0x74A2D0）。C# 现以 `m_StorageItemList`(普通) + `NativeAccountStorage`(账号) + DrugStore 登记（TRADE-35 提交 91770dec）建模。**隶属仓库组，本轮不动。**

### TRADE-36 -1 锁哨兵 / 账号仓起锁（0x74A510/0x74A330 → C# 账号仓 11/40/51/73）
`0x74A510 cmp[eax+8],-1/setne`=IsUnlocked；ctor `0x74A330 mov[esi+8],-1`。C# `NativeAccountStorage.Operations.cs` 多处 `state.Capacity==-1` 起锁、需先 DB load。**FAITHFUL。**

### TRADE-37 三层容量（0x6AD8EB/0x6FA057/0x6F30B4 → C# ApplyNativeAuthenticationLimits）【tier 表简化·报告】
native：ctor `[esi+8]=0x30`(48) → `sub_6FA040` 授权测试 `0x6179DC(cl=3)` 过则查表 `[0x7D6608][tier]`、否则 fallback `0x18`(24) → 扩容 `0x6F30B4` clamp `0xC0`(192)。同函数上半 `[0x7D6080][tier]`/fallback 2M 设 m_nGoldMax(+0x68C)。
C# `TPlayObject.cs:36-51`：默认 48(STORAGE_PAGE_SIZE)，`(auth2|auth1)&0x08 ? 192 : 24`（gold `&0x01 ? 50M : 2M`），MIN=24/MAX=192 全程 clamp。
**结构三层齐备（48/24/192）**；差异在 native 按 **运行期 config tier 表**取值，C# 简化为固定 192/50M（auth 位 0x08/0x01 门）。表 `[0x7D6608]/[0x7D6080]` 内容运行期加载、静态镜像不可取证，且与 TRADE-35 容器重构耦合。**判 FAITHFUL（默认/满级 tier 一致）+ 标注 tier 表未建模，交 TRADE-35 车道裁决。热点·不改。**

### TRADE-38 账号仓扩容界 / DB 请求（0x6E57B0-0x6E57C6 → NativeAccountStorageClient）
native `[+0x6D4].cap+delta`：`0x6E57BD jl`(负拒) / `0x6E57C1 cmp 0x12C(300)/jg`(拒)；锁态发 `0x16B`、存 `0x16C`、item 记录 0xD0 B。C# `NativeAccountStorageClient.cs`：`MaximumCapacity=300`、`capacity is <0 or >300 → false`(45)、`LoadCommand=0x016B`(28)、`SaveCommand=0x016C`(29)、`ItemSize=0xD0`(33)。**GameSvr 侧 FAITHFUL**（DBSvr 侧本契约声明 out of scope）。

---

## 四、仓库 NPC 中介 TRADE-39（FAITHFUL）

`sub_6C2A34`(存) / `sub_6C2D7C`(取) 门：`[ebx+0xCD8]` NPC 非空且 == msg.Recog → 同图 `[+0x128]==[+0x128]` → `mov cx,0xF;call 0x7743E0`。helper `0x7743EC..0x774419` 两组 `cdq/xor/sub`(abs) + `0x774400 cmp/jg`、`0x774415 cmp/jl` = `|dx|≤15 && |dy|≤15`（X=+0x12C，Y=+0x130）。
C# 存仓 `ClientStorageItem`(2271 NPC 绑定门 / 2288 同图+`Abs≤15`)、取仓 `ClientTakeBackStorageItem`(2381 / 2398) **双门对称**。**FAITHFUL。**

---

## 五、前人结论订正 / 遗留

1. **记录偏移歧义已消解**：C# codec 的 `0x050E`/`0x2BF6` 与 native `0x516`/`0x1C8E` 差 8，源于 codec 缓冲区取 rec_full+8（Data 体）；encoder `[ebp-0x18]=rec_full+0xF70` 复核后全自洽，非 bug。
2. **TRADE-37 是本段唯一实质简化**：config tier 表 → 固定 192/50M。非 fail-closed（默认满配一致），交仓库车道。
3. `STATE_37_38_39_BLOCKED.md` 为状态特效(0x2A/0x2C/0x36)，与本 TRADE 分片无关。

---

## 六、交付

- 判定：**FAITHFUL 26 / FAIL-CLOSED 0 / MISSING 0 / BLOCKED 0**
- 外科修复：**0**（无真缺口）
- 只报不改：TRADE-22 / 29 / 35（他人车道）
- 覆盖率：26/26（100%，均有 native 字节 + C# 行号双证）
- 分支：`w/eqv-04`（起点 `00841828`）
- 提交：本报告文档（无代码变更）
