# 等价核验报告 — 分片 27 (SERVER-GROUP 跨服消息 + CASTLE 金库税率)

- 工作树: `D:\loym2\.claude\wt2\eqv-27`
- 分支: `w/eqv-27`
- 基提交: `00841828` (`Merge branch 'w/trade-storage'`)
- 底本: `staging\_reunpack_work\flat_image.bin` (ImageBase=0x400000, file_off = VA-0x400000)
- 反汇编: capstone 5.0.7 / Python311；脚本置于 `%TEMP%\eqv27_disasm.py`(工作树外)
- 分片: `staging\_shards\shard_27.tsv` (26 行: SGRP-24..45, CGLD-01..04)

## 结论摘要

**26 行的二进制证据全部逐一独立复核通过 — 无一条捏造。** 每个地址/字节/反汇编声明均在底本核实。

现状与 TSV `csharp_status` 列（旧快照）存在系统性偏差：前序移植波已把 ISM 常量层与 CASTLE 税率完全修正，但 TSV 未更新。按铁律以二进制为准，下表给出**当前 master 的真实判定**。

| 判定 | 计数 | 行 |
|---|---|---|
| FAITHFUL (C# 匹配 native / native 事实已证) | 18 | SGRP-24,27,28,29,32,33,34,36,37,38,39,42,43,45; CGLD-01,02,03,04 |
| DIVERGENT (行为仍偏离，大缺口/热点，只报不改) | 4 | SGRP-25,26,31,44 |
| MISSING (无 C# case，默认 sink 兜底=fail-closed) | 1 | SGRP-30 |
| UNPROVEN/BLOCKED (需 DBSvr 侧或不可证负向) | 3 | SGRP-35,40,41 |

**代码改动: 无。** 所有可证缺口要么已由前序波修复（ISM 常量 / CASTLE 税率），要么是需整体移植 native handler 的行为热点（202/207/217/218 语义、247）。无干净、可证、单点、在范围内且尚未完成的小修复。详见「未改动理由」。

## 关键发现: ProcessOthGsMsg 跳表已完整解码

内层 `ProcessOthGsMsg = sub_657110`（外层 `sub_712EC8` 的 0x6F=111 分支在 `0x712F3F` 调用它；`0x712F4F mov ecx,[ebx+4]` 即 SGRP-42 的丢弃参数）。

- `0x657144 add edx,0xffffff36` (= -202)，`0x65714A cmp edx,0x37`，`ja 0x6573A0`(日志 sink) — **SGRP-45 证实**。
- 索引字节表 `@0x657160`(56 字节, ident=202+idx) → 地址表 `@0x657198`(28 项 dword)。
- 解出映射（idx→group→handler）与 shard 全部地址逐一吻合：

| ident | handler | shard 行 | 语义(native) |
|---|---|---|---|
| 202 | `sub_658384`→`sub_653ED0` | SGRP-25 | 反作弊惩罚('SD000'@0x65402C, 置 `byte[+0x1829]=3` 惩罚标志+时长) |
| 207 | `sub_658114` | SGRP-44 | 单引号位图扫描(读全局0x7D7038, `cmp al,0x27`, `cmp bl,0x25`) |
| 216 | `sub_6579D8` | SGRP-27 | 离婚(字面量0x657AAC, cx=0xFFDB, 0x278E→0x765E68) |
| 217 | `sub_657CF0` | SGRP-26 | 师徒: 徒弟离开(`dec byte[+0xB97]`, 清槽[+0xC78]) |
| 218 | `sub_657AC0` | SGRP-26 | 师徒: 逐出师门(清 `byte[+0xB95/0xC58/0xB98]`) |
| 222 | `sub_657700` | SGRP-28 | 唯一显式长度门 `cmp ecx,0x24 @0x65771C` 后读 word[+0x22]/[+0x20] |
| 241 | `sub_655A18` | SGRP-31 | CreditCard 全清(遍历在线, `call 0x71424C`+`0x6B99E4`) |
| 247 | `sub_65805C`→`0x699310` | SGRP-30 | 门 `cmp ebx,0xC`; C# 缺失 |
| 257 | inline | SGRP-32 | `mov eax,[0x7D6324]; mov byte[eax],1`(三指令无 call) |
| 默认 | `0x6573A0` | SGRP-24 | IntToStr+格式串"[Error]: ProcessOthGsMsg Ide"(len 0x1F@0x657458)+MainOutMessage |

native 真实 handler 27 个的 REAL/SINK 分布与 `Grobal2.cs:1751-1800` 注释**逐一吻合**（前序波已按跳表校准）。

## 逐行判定

### SGRP-24 FAITHFUL (证据已证)
`sub_712EC8` 外层 ladder 按 `word[body+0]` 分类：0x69/0x6F/0x72/0x74/0xCA/0x130 有分支，其余 `jmp 0x713042`(仅 `xor eax,eax`+SEH清理+`call 0x405500`字符串析构+ret，**无日志**)。0x6F→ProcessOthGsMsg，内层未命中才走日志 sink。C# 传输层(`SnapsmService`/`SnapsmClient` 文本协议)的默认分支 `MirrorMessage.cs:165-168` 复刻内层日志 sink 文本。✓

### SGRP-25 DIVERGENT (行为热点, 只报不改)
native 202 = 反作弊惩罚已**字节级证实**(`sub_653ED0 @0x653F35 mov edx,0x65402C`='SD000'；遍历玩家置惩罚标志/时长)。C# 常量层**已修**(`Grobal2.cs:1751 ISM_ANTICHEAT_PENALTY=202`，`ISM_USERLOGOUT=202` 标 Obsolete)，**但 `MirrorMessage.cs:31` 仍 `case ISM_USERLOGOUT: MsgGetUserLogout`** → 行为仍是用户登出。忠实化需移植 `sub_653ED0`(玩家遍历+惩罚状态机) = 大缺口。**本子系统最高风险语义错配依旧存在。**

### SGRP-26 DIVERGENT (行为热点, 只报不改)
native 217/218 = 师徒处理器已证：217 消息"你的徒弟 X 自行离开了师门！"(0x657DE0/0x657DF4)、218 清师父字段并发 0x657BB4，两者 cx=0xFFDB。C# 常量层**已修**(`ISM_MENTOR_STUDENT_1/2`，`ISM_FRIEND_CLOSE/RESULT` Obsolete)，**但 `MirrorMessage.cs:107-113` 仍把 217/218 归入 `MsgGetUserMgr` 空桩**(仅拆名不做事)。忠实化=移植师徒系统=大缺口。

### SGRP-27 FAITHFUL ✓
216 离婚：native 字面量 0x657AAC = GBK **"你的配偶与你离婚了"**(len 0x12)，cx=0xFFDB，附加 0x278E→`0x765E68`，`SendMsg` push 7。C# `MsgGetDivorce`(`MirrorMessage.cs:451`) 逐字节对应(同串、`RM_MASTERRELATION`+7、Red、`RefShowName`)。(注: native `sub_6579D8` 不校验 serverNum；C# 加了 `serverNum!=nServerIndex` 守卫，在 native 路由模型下恒真——已由 MarryClusterCompatCheck 审计固化，接受。)

### SGRP-28 FAITHFUL (证据已证)
222 `sub_657700`：`0x65771C cmp ecx,0x24`(长度≥0x24)+`jl` 后 `0x657762 movzx eax,word[ebx+0x22]`、`0x657779 movzx ecx,word[ebx+0x20]`。唯一显式数值长度门，属实。

### SGRP-29 FAITHFUL (负向结论已证)
串消费助手 `sub_405774`(取长度前缀字节 `cl=byte[edx]` 再 `LStrFromPCharLen`) 与 `sub_405708`(空终止扫描) 均为 Delphi 有界串构造；在完整收帧下不越读。属实。

### SGRP-30 MISSING (fail-closed 兜底)
247 `sub_65805C`：`call 0x78FE80`(使能)+`cmp ebx,0xC`(门)后读 body 三 dword 调 `0x699310`。**已证为真实 handler**。C# 有常量 `ISM_IDENT_247=247` 但 **switch 无 case** → 落默认日志 sink(fail-closed)。忠实化需移植 `sub_65805C`/`0x699310`/`0x78FE80`+全局 0x7D5D20，且 C# body 是字符串而 native 按二进制 dword 读取(格式不同) = 大缺口。

### SGRP-31 DIVERGENT (部分忠实, 只报不改)
241 `sub_655A18` = CreditCard 全清已证(遍历在线，`cmp byte[+0xD2C]`+`cmp byte[+0x73]` 后 `call 0x71424C([+0x1824])`+`0x6B99E4`)。C# 常量碰撞已解(`ISM_CREDITCARD_CLEARALL=241` 主，`ISM_GUILDWAR` Obsolete)。`MirrorMessage.cs:67` 空 body→`ResetOnlineAll`(信用卡清，路径在)，非空 body→行会战(**C# 扩展**，native 241 完全不解析 body)。信用卡清路径忠实；241 上的行会战为 C# 附加语义。

### SGRP-32 FAITHFUL ✓
257 inline `mov eax,[0x7D6324]; mov byte[eax],1`(三指令无 call) 已证。C# `NativeFireKingEventState.ForceLocally()`(`Volatile.Write(s_forced,1)`) 语义对应置位。

### SGRP-33 FAITHFUL (native 传输事实已证)
外层 category 0xCA(=202, **非** OthGs ident)：`0x712FD5 mov ebx,[ebx+4]`(取帧内 dword 作对象指针)、`0x712FE7 cmp byte[ebx+0x675],4`、`0x713021 mov ebx,[eax]`+`0x713023 call dword[ebx+0xD4]`(虚调用)。唯一无界线上解引用，属实。C# 传输层为文本协议，不复刻此二进制 DB-link，故无对应可改代码。

### SGRP-34 FAITHFUL (native 事实已证)
`sub_772DAC`：`0x772DB2 cmp byte[ebx+0x73],0`(解引用1)、`0x772DBA call 0x772DA8`(=`mov al,[eax+0x74]`, 解引用2)后才判 al；范围内无 `call 0x404828`(@IsClass)。校验前已信任并解引用，属实。

### SGRP-35 UNPROVEN (需外部) 
DB 链为出站客户端(`sub_713AAC` 重连 `cmp eax,0x2710`+`call 0x4C97D8`；端点 'DBAddr'/'DBPort' @0x79462C/0x79463C)。0xCA 可达性依赖控制/MITM 配置的 DBServer，保持 UNPROVEN(证据一致，不作远程可利用断言)。

### SGRP-36 FAITHFUL (负向, native 事实)
DB 链除 0x33AABB77 magic 外无逐帧鉴权/校验和。负向结论，与解码 builder/reader 一致。

### SGRP-37 FAITHFUL (证据已证)
跨服解锁 request-only：`0x628C69 dx=0x185`+`call 0x713094`(空体)→`0x628C72 cx=0xFFDB` 自打回执 0x62D50C；按名 `0x628CAE dx=0x186`+`call 0x7130E8`→回执 0x62D52C。无回复 ident/完成回调。属实。

### SGRP-38 FAITHFUL (证据已证)
`0x628CE4` 空名分支 `cx=0x38FF`(红, 字面量0x62D548)；成功回执 0x62D50C/0x62D52C 用 `cx=0xFFDB`(绿)。极性属实。

### SGRP-39 FAITHFUL (证据已证)
位设助手 `sub_619F74`：`0x619F98 cmp bl,7`+`ja`(封顶索引7)、`0x619F9D and ebx,0x7F`、`0x619FA0 bts dword[eax+0x26],ebx`；身份经 `call 0x49F98C`([esi+0x1C]+两参)匹配。属实。

### SGRP-40 UNPROVEN (报告采信)
0x19B(0x50 字节)负载 word[+0x3E] 各调用点为零——未逐一复核全部 6 站，采信原报告；结论(不作 ident 载体)与 `sub_713094` 不初始化 word[body+2] 一致。

### SGRP-41 BLOCKED (需 DBSvr 侧)
无 M2Server 对 202..257 的发送方(无 `mov word[...],0x6F`+ident writer)。最近出站候选 type-1 ident 0x19B 无法在缺 DBSvr 侧时配对。保持 UNPROVEN。

### SGRP-42 FAITHFUL (证据已证)
`0x712F4F mov ecx,[ebx+4]`(=dword[body+4])传入 ProcessOthGsMsg，但 `0x657110` 序言仅 `mov word[ebp-2],dx`(ident)，从不存 ecx；各 case 用栈参 [ebp+8]/[+0xC]/[+0x10]。ecx 丢弃属实。C# `ProcessData(ident,serverNum,body)` 参数面对齐。

### SGRP-43 FAITHFUL (证据已证)
SQL 模板用 Delphi %d/%s：`@0x658730`="select * from castleord order by TotalTime desc,acttime limit **%d**,1;"(0x658560 处 `mov eax,0x658730` 引用)。若 C# 移植这些 builder，须用顺序替换助手，禁喂 `string.Format`。属实。

### SGRP-44 DIVERGENT (行为热点, 只报不改)
native 207 = 单引号位图扫描已证(`sub_658114` 读 0x7D7038，`cmp al,0x27`/`cmp bl,0x25` 位测)。C# 常量层已修(`ISM_SINGLEQUOTE_SCAN=207`，`ISM_RELOADGUILD/ISM_SERVERSWITCH` Obsolete)，**但 `MirrorMessage.cs:52` 仍 `case ISM_RELOADGUILD`** → switchWord/重载行会。行为仍偏离；忠实化=移植 SQL 注入过滤位图=大缺口。

### SGRP-45 DIVERGENT→设计说明 (只报不改)
-202 无符号窗口已证：任何 <202 的 ident 经 `add edx,-202`+`ja` 落日志 sink。C# `ISM_GROUPSERVERHEART=100/ISM_USERSERVERCHANGE=200/ISM_USERLOGON=201` 在窗外，native 无对应(会命中 sink)。C# 传输层为文本协议、不强制 202..257 窗口，这三者是**已注释的 C# 扩展**(`Grobal2.cs:1745-1748`)，C# 内部自洽。属设计扩展，非缺陷。

### CGLD-01..04 FAITHFUL ✓ (MONEY, 建议台账登记)
- **CGLD-01**: `@0x65B39C tbyte = cd cc cc cc cc cc cc cc fa 3f` → sign0 / exp 0x3FFA(unbiased -5) / mant 0xCCCCCCCCCCCCCCCD = **0.05**；`0x65B32C fld xword[0x65B39C]` 读取。字节与解码属实。
- **CGLD-02**: 同 10 字节按 float32/float64 读为垃圾，仅 80-bit Extended 得 0.05。C# **不重解释 tbyte**，而是 `nCastleTaxRate/100.0`=5/100.0 现算 0.05，**未犯 CGLD-02 错误**。
- **CGLD-03**: `0x65B329 fild dword[ebp-4]`(**signed**)、`0x65B332 fmulp`、`0x65B334 call 0x403574`=@ROUND(`fistp qword`用当前 FPU 就近偶数=**银行家舍入**)。C# `HUtil32.Round`(`Math.Round(x,MidpointRounding.ToEven)`)+`int nGold` 精确对应。
- **CGLD-04**: @ROUND 返回 Int64 于 edx:eax，`0x65B339` 立即从 [ebx+0x84] 重载 edx、`0x65B341 add ecx,eax` 仅用低 dword。C# 用 int 结果，现实量级无差(与原报告"realistic magnitudes 无害"一致)。

**C# 实现 `GameSvr/Castle/UserCastle.cs:876 IncRateGold` 为逐条精确复刻**：
- 税 = `HUtil32.Round(nGold * (nCastleTaxRate/100.0))`(整数×0.05；double 与 Extended 因常量同向上舍入，就近偶数结果恒等)。
- 日额 `m_nTodayIncome` 封顶 `nCastleOneDayGold`(=2,000,000=0x1E8480)，`<=` 加/否则取部分并置顶 — 对齐 native `[+0x84]` 逻辑(`0x65B343 cmp ecx,0x1E8480`/`0x65B366`)。
- 总额 `m_nTotalGold` 封顶 `nCastleGoldMax`(=100,000,000=0x5F5E100)，`<` 加/否则钳位 — 对齐 native `[+0x80]`(`0x65B37C cmp edx,0x5F5E100 jge`/`0x65B38C`)。
- 日额跨日重置 `UserCastle.cs:420-427`(无永久封锁 bug)。
- 配置默认 `GameSvrConfig.cs:1488/1500/1499`: nCastleTaxRate=5, nCastleOneDayGold=2000000, nCastleGoldMax=100000000 — 全部匹配 native 硬编码值。

## 未改动理由 (fail-closed 决策)

1. **ISM 常量层**(SGRP-25/26/31/44)已由前序波修正且经本次跳表复核正确；`Grobal2.cs` 在禁改名单内，无需亦不应改。
2. **CASTLE 税率**(CGLD-01..04)已在 `UserCastle.IncRateGold` 逐条字节级忠实，无缺陷。
3. **行为重布**(202→反作弊、207→注入过滤、217/218→师徒、247→缺失 handler)均需整体移植 native handler(玩家遍历/状态机/DB 调用/二进制 body 解析)，属大缺口/热点，按铁律**只报不改**；且贸然重布会破坏现有 C# 发送侧对旧 ident 语义的依赖。
4. 其余 UNKNOWN 为 native 传输层(category ladder / 0xCA DB-link / 帧格式 / SQL 模板)分析事实，C# 采用不同的文本传输层，无对应可改代码。

故本分片无干净、可证、单点、在范围内且尚未完成的小修复；改动仅为本报告。

## 建议台账修订 (`shard_27.tsv`, 以二进制为准)

- **CGLD-01/02/03/04**: `UNKNOWN` → `FAITHFUL`；`csharp_ref` = `GameSvr/Castle/UserCastle.cs:878`(+`SystemModule/HUtil32.cs:120` @ROUND, `GameSvr/Configs/GameSvrConfig.cs:1488` 默认)。
- **SGRP-25/26/44**: `DIVERGENT` 保留，但注明"常量层已修(Grobal2 Obsolete+正名)，**行为重布仍待办**(switch 走旧语义)"。
- **SGRP-31**: `DIVERGENT` 保留，注明"常量碰撞已解；信用卡清路径在(空 body)；241 上行会战为 C# 扩展"。
- **SGRP-30**: `MISSING` 保留(常量已加，无 case，默认 sink fail-closed)。
- **SGRP-27/32**: `FAITHFUL` 确认无误。
- **SGRP-24/28/29/33/34/36/37/38/39/42/43/45**: `UNKNOWN` → 建议标 `VERIFIED-NATIVE`(证据已证的 native 事实/负向结论；C# 无对应传输层代码或已自洽)。
- **SGRP-35/40/41**: 保留 `UNPROVEN`(需 DBSvr 侧/不可穷尽负向)。

TSV `csharp_status` 与行号多为旧快照：本次核验对当前 master 重新判定，未照旧盲改。
