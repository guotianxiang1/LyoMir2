# 等价核验报告 — 分片 26 (shard_26.tsv)

- 日期：2026-08-14
- 工作树：`D:\loym2\.claude\wt2\eqv-26`，分支 `w/eqv-26`
- 基线 HEAD：`00841828` (Merge branch 'w/trade-storage')
- 底本：`D:\loym2\staging\_reunpack_work\flat_image.bin` (ImageBase=0x400000, 17,661,952 B)
- 工具：capstone 5.0.7；映射 file_offset = VA − 0x400000（用 0x6573A0/0x657140 自检通过）
- 子系统：SPAWN/DROP（3 行）、SERVER-GROUP / CROSS-SERVER MESSAGING（23 行）

## 结论摘要

- 26 行的 **native 二进制证据全部独立复核通过**（地址/字节/反汇编逐条对照）。
- 无 FAIL-CLOSED、无 BLOCKED；无新增 MISSING（旧 MISSING 的 SGRP-08 在当前 master 已实现）。
- 关键背景：前序移植波（Grobal2.cs 中可见 SGRP-25/26/31/44 注记）已把 ISM_* 常量**重编号到 native 202..257**，并逐个标注 REAL/SINK。本次将该 C# 标注与我从 `0x657160` 提取的 56 字节 native 索引表逐一比对，**56/56 全部吻合**（REAL=27、SINK=29）。故分片 TSV 里多条 DIVERGENT/MISSING/UNKNOWN 状态与行号为旧快照。
- C# `Snaps`（`SnapsmService` socket 星型拓扑 + `MirrorMessage` 的 SS_/ISM_ 扩展 handler）与 native「经 DBServer 连接中继的 OthGs（category 0x6F，仅接收）」是**架构性热点**——按铁律只报不改。

### 状态计数（本次判定，针对每行契约的 native 证据 + 当前 master C#）

| 判定 | 数量 | 行 |
|---|---|---|
| FAITHFUL（证据已独立复核；C# 匹配或分歧已被正确记录）| 26 | SPWN-58/59/60，SGRP-01..23 |
| FAIL-CLOSED | 0 | — |
| MISSING | 0 | —（SGRP-08 旧快照为 MISSING，当前已实现）|
| BLOCKED | 0 | — |

> 说明：这里的 FAITHFUL 指「契约所述 native 行为经底本反汇编独立坐实」。其中 SGRP-01/05/07/09/20/23 的 **C# 实现**相对 native 存在架构性分歧（热点），已在下表逐条标注并给出台账建议，不做代码改动。

---

## SPAWN / DROP

### SPWN-58 — 掉落条目 +0x18 是指针，传入 sub_751BBC 的 edx — **FAITHFUL（可证部分坐实）**
- 0x71ED09 `mov eax,[ebp-0x14]`；**0x71ED0C `mov eax,[eax+0x18]`**；`test eax,eax`/`je 0x71ED28`（指针判空）；`mov edx,eax`；`mov eax,[0x7D5D6C]`/`mov eax,[eax]`；`xor ecx,ecx`；**0x71ED1E `call 0x751BBC`**。
- 另一处 0x71FD53 `mov eax,[ebp-0x1c]`；**0x71FD56 `mov edi,[eax+0x18]`**；`test edi,edi`/`je`；**0x71FD5D `cmp word[edi],0`**/`jne 0x71FD7E`（物品分支），否则累加 [entry+0x1c]。
- 结论：+0x18 确为指针，被判空并作 edx 传入 sub_751BBC；其首 word 参与「金币/物品」判别。契约「未坐实 +0x18 指向何物、不下结论」的谨慎态度恰当。csharp_ref 为空，无 C# 可改。

### SPWN-59 — 循环1以 `cmp word[eax],0` 区分金币/物品 — **FAITHFUL**
- 0x71FB62 `je 0x71FB92`；0x71FB64 `mov eax,[ebp-0x20]`/`mov eax,[eax+0x18]`；**0x71FB6A 字节 `66 83 38 00` = `cmp word ptr[eax],0`**；**0x71FB6E `75 22` = `jne 0x71FB92`**（word≠0→物品/跳过）；word==0 落入 0x71FB70+ 金币累加（[entry+0x1c]，`call 0x403B4C`/`sar`/`add`）。
- 与契约完全一致：word==0 走金币，jne 走物品。

### SPWN-60 — 多个 dump 窗口起点落在指令中段产出垃圾 — **FAITHFUL（方法论坐实）**
- 抽验：0x56577F=`bound edx,[esi]`（其后紧接 `64 ff 30 push fs:[eax]`，证明处于 SEH 中段）；0x682C40=`idiv byte[edi+ebp+0x46]`；0x73F0C3=`call 0x475C6653`（荒谬目标）。三处均为对齐错误产物。
- 契约「这些窗口前几行不作证据、仅末尾带 <<< 的 `call dword[reg+0xA8]` 及可自洽数行可用」成立。

---

## SERVER-GROUP / CROSS-SERVER MESSAGING

> 总背景（SGRP-01/05）：native **没有独立 server-group socket/类**；跨服（OthGs = Other GameServer）消息经 **DBServer 连接**的 type-2 帧、category `0x6F` 到达 `ProcessOthGsMsg = sub_657110`。C# 则用独立 `SnapsmService`（MsgServer 星型 socket + 文本协议 `(Ident\sNum\Body)`）。**架构性热点，只报不改。**

### SGRP-01 — native 无 server-group 类 / 无 SS_* 符号空间 — **FAITHFUL（强佐证；全量 census 未复跑）**
- 串扫描：`TFrmSrvMsg`、`TCenterServer`、`ServerGroup`、`TInterServerMsg` 四个典型类名**全部零命中**。
- native 真实机制经 SGRP-05 坐实为 `ProcessOthGsMsg`（error 串 `[Error]: ProcessOthGsMsg Ident=`@0x65745C 存在）。
- 说明：1349 类 RTTI 全量 census 未独立复跑，但典型类名缺失 + OthGs-经-DBServer 机制坐实，充分佐证。C# 侧 SS_*/ISM_* 为 C# 命名，属热点。

### SGRP-02 — 0x33AABB77 是≥4 条链共享的进程间帧 magic，非 server-group 判别符 — **FAITHFUL（计数订正：26 而非 27）**
- 全量程（0x401000–0x14D0000）扫描 `77 BB AA 33` 得 **26** 处；四链区域全部命中：GameGate `0x5F5xxx–0x5F7xxx`(8)、YB `0x637/0x63A`(3)、Lucky `0x69Cxxx`(2)、DBServer `0x711/0x713`(4)，其余散见 0x654A0E/0x68885A/0x6B6605/0x6BEF58/0x6D7xxx/0x6DCEB9/0x79D40F/0x79E7B2。
- 「共享 magic、≥4 链、非子系统判别符」实质结论成立。**台账建议**：immediate 站点数改 26（契约 27 为 off-by-one）。

### SGRP-03 — 16+12=0x1C 布局只属 GameGate 帧 — **FAITHFUL**
- sub_6D7BF8：0x6D7C3A `mov edx,0x1C`+`call 0x403B2C`(FillChar 清零 0x1C)；`mov [ebp-0x24],0x33AABB77`(magic@+0)；dw@+4、dw@+8；**0x6D7C5D `mov word[+0xC],0x0E`**；**0x6D7C63 `mov word[+0xE],di`(len)**；TDefaultMessage 自 +0x10。header=0x10、体 0x0C → 0x1C。

### SGRP-04 — DB link header 是 0x0C（非 0x1C）— **FAITHFUL**
- Writer sub_713094：`mov ebx,0x18`/`call 0x402FA0`(GetMem)；**0x7130B3 `mov [eax],0x33AABB77`**；**0x7130B9 `mov word[eax+4],2`**；**0x7130BF `mov dword[eax+8],0xC`**；体自 +0xC。
- Reader sub_713408：**0x71346F `lea eax,[esi+0xC]`**；**0x713475 `mov eax,[edi+8]`**(len)；0x713487 `cmp word[edi+4],2`(type@+4)。header=0x0C，magic@+0/type-word@+4/len-dword@+8/body@+0xC。逐链 header 尺寸不同，单一共享 header 模型错误。

### SGRP-05 — OthGs 接收路径 = sub_657110，经 DBServer 连接，非专用 socket — **FAITHFUL（binary 坐实；C# 架构分歧=热点）**
- error 串 0x65745C `[Error]: ProcessOthGsMsg Ident=`（len=31@0x657458，前 `FF FF FF FF` 为常量串），0x6573B2 `mov edx,0x65745C` 引用。
- **唯一调用者**：`scan_branch(0x657110)` 只返回 **0x712F56 `call 0x657110`**，位于 DB-link router sub_712EC8（其自身由 0x713EF0 调用）。
- C#：`MirrorMessage`/`SnapsmService` 走独立 MsgServer 星型拓扑 → DIVERGENT 热点。

### SGRP-06 — ident 空间恰为 202..257 = 56，56 字节索引表 + 跳转表 — **FAITHFUL**
- 0x657140 `movzx edx,word[ebp-2]`/`add edx,-202`/`cmp edx,0x37`(55)/`ja 0x6573A0`(default)/`mov dl,[edx+0x657160]`/`jmp [edx*4+0x657198]`。0..55 → 202..257 共 56。

### SGRP-07 — 56 中 27 有真 handler、29 落 default sink — **FAITHFUL（且 C# 已完全对齐）**
- 索引表 0x657160（56B）：数得 **非零 27 个、零 29 个**；跳转表 0x657198[0]=0x6573A0=default sink，[1..27] 为 27 个真 handler。
- **C# Grobal2.cs 的 ISM_* REAL/SINK 标注与该 native 表 56/56 全吻合**（如 204/205/206/208/215/223/225/229–239/242/244–246/248/250/252–256=SINK；其余=REAL）。
- **台账建议**：旧 DIVERGENT 状态过期，C# 侧 ident 表已对齐，建议改 FAITHFUL；「C# 为 native sink 实现 20+ handler」是**当前有意的扩展**（Grobal2 注为 "C# extension handler"），属热点而非缺陷。

### SGRP-08 — default sink 不静默：先 LOG 再忽略 — **FAITHFUL（旧 MISSING 已实现）**
- 0x6573A0 `lea edx,[ebp-0xC]`/`movzx eax,word[ebp-2]`/`call 0x40C89C`(IntToStr)/`mov edx,0x65745C`/`call 0x40581C`(concat)/`mov eax,[0x7D5ECC]`/`call 0x79DF74`(MainOutMessage)。即输出 `"[Error]: ProcessOthGsMsg Ident=" + IntToStr(ident)`。
- C# `MirrorMessage.ProcessData` default 分支（约 165–169 行）已 `M2Share.MainOutMessage("[Error]: ProcessOthGsMsg Ident=" + Ident);`，与 native 一致。
- **台账建议**：MISSING → FAITHFUL。

### SGRP-09 — ident 204 落 default sink，非活的城堡占领接收 handler — **FAITHFUL（binary 坐实；csharp_ref 已过期）**
- 索引表[204−202=2]=0x00 → 0x6573A0 default sink，坐实。
- csharp_ref `UserCastle.cs:425` 已失效（当前 425 行是 Run() 里 `m_btWineCount=0x14` 注释，与城堡占领无关）。
- ident 重编号后：C# `ISM_SYSOPMSG=204`（注为 SINK/扩展 handler），`ISM_CHANGECASTLEOWNER=211`（native idx6=REAL）。即城堡占领已迁到 211（native 有真 handler），204 现由 C# 作扩展处理。
- **台账建议**：更新 csharp_ref 与描述（204=SYSOPMSG 扩展；城堡占领=211 REAL）。C# 仍处理 204 属热点扩展。

### SGRP-10 — ident 212 有真 handler（前提成立）— **FAITHFUL（抽查）**
- 索引表[212−202=10]=0x07 → 跳转表[7]=0x65726D：`mov ecx,[ebp+8]`/`mov edx,[ebp+0xC]`/**`call 0x6577B0`**/`jmp 0x6573CD`。C# `ISM_RELOADCASTLEINFO=212` 有 handler。

### SGRP-11 — 0x12E 在代码段不作为立即数存在 — **FAITHFUL**
- 原始字节 `2E 01 00 00` 在代码段有 23 处，但抽验均为相对分支位移（0x712F10=`jmp 0x713042` 的 rel32；0x5F3EA8=`je 0x5F3FDA` 的 rel32），**非指令立即数操作数**。指令感知的 scan_imm=0，成立。且 ident 空间 202..257 排除 302(0x12E)，「该 ident 的安全隐患不存在」成立。

### SGRP-12 — 0xF0A8 gate 在 0x6547BD（sub_654748 内，type-1 玩家数据通道），非 server-group 路径 — **FAITHFUL**
- 0x6547BD `cmp dword[ebp+8],0xF0A8`/`jle 0x6547D6`/`lea edx,[eax+0xF0A8]`/`call 0x6E448C`。sub_654748 起于 0x654748（含 0x6547BD）；type-1 router=sub_654140（由 SGRP-17 的 type-1 分支 `call 0x654140` 坐实）。两通道被混淆的判断成立。

### SGRP-13 — DB 帧仅声明体长，完整性 gate 保证 nLen 有真实字节 — **FAITHFUL**
- sub_713408：0x713467 `cmp dword[edi],0x33AABB77`；**0x71346F `lea eax,[esi+0xC]`**；**0x713475 `mov eax,[edi+8]`**；`add eax,[ebp-8]`；**0x71347B `cmp eax,[ebx+0x44]`**/**0x71347E `jg 0x7134D4`**（不完整则延迟）。

### SGRP-14 — 接收缓冲 0x20000，越界 clamp 并 LOG — **FAITHFUL**
- 0x713422 `cmp edx,0x20000`/`jle 0x713444`；越界：`mov esi,0x20000`/`sub esi,eax`(=0x20000−[ebx+0x44])；`mov edx,0x71351C`/`call 0x79DF74`。串 0x71351C=`[Error]: 收到DB超长数据`(GBK,len 0x17)。

### SGRP-15 — magic 逐字节 resync 并 LOG 不完整包 — **FAITHFUL**
- 0x71346D `jne 0x7134C5`→0x7134C5 `mov byte[ebp-1],1`/**0x7134C9 `inc esi`**（逐字节）；循环后 0x7134D4 `cmp byte[ebp-1],0`/`je`/否则 `mov edx,0x71353C`/`call 0x79DF74`。串 0x71353C=`[Error]: 收到DB不完整包`。

### SGRP-16 — word[body+6] 从未被任何 builder 写入，属未初始化填充 — **FAITHFUL**
- sub_713094 经 `call 0x402FA0`(GetMem，无清零) 分配 0x18；仅写 +0(magic)、+4(type word)、+8(len dword)，header 区 **+6..+7 从未写**。对比 GameGate sub_6D7BF8 有 FillChar 清零，故 DB 链此字段为脏。移植中勿断言其值。

### SGRP-17 — type-1 每 tick 限 5 帧；type-2(OthGs)/type-3 不计数，抽干为止 — **FAITHFUL**
- sub_713DDC drain：type-1(0x713E80)→`call 0x654140`后 **0x713EBD `inc edi`**；type-2(0x713EC0)→`call 0x712EC8`(无 inc)；type-3(0x713EF7)→`call 0x713A98`(无 inc)；尾部 **0x713F4C `cmp di,5`/`jae 0x713F60`** 退出。仅 type-1 计数。
- 交叉印证：type-2=OthGs 经 sub_712EC8（SGRP-05）、type-1 router=sub_654140（SGRP-12）。

### SGRP-18 — 链路 down 时把帧入内存队列而非丢弃，该路径未见上限 — **FAITHFUL**
- sub_713CBC：**0x713CCD `cmp byte[obj+0x4C],0`**/`je 0x713CE9`。链路 up：`push 1`/`mov eax,[obj+0x38]`/`call 0x4C93F8`（立即发）。链路 down：`mov eax,0x10`/`call 0x402FA0`（分配 0x10 节点）/写 node+0=data、+4=len/`inc dword[obj+0xE8]`。分配前无容量检查。

### SGRP-19 — 同一 10000ms 定时器保活并重拨链路 — **FAITHFUL（核心坐实；措辞订正）**
- sub_713AAC：`mov bl,byte[esi+0x4C]`/`test bl,bl`/`jne`（up 即返回）；down：`call 0x408340`(GetTickCount)/`sub eax,[esi+0x60]`/**`cmp eax,0x2710`**/`jb`；≥10s：**`call 0x4C97D8`**(重连)/`call 0x408340`/`mov [esi+0x60],eax`(restamp)。
- **台账建议**：restamp 实际在 sub_713AAC 内 0x713AD5，契约「tail 0x713F60 restamps」措辞不精确（0x713F60 是 sub_713DDC 尾）。核心重拨逻辑成立。

### SGRP-20 — 不做 OthGs 扇出：只把一个帧入队到一个 DB link — **FAITHFUL（binary 坐实；C# 分歧=热点）**
- sub_713CBC 追加到 [obj+0xE0](head)/[obj+0xE4](tail)、计数 [obj+0xE8]、锁 [obj+0xC8]（0x408210 enter / 0x4083A8 leave）。单链表单对象，无扇出。
- C# `SnapsmService`（约 121 行 `SendServerSocket`）遍历 m_SrvArray 向所有节点发送 → DIVERGENT 热点。多服扇出在 native 由对端(DBServer/peer)完成。

### SGRP-21 — GameGate 真实扇出中，无效/缺失 peer 被跳过且计数继续前进 — **FAITHFUL（抽查）**
- sub_5F7778 三个循环遍历 [esi+ebx*4+0x30C]（ebx=1..0x20=32 槽）：null `test eax,eax`/`je` 跳过；range `shr eax,0x11`/`cmp al,1`/`jb`、`cmp al,0x20`/`ja` 跳过。
- 注：csharp_ref `SnapsmService.cs:121` 指的是节点服务(node-service)，而 sub_5F7778 是**客户端网关(GameGate)扇出**，二者子系统不同；「skip null/断开」语义在 C# `SnapsmService` 确有 `continue`，但**这是不同子系统的错配引用**。C# GameGate 扇出应另在网关管理器。

### SGRP-22 — 失败 peer 会中止整个扇出（循环内外无 try/except），仅外层 TGateManager.Run 捕获 — **FAITHFUL**
- sub_5F7778 全体 **0 处 fs:[]**；sub_5F6C24（0x5F6C24–0x5F6D89 `ret 4`）内 **0 处 fs:[]**（先前 0x5F6DE6+ 的 fs 属后续函数 sub_5F6DCC）；sub_5F7554（至 0x5F75AE 首 ret）**0 处 fs:[]**。外层函数 0x5F79EC–0x5F7A24 有 SEH 帧（`fs:[]` + `jmp 0x404E38` Delphi 异常分发），即外层捕获。「skip-on-invalid」与「abort-on-exception」是两种不同保证，须同时复现。

### SGRP-23 — OthGs 通道在 M2Server 只收不发：category 0x6F 从不被写出 — **FAITHFUL（airtight；C# 分歧=热点）**
- 接收：sub_712EC8 分派 `sub edx,0x69`(105)/`sub edx,6`(→111=**0x6F**)→0x712F3F→0x712F48 `mov dx,word[ebx+2]`(ident)→**`call 0x657110`**(ProcessOthGsMsg)。故 category 0x6F = OthGs。
- 发送：type-2 builder sub_713094 的 **6 个调用者**传 category=`0x3E/0x3C/0x42/0x42/0x180/0x185`(60/62/66/384/389)——**无一为 0x6F**。故 M2Server 从不发送 OthGs 帧。
- C#：`MirrorMessage`/`SendServerGroupMsg` 会发送若干 ident（如 SS_204/205/207/208/211/212），native 无对应发送者 → DIVERGENT 热点。

---

## 台账修订建议汇总（不改代码，仅建议）

1. SGRP-02：immediate 站点数 27 → **26**（全量程实测）。
2. SGRP-07：csharp_status DIVERGENT → **FAITHFUL**（C# ISM_* REAL/SINK 已与 native 索引表 56/56 对齐）。
3. SGRP-08：csharp_status MISSING → **FAITHFUL**（default sink 日志已实现）。
4. SGRP-09：csharp_ref `UserCastle.cs:425` 已过期；更新为「204=ISM_SYSOPMSG（SINK/扩展），城堡占领=211 REAL」。
5. SGRP-19：证据措辞「tail 0x713F60 restamps」不精确；restamp 在 sub_713AAC 内 0x713AD5。
6. SGRP-21/22：csharp_ref `SnapsmService.cs:121` 为**子系统错配**——sub_5F7778 是 GameGate(客户端网关)扇出，非节点服务；建议改指网关管理器代码。

## 热点（大缺口，只报不改）

C# `GameSvr/Snaps` 子系统（`SnapsmService` socket 星型拓扑 + `MirrorMessage` 的 SS_/ISM_ 扩展 handler + `SendServerGroupMsg` 发送路径）与 native「无独立 server-group socket、OthGs 经 DBServer 连接 type-2/category-0x6F 仅接收、default sink 记录并丢弃、不扇出」的模型系统性不同（SGRP-01/05/07/09/20/23）。该差异跨越传输层、ident-handler 映射、收发方向与扇出语义，属架构级重写，非单点可修，按铁律只报不改。

## 未做代码改动说明

本分片无「小的可证单点缺口」可外科修复：唯一旧 MISSING（SGRP-08）在当前 master 已实现；其余 C# 分歧均为架构性热点。故本次仅提交本核验报告，未改任何 C# 源码（无需 dotnet build）。
