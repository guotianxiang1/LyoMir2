# 复刻规则（子代理必读）

本项目把 **战神引擎 M2Server**（Delphi x86-32，无源码）和 **眼神插件**（MFC C++，VMProtect 加壳）
一比一等价复刻为 C#。目标不是"功能差不多"，而是**逐字节等价**：伤害公式的取整方式、
状态判定是 `>` 还是 `>=`、随机数的调用次数和顺序、卖价该不该乘堆叠数——每一条都要和原版一致，
且每一条都必须有反汇编字节作为证据。

开工前把这份文档读完。里面每一条都是真实事故换来的，不是纸面规范。

---

## 一、三条铁律

### 1. 不许并行编译

用户机器在并行 `dotnet build` 时会死机。这不是性能建议，是硬约束。

- **只读分析代理：一律不许执行任何编译命令**（`dotnet build` / `dotnet run` / `msbuild`）。
- 需要编译验证的，把补丁方案写清楚交给主控串行落地。
- 只读扫描、反汇编、grep 可以自由并行。
- 同理，IDA（idat）也必须串行，一次只能开一个。

**编译锁协议（确实需要跑审计的代理才适用）**：

```powershell
while (-not (New-Item -ItemType Directory "D:\loym2\staging\_buildlock" -ErrorAction SilentlyContinue)) { Start-Sleep 20 }
# 抢到锁后，批量跑完一批再释放。不要一个工具抢一次锁。
Remove-Item "D:\loym2\staging\_buildlock" -Recurse -Force
```

锁目录 `LastWriteTime` 超 15 分钟视为残留，可删掉重抢。每次持锁不要超过 10 分钟。

**2026-08-13 实际事故**：某审计代理把同一个 sweep 脚本**并发起了 5 份**，每份各自派生
`dotnet build`，锁形同虚设——机器 CPU 打满、20 个 dotnet 进程、15 个 MSBuild 常驻
worker 堆积占掉 4 GB 内存。由此追加三条：

1. **一个代理同一时刻只能有一个编译进程。** 写 sweep 脚本时要自查是不是已经有一份在跑
   （检查自己的 pid 文件或进程列表），不要靠"我这次跑得快"来赌。
2. **抢锁要覆盖整个批次**，不是覆盖单个 `dotnet run`。脚本从头到尾持一次锁。
3. 环境已全局设置 `MSBUILDDISABLENODEREUSE=1` 和 `MSBUILDNODECOUNT=2`，
   **不要在自己的命令里覆盖它们**。node reuse 打开时，每次构建都会留下常驻 worker，
   几十次构建后就把内存吃光。

### 2. 不许捏造

证据不足就写 `BLOCKED` 并说明缺什么。**宁可少一条结论，也不要给一条错的。**

绝对不许编造：函数地址、字段偏移、常量值、比较方向、状态编号、opcode、概率分母、
`Random` 参数、配置键名、默认值。

推测必须显式标注为推测并给出置信度。**不许把推测写成结论**。

### 3. 原版有的照抄，原版没有的移除

- native 有 → 补齐（`MISSING`）
- native 有但 C# 行为不同 → 改对（`DIVERGENT`）
- **native 没有但 C# 有 → 标 `INVENTED`，按规矩应当移除或屏蔽**

判定 `INVENTED` 前必须做**全镜像多编码字节扫描**（GBK + 裸 ASCII + UTF-16LE，
纯 ASCII 名走大小写不敏感），三者皆 0 命中才成立，并在报告里附上 0 命中证据。

#### 3.1 原版的缺陷也要照抄（2026-08-13 用户裁决）

**「等价」的判据是行为与原版一致，不是行为正确。** 遇到原版明显是 bug 的地方，
照抄，不要"顺手修好"。

这条是被问过之后明确裁决的，不是默认推定。此前累计出的分歧一律按它处理：

- 眼神回收的经验单价**跨物品泄漏**（循环内不重置，逐件复利）——照抄。
- 眼神回收**先删后结算、无回滚**；货币总额一次落账，金币顶到 `m_nGoldMax`
  时 `IncGold` 整笔拒绝，而物品已经删光 ⇒ **整批只删不给**——照抄。
- 判「产出是否为 0」用的是**缩放前**的单价，所以 `单价=1 & 倍率=50` 会过门却入账 0
  ——照抄。
- 摊位事件析构不还地图格子（不走 Run 就释放会把格子永久堵死）——照抄。
- 1034 制造失败不退材料、不回滚——照抄。
- 装备与背包两个爆装 worker 的红名判据在原生就不一致（`jge` 是 `> 200`，
  `setle` 是 `>= 200`）——**不许统一**。

**同时意味着**：C# 里那些"原版没有、但删掉玩家会当场吃亏"的保护性代码，
（新人 PK 保护段、不死戒指早退、回收的背包模型守卫等）**属于 INVENTED，要删**。
删之前照例做多编码零命中扫描并留证。

**唯一的例外是会破坏既有存档数据的翻转**（例如英雄存档两个名字槽与原生互换）：
那类改动本身是对的，但必须配一次性迁移，不能盲翻——盲翻会把已经错位写进库的
数据再翻一次。这类项标 `DIVERGENT(needs-migration)`，单独排期。

照抄缺陷时**必须在注释里写清这是原生缺陷**及其 VA，否则下一个人会再把它"修好"一次。

---

### 1.4 所有协议必须逐字节等于原版

**这不是偏好，是硬约束，而且它决定了整个项目能不能安全落地。**

终局是**服务端全部换成 C#**（GameSvr / DBSvr / LoginGate / GameGate 都换）。当前是
过渡态：只有 GameSvr 是 C#，其余仍是原版 Delphi。

正因为终局是全换，协议才更不能动。理由是**可回退性**：只要每一层协议都逐字节等于原版，
就能一个组件一个组件地替换，哪个 C# 组件出问题就换回对应的 Delphi 组件，不必整体回滚。
协议一旦偏离，这张安全网立刻消失，变成全有或全无。

**要守的协议边界（全部）**：

| 边界 | 说明 |
|---|---|
| 客户端 ↔ GameGate | 客户端永远是原版，`CM_*` / `SM_*` 不可动 |
| GameGate ↔ GameSvr | 迁移期要能单独替换任一端 |
| GameSvr ↔ DBServer | type1 (`0x01xx`) / type2 (`0x00xx`) 命令族 |
| LoginGate ↔ LoginSvr | 登录链路 |
| 跨服 / 服务器组 | SGRP ident 族 |
| 存档记录布局 | 见下，这条最容易被忽略 |

**具体要求**：

- 上线的编号、字段偏移、字段宽度、字节序、参数顺序、GBK 字符串的长度前缀约定，
  全部必须与原版一致。**任何情况下都不许为了实现某个功能去改协议。**
- **收包侧的校验也是协议的一部分。** 原版做了掩码或范围检查而 C# 没做，就是可被伪造包
  利用的漏洞。已修两例：`CM_RUN` 方向缺 `& 7`（MOVE-19）、攻击族九个 ident 方向缺 `& 7`
  （原生 `0x6D9EF4` / `0x6D9F90` 都是 `and al,7`）。**这类要主动去找，不要等报。**
- **存档/数据库记录布局同样算协议。** 玩家存档 blob、道具记录、行会表的字段偏移必须与
  原版一致，否则换 DBSvr 时老数据读不出来、或者两版并存时互相写坏。已知教训：某处把
  存档的 type0/type1 与 V/S 两个变量银行接反了，单独改代码会把已经错位存下的数据再翻
  一次，比不改更糟——这类改动必须配套数据迁移方案。
- `RM_*` 是服务端**进程内**的消息队列标签，不上线，数值不影响互通。发现命名或取值问题
  记录即可，不要当协议缺陷处理。（已核实：`RM_PHYSICAL_ATT` 只是内部转发标签，真正下发
  的是 `SM_PHYSICAL_ATT = 1230`，而 1230 在原生镜像有 6 个发出点，是真编号。）
- 复刻第三方插件（如眼神）的功能时，**不许为它新增或改动 M2 的协议编号**。插件本身也没
  这么做——它复用原生编号、往原生 body 里夹带扩展字段、并给脚本开"任意 ident 隧道"。

## 二、证据标准

一条结论要能落地，必须给出：

1. **VA 地址**（虚拟地址，ImageBase `0x400000`）
2. **原始字节**（例如 `0x76B398 84 D8 test al,bl`）
3. C# 对应位置（`文件:行`）
4. 两者差异，以及**玩家可见后果**

参考范例：`staging/magic_dispatch_map_FINAL_20260813.md`——每个结论都有 VA 和字节。

**台账（`staging/equivalence_ledger_20260810.tsv`）的 evidence 列不可直接采信。**
台账作者自己声明过"这是图书管理员的索引，不是新的逆向"。实测约 8 处子系统里每处都有
数条 evidence 抄错（地址偏移、指令认错、契约文字与字节矛盾）。**每条都要自己复核。**

---

## 三、判定词汇（统一使用）

| 判定 | 含义 |
|---|---|
| `FAITHFUL` | C# 行为与原版一致 |
| `DIVERGENT` | C# 有实现但行为不同（**必须写清差在哪一行、差什么**） |
| `MISSING` | 原版有、C# 完全没有 |
| `INVENTED` | C# 有、原版没有（附 0 命中扫描证据） |
| `NOT_FOUND` | 找不到 C# 对应位置 |
| `BLOCKED` | 证据不足以判定（**必须说明缺什么、卡在哪一层**） |

死代码算 `MISSING`：代码写对了但零调用者，运行期不可达，等于没有。

---

## 四、已知的坑（全部是本项目真实事故）

### 4.1 注释和提交信息会撒谎

- 某注释把处理器地址 `0x6EDEC1` 归属给魔法 id 149，逐字节读跳表证明属于 **id 117**；
  `0x6EDF01` 同理，属于 118 不属于 150。
- 某提交标题写"TRADE-42 交易绑定检查"，实际 diff 全是**地图标记解析**，与交易无关。
- 某提交标题写"Add SM_MINESUCCESS constant"，整个 diff **没有一行** `Grobal2.cs`。
- 某提交声称把封包放在扣耐久**之后**，实际 diff 放在了**之前**。
- 某注释声称"命令名字符串零 xref，原版不存在此命令"——实际它是 430 条连续记录的注册表第 611 条，
  **那张表靠 `ebx += 0x120` 遍历，任何单条记录本来就不会有独立 xref**。这个推理对全部 430 条无效。

**结论：一切以字节为准。看到注释里的 VA，去核对它是否真的在说那件事。**

### 4.2 同一修复可能被应用→回退→重做

MINE-08 的历史是 `caa8bd7` 应用 → `54b9a29` 回退 → `7c9c62d` 重做。
SPAWN-16 因引用不存在的常量被回退。

**看到相关代码时要判断"当前是不是正确的那一版"，不要以为有代码就等于对。**

### 4.3 提交会夹带无关改动

一个自称"魔法处理器"的提交同时删除了挖矿配置字段；一个自称"CRAFT-08 两行字典赋值"的提交
夹带了 52 个文件近 4000 行。**只接受与任务相关的部分，拒绝夹带并在报告里说明拒绝了什么。**

### 4.4 旧提交会用已重命名的 API

历史重命名：`HasNativeState` → `HasNativeActiveState`、
`Grobal2.TMsgColor.c_Red` → `MsgColor.Red`、`Grobal2.TMsgType.t_Hint` → `MsgType.Hint`。

cherry-pick 旧提交时遇到这类冲突**保留 HEAD 的新命名**，否则编译失败。

### 4.5 有过凭空发明的先例

已确认被移除或待移除的：地图标记 `NOHUMNOMON`（全镜像 0 命中）、`MINE2` 宝石产线、
背包满时把物品掉地上（原版是 `FreeAndNil` 直接释放）、魔法 291-320 波段的硬拒绝、
矿点事件类型 55/56/57、GM 命令 `@AddGold`（原生注册表无此项）、
眼神脚本 API 的 95 个英文别名。

### 4.6 伪造的地址引用

某注释引用 `0x6D7934` 当作"背包容量门"，实际那是 `IncGold` 的金币上限 `jg`
（`cmp ebx,[eax+0x68C]`，金币字段 `[eax+0x15C]`），与背包毫无关系。

**核对每个被引用的 VA 是否真在说那件事。**

**已确认被污染的既有产物（不要引用，引用前先自己复核）**：

- `hit_rate_formula_report` 把 `0x4054B8` 标成 `@RandInt`。那是个 8 字节的分配器，
  全镜像 **0 个 `E8` 调用者**——一个从没被调用过的地址不可能是随机数入口。
  真正的命中判定在 `0x666CB2`（`movzx eax,[esi+0x266]` 目标敏捷 → `call 0x403B4C`
  → `cmp` 攻方准确 `[edi+0x264]`），技能包装在 `0x772578`。
  `@RandInt` 只有一个：`sub_403B4C`。
- 仓库里有若干注释把 `[obj+0x73]` 写成 `m_boDeath`。**那是 ghost**
  （唯一写入点 `0x7680EF C6 43 73 01`，在 MarkDelete 里，且从不写 0）；
  死亡是 `[obj+0x74]`（`0x766323 C6 43 74 01`，复活会清零）。
  两者混用会让尸体停止入队——死亡动画、爆装、制造回包全部消失。

### 4.7 比较方向极易弄反

原版大量使用 `jb/jbe/ja/jae`（无符号）和 `jl/jle/jg/jge`（有符号）。

真实案例：
- `jb` 表示"elapsed >= 阈值才触发"，C# 写成严格 `>` 会漏掉边界那一 tick（POIS-05）。
- `(旧-新-1) < 0 则跳过` ⟺ 发包条件是 `旧 > 新`，C# 写成 `!=` 会在数值回升时多发包（MINE-44）。
- `jne` 绕过时限比较意味着判定式是 `A OR B`，C# 写成 `A AND NOT B` 方向完全相反（STATE-21）。

### 4.8 默认值必须找初始化指令

不要假设默认是 0 或 false。真实案例：某 map flag 的 C# 默认值一度被怀疑写反，
查到构造函数 `0x774A5C mov byte [esi+0xB0], 1` 才确认**原版默认就是 1**，C# 是对的。

Delphi 构造函数有明显特征（分配 TStringList、连续 `mov [esi+off], imm`）。

### 4.9 除法顺序与取整助手

Delphi 浮点 `/` 被直译成 C# 整数 `/` 是一类**系统性 bug**：
先除后乘会截断成 0 或 1，**先乘后除才忠实**。

取整助手要认清：
- `@ROUND = 0x403574`（半偶入 banker's rounding）
- `@TRUNC = 0x403580`（截断）
- 两者结果不同，混用会产生系统性偏差

常量可能是 **80 位扩展精度 tbyte**，用 double 重算可能双重取整。
但也别过度紧张——实测 0.05 / 0.8 / 半月的 /15 / 刺杀的 /5 这些链路，
扩展精度与 double 的差异都是 **0 处**。**要实测，不要假设。**

### 4.10 跳表几何要自己算

Delphi 把大范围 `case` 编译成多个 `sub eax,imm / cmp / ja / jmp [table+eax*4]` 簇。
**位索引常带偏移**。

真实案例：`0x773254` 是 `add dl,0xF8`（即 -8）后再 `bt`，C# 直接用 `internalType` 做索引，
**112 个类型里 41 个判错**。

不要凭表基址猜 id 范围，要跟着 `sub`/`add` 的累计偏移算。

### 4.11 结果是否被存回决定语义

魔法 id 118/128 调用恒假函数**且把结果存回** `[ebp-6]` → 硬拒绝。
id 231/236 也调恒假函数但**丢弃结果** → 仍然成功。

这个区别只能从 `mov [ebp-6],al` 是否存在看出来。**别只看被调函数返回什么。**

### 4.12 DEFAULT 汇聚点是成功路径

魔法分发器的 DEFAULT 汇聚点 `0x6EE04B`：`boSpellFail` 保持 0，发 `0x27E` 特效包，
`DoSpell` 返回 TRUE。**落到 DEFAULT 是静默成功，不是失败。**

曾有代码把整个 291-320 波段判成硬拒绝，理由是"默认汇聚点也拒绝"——完全反了。

### 4.13 编码：GBK 无处不在

引擎和插件的中文字符串都是 **GBK**，不是 UTF-8。

- 用 UTF-8 解会得到乱码，容易误判成"字符串不存在"。
- Delphi 长字符串带**长度前缀**：`(ptr-4)` 处的 dword 是字符数。
  这是判定字符串长度的决定性证据（例：某矿名前缀是 8，即四个汉字，而 C# 只写了三个字）。
- 眼神的字符串转储里中文乱码是**可逆的**：转储工具按 UTF-16LE 读了 GBK 内存，
  `mojibake.encode('utf-16-le').decode('gbk')` 即可还原。

### 4.14 PowerShell 控制台会吃掉中文

**不要依赖控制台看中文。** 把内容写进 UTF-8 文件，再用 Read 工具读。

提交信息用 `git commit -F <文件>` 从文件传入，不要用 `-m` 加引号（转义会把信息弄脏）。

内嵌引号的 python 单行命令在 PowerShell 里经常被解析坏，**写成 .py 文件再执行**。

### 4.15 加壳二进制

`yanshen2.0.8.dll` / `yanshen2.0.7.dll` / 某些 M2Server 变体是 **VMProtect** 加壳：
节表 `SizeOfRawData = 0`、存在 `.tvmp` 节、载荷节熵接近 8.0。**静态反汇编不可行。**

可用的替代证据源：运行时内存转储、插件自动生成的默认配置文件、作者随包的说明文档、
GUI 控件枚举、随包 Pascal 脚本（可能是重复 XOR 加密，可用已知明文破解）。

**注意核对转储的 `SizeOfImage` 与磁盘 DLL 是否一致**——不一致说明转储来自另一个构建，
结论不能直接套到当前版本。

### 4.16 patch-id 和 cherry-pick 试算都会误判

判断一个提交是否已落地：
- 按 SHA 比对**必错**（cherry-pick 会重写 SHA）
- 按 patch-id 比对会**漏判**（同一逻辑改动、不同上下文 → patch-id 不同）
- cherry-pick 试算能识别"应用后为空"，但**文本冲突时分不清**"内容已在但形式不同"和"真缺失"

**最可靠的是内容存在性检测**：提取该提交新增的特征标记（缺陷 ID、新方法名、新字段、
引用的 VA），看 master 是否已包含。实测 30 个"冲突"里 12 个其实早已落地。

### 4.17 审计工具本身可能是坏的

- 断言机制是 `throw new InvalidOperationException` 且无 catch，所以**断言失败和真崩溃
  共用退出码 `0xE0434352`**，无法从退出码区分。
- 有工具用 `BindingFlags.Instance` 反射查一个**静态**方法，`GetMethod` 返回 null、
  `?.Invoke` 静默跳过，**编译通过也是空跑**。
- 有工具断言的是**源码文本形状**（grep 式），实现改得更忠实反而会让它失败。
- 有工具的测试 fixture 格式就是错的（例：MapInfo.txt 的标记应在**右括号之后**，
  写在括号内或独立行都不会被解析）。

**绝对不许为了让工具通过而删除或弱化断言。** 断言守的是原版契约。
无法满足就让它 FAIL（有效信号），而不是 BUILD-ERROR 或崩溃（盲区）。

### 4.18 一个原生字段不要拆成两个 C# 权威

原版一个字节被 C# 用两处独立存储 = 双权威隐患，迟早打架。
状态子系统就有这个问题（新的 `TimedAbilityNode` 层和遗留 `ushort[12]` 数组并存，
而且**单位不同**：毫秒 vs 秒）。

发现这类情况要明确指出，落地时收敛成单一权威或转发属性。

### 4.19 读 / 写 / 持久化三条路径必须一起看

真实案例（QST-30）：原版 `SetV` 没有零值测试，原样存 0。
C# 在**写入端**和**加载端**各擦除了一次零值——只修写入端，下次登录会被加载端撤销。
后来又发现**存档编码器**里还有第三处。

**任何涉及数据存取的契约，三条路径都要核对语义是否一致。**

### 4.20 扫 ident 必须「从发送槽反查」，不能「按立即数形状正查」

按 `mov dx/cx, imm16` 的形状扫全镜像，会**漏掉换了寄存器或多绕一跳的下发点**。
真实事故：SM 34 的死亡分发臂走
`0x6B49D3 66 B8 22 00 mov ax,0x22` → `mov ecx,eax` → `call 0x6BCE54`，
形状扫描整条吞掉，而它在生产计数器里是 **18,255,061 次**——全镜像最热的包之一。

**正确做法**：先枚举全镜像所有走发送槽 / 发送包装器的调用点（本镜像 875 个），
再从每个调用点**向后**反解 ident。还要补两件事：

- 分支合流臂：`if/else` 选 ident 时，目标优先扫描只会解出直落臂，另一臂要单独补扫。
- 包装器自动发现：手工列的包装器名单一定不全（本镜像从 7 个扩到 13 个）。

三条路径（形状正查、目标反查、生产计数器）交叉收敛到同一答案，才算扫干净。

### 4.21 `VMT+0xE0` 既不是发送槽，也不能一概否定

两个方向都出过错，判据要精确：

- **它不是 `+0x250` 单播槽。** 每个 Delphi 类都有 `+0xE0` 这个低位 VMT 槽，VCL 窗体也有。
  SM 0 就是这么误报出来的：两个「下发点」的 VMT `0x795590` 解出来是 `TSearchLogForm`，
  `[VMT+0xE0]=0x493510` 是窗体方法，而 `[VMT+0x250]` 已经越过该 VMT 尾部。
  **只有 TBaseObject 家族的 `+0x250` 才是发送槽。**
- **但 `+0xE0` 的 ident 可能是真的。** 它经 `sub_7651EC → sub_5F7778 → sub_5F6C24`
  直达网关组播（`33 AA BB 77` 头、子命令 `0x13`），同槽 ident 全是已知 SM 号
  （1230、3413/3414/3415、638）。所以不能因为「不在 `+0x250`」就判臆造。

同理 `VMT+0xD8` 是入队式广播，真正的 wire ident 由接收臂再从 `+0x250` 发出
（例：261 的 `0x3042` → `0x6B6065` → `sub_774288` 距离/状态门 → `SM_DISAPPEAR`）。
**判据始终是：顺着调用链走到真正出网的那一跳，读那里的 ident。**

### 4.22 眼神的开关在内存里是随机数，不是 0/1

加载器读到 json 的 0/1 后立刻**重编码**：开 → `rand()%1000 + 1000`，关 → 小模数取余；
消费端一律 `cmp …, 0x1F4` 即 `> 500`。

- 看到 `0x1F4` **不要**当成「阈值 500」去建模，它只是编码区间的中点。
- 可观测契约就一句：**json 值 != 0**。C# 现有的 `Enabled()` 语义是对的。

配套两条：

- **一个键有两个落点**——页面对象 `this` 和那个 `0x19B8` 字节单例
  （`0x1031C0E0/C4BC/C510/C514/C5E0` 全指向同一个 `0x10319DA8`）。
  偏移会撞车：`邮件防刷` 是 `this+0x7A0`，`踢玩家下线` 是单例 `+0x7A0`。
  判定一个偏移属于谁，必须看基址是从哪来的。
- **补丁协议**是「配置门控 + 单例里的已打标记(=100) + 成对打/还原」。
  还原支写回的字节，就是关闭态的宿主原貌——这是读取「原生默认行为」最可靠的来源。

### 4.23 定位补丁目标要回到 `call 0x10033340` 的四个 push 现场

「从补丁点向后扫最近的 `cmp`」这个启发式**会把上一个特性的 host VA 带进来**，
本项目已因此误判过一次。补丁目标只能从打补丁调用的实参现场读。

同理，被改写的指令**本身就决定了取值域**：数量类补丁写的是 `al`、imm8，
所以上钳 127、负数按 8 位回绕；名称类补丁不含 Delphi 长度前缀，
所以神兽名恒为两个汉字。**不要在 C# 侧放宽这些边界**。
---

## 五、环境速查

| 项 | 值 |
|---|---|
| Git 仓库根 | `D:/loym2/LyoMir2-master` |
| master 检出位置 | `D:/loym2/.claude/worktrees/pois11-fix`（**只读，不要 commit 到这里**） |
| 原版 M2Server 镜像 | `D:/loym2/staging/_reunpack_work/flat_image.bin`（16.8 MB 平坦镜像） |
| ImageBase | `0x400000`（偏移 == RVA，VA = 0x400000 + 偏移） |
| Python（含 capstone 5.0.7） | `C:\Users\Administrator\AppData\Local\hermes\hermes-agent\venv\Scripts\python.exe` |
| 等价性台账 | `staging/equivalence_ledger_20260810.tsv`（**证据需自行复核**） |
| 审计工具 | `AuditTools/*/`（约 400 个，每个钉一条契约） |
| 审计基线 | `staging/_master_audit_baseline_1433583.json` |
| 眼神主插件 | `D:/loym2/眼神2.08/Gs1/ys/yanshen2.0.8.dll` |
| 眼神运行时转储 | `D:/loym2/YanshenDump/`（只有字符串，无模块镜像） |
| 眼神生产部署 | `D:/光头卧龙/mud2.0/Mir200`（真实线上数据：31 个大背包 bin、生产配置、20+ 生产脚本） |

### 5.1 眼神插件的既定事实（不要重复考证）

- **壳是 Themida / WinLicense，不是 VMProtect。** 已定案：节名是 `.tvmp`（VMProtect 用
  `.vmp0`/`.vmp1`）。曾有两份报告给出互相矛盾的壳名，以此条为准。
- **磁盘上的 DLL 确实啃不动**：`.text` / `.rdata` / `.data` 的 `SizeOfRawData` 全是 0，
  唯一载荷是 21.67 MB、熵 7.968 的 `.w(d` 节，入口点也在里面。**不要去啃磁盘那份。**

### 5.1.0 【最重要】眼神已有脱壳内存转储，可以做「实现复刻」而不止「接口复刻」

**本条推翻了本文件此前「运行时脱壳也不可行、不要再派任务去啃这个壳」的结论。**
那句话写于不知道下面这个产物存在的时候；产物 2026-07-19 就躺在 staging 里，
导致此后所有眼神任务都在绕开唯一的一手证据源。

```
D:\loym2\staging\yanshen208_strparam_runtime_dump_20260719\yanshen2_0_8_dll.memory.bin
  45,821,952 字节   整体熵 5.470（磁盘那份是 7.990）
  真实加载基址 = 0x10000000        ← 不是 PE 头里的 ImageBase 0x57C40000
  VA = 0x10000000 + 转储内偏移      ← 与 M2Server 用 flat_image.bin + 0x400000 同构
```

**已脱壳的部分**（用分块熵验证过）：

| 节 | RVA | 大小 | 熵 | 状态 |
|---|---|---|---|---|
| `.text` | `0x0001000` | `0x27BC40`（2.6 MB） | 6.79 | **已脱壳，可反汇编** |
| `.rdata` | `0x027D000` | `0x091154` | 5.67 | **已脱壳，字符串在此** |
| `.data` | `0x030F000` | `0x00D680` | 2.97 | **已脱壳** |
| `.w(d` | `0x15C3000` | `0x15AB3B0` | 7.99 | 原加壳载荷，已无用 |

`+0x400000..+0x1400000` 是未提交零页（全零 43.7%），属正常内存转储形态，不是损坏。

**可用性已实证**，不是「看起来像」：
- 前 4 MB 内有 7,088 个 `55 8B EC`、3,921 个 `83 EC`、751 个热补丁序言 —— 真实 MSVC 代码密度。
  注意 **MSVC Release 默认省略帧指针**，所以不能像 Delphi 那样只数 `push ebp` 来判断代码密度。
- **11,875 条 GBK 中文串，其中 318 条含功能关键词。**
- 字符串 → 交叉引用 → 代码链路打通，示例（基址 0x10000000）：
  `大背包` @ `0x102B0178`（被引用 2 次）、`自定义元素` @ `0x102B005C`（2 次）、
  `火墙修改` @ `0x102BAB08`（4 次）。

**踩过的坑，照抄可省半小时**：
1. **不要用 PE 头的 `ImageBase 0x57C40000`**。内存转储的重定位已按**实际**加载基址应用，
   用错基址会导致所有交叉引用查找返回 0 命中，看起来像「字符串没被引用」。
   反推方法：统计 `.text` 内 `push imm32` 的高位分布，本例 7,827 次聚集在 `0x102xxxxx`，
   而 `.rdata` 在 RVA `0x27D000..0x30E154`，两者对齐即得基址 `0x10000000`。
2. **对话框资源仍然拿不到**：转储里 `DLGTEMPLATEEX` 签名只有 1 处、资源目录解析为 0 项。
   GUI 功能清单继续用生产 `Gs1/config.json`（527 键），那本来就是更好的来源。
3. 另有一份 `yanshen208_strparam_runtime_dump_delayed_20260719\` 熵 **7.925 —— 那份没脱开**，
   别拿错。2.0.7 的 `questinfo_runtime_dump\` 熵 7.842，同样没脱开。

**推论**：眼神此前所有标注「插件实现在 Themida 壳里，无法证伪」的 BLOCKED，
包括回收结算、S 变量 miss 语义、`NPC_CreatMons` 的 12 项怪物属性、战士技能回落路径，
**现在都有一手证据可查，应当重开**。（截至目前这四项已全部解掉或大幅推进。）

**但「已脱壳」不等于「全可读」，这条我原先没写清楚，补上：**
Themida 是**逐函数虚拟化**的。被虚拟化的函数会跳进 `0x400000..0x1400000`，
而**那 16 MB 在这份转储里 100% 全零**。所以：
- 拿不到某个函数时，先确认它是不是跳进了零页，**这与「加壳打不开」是两回事**，
  前者可以靠重抓一份包含 VM 段的转储解决，后者不能。
- 已知落在零页里的：`S(1,1)` 的模式 6/7/8。它不是被轮询的，
  而是眼神在 `0x100CEC39` 给引擎 `SetS`（`0x6DF240`）装了 detour，**detour 本体正是虚拟化那批**。
- 判断依据：`.text` 里出现指向 `0x400000..0x1400000` 的 `call`/`jmp`，且目标区全零。

### 5.1.0.2 S 银行的 miss 语义分两段，不是统一 -1

**本文件此前写的「未设置过的 S 槽读回 -1」范围过宽，会让消费端整体偏一档。** 精确的是：

- **引擎侧**确实预置 -1（`0x6DF1BB` / `0x6E427A`），插件自己的 `GetS` 包装器
  `0x10056040` 在 `0x10056056` 也预置 -1。
- **但插件有一段一次性初始化 `0x100CE4EA`，把 `S(1,9..150)` 全部灌成 `0`**，
  只有 `S(1,1..8)` 才真正停在 -1。
- 消费端一律 `test` + `jle`，即 **`≤ 0` 整段跳过**，所以 -1 与 0 在「是否生效」上同义，
  但在「读回什么值」上不同 —— 凡是把读数直接参与运算的地方必须区分。

**插件访问 S 银行只有一个入口 `0x10056040`**（引擎 `GetS` 的 VA `0x6DF1B4` 全转储仅 3 处引用，
所以「唯一」是可证的），共 **58 个调用点**，眼神真正读写的 S 坐标全集由此反推得出——
184 槽表里**大部分是死的**，排期时不必逐槽考证。

**顺带订正两条以讹传讹的数值**：
- 所谓「4095 分母」实为 `Random(0x1000)`，**分母是 4096**。
- 所谓「`>1000` 统一转固定切割」不存在。同一个二进制里并存三种处理：
  `==1000` 跳过、`>1000` 丢弃、夹到 1000。**不要按统一规则实现。**

### 5.1.0.1 DBServer / GameGate 的脱壳产物同样早已存在

同一类问题：`m-logindb` 把 10 项标成 BLOCKED，理由是「`DBServer.exe` 是 VMProtect 加壳，
不可静态验证」，但产物 2026-08-03 就在：

```
staging\_dbsvr_reunpack_work\dbserver_CODE_live.bin   1,916,928 B  熵 6.433
    对应 DBServer.exe 的 CODE 节 VSZ 0x1D34C8；函数序言密度 5.1/KB
    （对照 M2Server flat_image 0.9/KB）—— 是可逐字节分析的 Delphi 代码
staging\_dbsvr_reunpack_work\DBServer_repaired_20260803.exe   14,708,736 B
staging\_gg_reunpack_work\   GGService 2019 / 2021 / 2025 三版脱壳 + 34 MB flat_image
```

**开工前先 `dir staging\_*reunpack*` 与 `dir staging\*runtime_dump*`，
不要凭 PE 头的加壳特征就判 BLOCKED。**

### 5.1.0.2 眼神怎么接进引擎的：硬编码 VA + 四个包装器 + SetS detour

复现脚本 `staging/_ysimpl/`（只读，基址 `0x10000000`）。**基址是实测出来的**：
`.text` 内 dword 前缀直方图里 `0x102xxxxx` 出现 23,870 次，`0x57Cxxxxx` 一次都没有——
转储自带的 `regions.json` 写着 `0x57C40000`，那是 PE 头的 ImageBase，**不要用**。

眼神**不走插件接口，直接硬编码 M2Server 的 VA**（`.text` 里落在
`0x401000..0xB00000` 的 dword 共 26,292 个）。对 S/V 银行它只有**四个**包装器，
形状一致（`__fastcall(ecx=player, edx=group, [esp+4]=index[, esp+8]=value)`）：

| 插件 VA | 转调引擎 | 证据 |
|---|---|---|
| `0x10056040` | `GetS 0x6DF1B4` | `0x1005604F C7 45 F0 B4 F1 6D 00` → `0x10056067 FF 55 F0 call [ebp-0x10]` |
| `0x10065F00` | `GetV 0x6DF1E4` | `0x10065F16 C7 45 F0 E4 F1 6D 00` |
| `0x10065F40` | `SetS 0x6DF240` | `0x10065F4F C7 45 F4 40 F2 6D 00` |
| `0x100CE200` | `SetS 0x6DF240`，**group 恒为 1** | `0x100CE20C C7 45 FC 01 00 00 00` |

全转储里 `0x6DF1B4` 这个 dword **只出现 3 次**（`0x1002DAA8` 脚本 API 派发、
`0x10056052` 上表包装器、`0x10310FE0` 数据表），所以 **`0x10056040` 是插件读 S 的唯一入口**。
`GetV`/`SetV` 另有一条运行时解析链，把引擎 VA 算进全局指针槽
（`0x1006B0F4 81 C2 E4 F1 6D 00` → `mov [0x1031BC58],edx`），调用形如 `call [0x1031BC58]`——
**只按立即数找交叉引用会漏掉这一路。**

**眼神还 detour 了引擎的 `SetS`**：`0x100CEC39` 处 `push 0x100CEB40 / push 0x6DF246 /
push 0x6DF240 ×2 / call 0x10032C00`（`0x6DF246` = Delphi 6 字节序言之后的续跑点）。

### 5.1.0.3 转储里仍有一部分打不开：Themida 逐函数 VM

`.text` 是脱开的，但**被标记虚拟化的单个函数没有还原**。它们的形状是
`push <.rdata 密文块> / jmp <0x400000..0x1400000 区间>`，例如 SetS detour 本体
`0x100CEB40: 68 70 6C 2C 10 / E9 81 11 B9 00 → 0x10C5FCD2`。
而 **`+0x400000..+0x1400000` 这 16 MB 在转储里 100% 全零**——VM 解释器和字节码都不在。

所以「眼神已脱壳」不等于「眼神全可读」。判 BLOCKED 前要区分两种：
**① 函数是干净原生码 → 必须查，不许再拿壳当借口；② 函数以 `push imm32 / jmp` 跳进零页
→ 永久不可判**，除非重新抓一份把 VM 段也 dump 下来的转储。

### 5.1.0.4 S 银行：插件侧的三条硬契约

- **miss 仍然是 `-1`，插件没有自己的默认值。** 包装器先 `0x10056056 C7 45 FC FF FF FF FF`
  种下 -1，再调引擎（引擎 miss 也是 -1）。
- **消费端一律 `test/cmp <=0 → 整段跳过`**，即作者文档那句「变量小于等于0就无视」
  是**字节确证**，不再是推测。样例：`0x10062D19 85 F6 / 0x10062D1B jle`（`S(7,31)`）、
  `0x10062DD4 85 F6 / 0x10062DD6 7E CA`（`S(1,57)`）。
- **但插件会给角色**「**灌默认值**」：`0x100CE4EA..0x100CE560` 是一段一次性初始化——
  以 `S(1,49) == 0x522`（1314）为「已初始化」标记（`0x100CE4FB 3D 22 05 00 00`），
  否则遍历 `i = 1..150`（`0x100CE50D 81 FE 96 00 00 00 / jg`），
  凡当前值 **为负**（`0x100CE53B 85 C0 / 0x100CE53D 79 20 jns` 跳过非负）就写入：
  **`i < 9` 写 `-1`**（`0x100CE546 6A FF`）、**`i >= 9` 写 `0`**（`0x100CE554 6A 00`），
  `i == 49` 写 1314。
  **推论：`S(1,9..150)` 在插件跑过之后读到的是 `0` 而不是 `-1`，而且这 150 个槽会真的落盘。**
  「未设置一律 -1」只在**引擎**层面成立，写眼神消费端时不能照搬。

关于 `>1000`：**没有统一规则，同一个二进制里并存三种策略**，照抄任何一种都会错三分之二：

| 策略 | 字节 | 样例 |
|---|---|---|
| `==1000` 视作恒等直接跳过 | `cmp v,0x3E8 / je` | `0x10079EC8`、`0x1007A11E`、`0x100880AC` |
| `>1000` 整个丢弃 | `cmp v,0x3E8 / jg` | `0x1007A915`（`S(1,58)`） |
| `>1000` 夹到 1000 | `cmp v,0x3E8 / jle .ok / mov v,0x3E8` | `0x1007B2FF` |

千分比换算恒为 `cvtsi2sd → divsd [0x102C8950]`，而 `0x102C8950` 实测就是 **`1000.0`**
（`00 00 00 00 00 40 8F 40`）。概率族的分母是 **4096 不是 4095**：
`0x1007B269 B8 00 10 00 00 → call [0x1031BCC4]`（Random），命中条件 `v-1 > rand`。

### 5.1.0.5 眼神实际读的 S 坐标只有这些——184 槽的表大半是死的

`0x10056040` 是唯一入口，把它的 **58 个调用点**逐个反汇编解出常量参数，得到的
就是插件原生码读 S 的**全集**：

| group | index |
|---|---|
| 1 | 9 10 11 29 50 51 52 53 56 57 58 60 62 63 69 70 73 95 100 101 129 130 131 175 178 179 180 181 200 201 202 214 216 217 219 220 221 228（38 个） |
| 2 | 1 2 3 4 5 6（主号/英雄那组切割） |
| 6 | 1、2 |
| 7 | 31 + 1 处变址 |
| 8 / 9 | 各 1~2 处变址 |
| 变址 group | index 61（`0x1006D507 lea edx,[ebx+1]`，拾取范围） |

写侧更少，只有三处常量：`SetS(1,110)` `0x100697FF`、`SetS(6,1)` `0x10069844`、
`SetS(6,2)` `0x10090110`，外加 §5.1.0.4 那段 `S(1,1..150)` 初始化。

**据此可以结掉几条一直悬着的归属问题**：`S(1,1)`（禁言）、`S(1,2)`（名字变色）、
`S(1,125)`（多阵营）、`S(1,205)`（永久背包负重）**在插件原生码里一次都没被读过**。
`S(1,61)` 也只以 `ebx+1` 的变址形式出现，不是写死的 group 1。

**但这条推论有边界，别当全称命题用**：被 Themida 虚拟化的函数（§5.1.0.3）里如果
有 S 访问，这个扫描看不见。判定强度是「原生码全集 = 上表」，不是「插件全集 = 上表」。
`S(1,1)` 恰好就落在这个缺口上——它的实现是 SetS detour，而 detour 被虚拟化了。

**2026-08-13 订正：上面这句「全集」不成立，边界比原先写的大得多。**
`0x10056040` 只是**包装器**入口。英雄族的 trampoline **绕过包装器直读
`[player+0x804]`**——而 `+0x804` 正是原生 S 银行指针（`GetS 0x6DF1CF`、
`SetS 0x6DF26D`、存档 codec 都读它）。所以 58 个调用点解出来的是
「经包装器读 S 的全集」，不是「插件读 S 的全集」。

两个缺口叠加（Themida 虚拟化 + trampoline 直读），这张表**只能用来证明某坐标
被读过，不能用来证明某坐标没被读过**。凡是基于「表里没有 → 没人读」做出的删除
判定，全部作废重来。

**DBServer 转储的基址已定案：`VA = 0x401000 + 转储内偏移`。** 三条独立判据
（`staging/dbsvr_re/q01_verify.py` 可复跑）：PE 节表 `CODE VA=0x00401000 VSZ=0x1D34C8`，
向上对齐 `0x1000` 得 `0x1D4000` = 转储长度；`jmp dword[imm32+reg*4]` 表项落在转储内
`0x401000` 命中 91.7%，`0x400000`/`0x402000`/`0x10000000` 全是 **0%**；
按此基址三个分发器臂都解出自洽的 Delphi。**用 `0x400000` 会让全部交叉引用 0 命中，
看起来像「证据不存在」——这个坑已经踩过一次。**

工具链在 `staging/dbsvr_re/dbs.py`（capstone，`BASE = 0x401000`）。
入口：type-1 `sub_59889C`、type-2 `sub_599860`、type-3 `sub_599DB4`；
连接角色字节 `[conn+0x40A0]`，`9` = DBTool，其余 = GameServer 序号，由 type-2 `0x003D` 设置。
结论见 `docs/m_dbsvr_impl_20260813.md`。

> **教训（2026-08-13）**：仅凭「原版 M2Server 的分发器不认某个码」就判 `INVENTED`
> 是不成立的推理 —— **认不认在接收侧，发不发在发送侧**。据此判过的
> `0x0073/0x0075/0x0076/0x006D/0x0059/0x00C9/0x0071` 七个码，用 DBServer 字节全部翻案为
> `FAITHFUL`。判 `INVENTED` 前必须**两侧都扫**。
- **`.rsrc` 里没有对话框资源，这条已被逐字节复核，不要再翻案。** 主代理曾据「`.rsrc`
  熵 3.575、未加壳」推断可以静态提取 GUI，并据此派了任务——**推断是错的，上一条才是对的**。
  资源目录（文件偏移 `0x15ACC00`）只有 4 个 ID 条目：`03`(ICON) / `0E`(GROUP_ICON) /
  `10`(VERSION) / `18`(MANIFEST)，`NumberOfIdEntries=4`、named=0，**`05`(RT_DIALOG) 与
  `06`(RT_STRING) 都不在**。全文件 22,999,040 字节里 `DLGTEMPLATEEX` 签名 `01 00 FF FF`
  **0 命中**，`IMAGE_RESOURCE_DIRECTORY` 结构**全文件仅 1 个**（不存在被搬走的第二棵资源树）。
  熵 3.575 属实，但低熵只是因为该节几乎全是一张 ICON 位图——**低熵不等于有可用资源**。
- **2.0.8 的功能开关权威源是生产配置，不是 GUI 转储。** 生产部署
  `D:/光头卧龙/mud2.0/Mir200` 的 `ys/version.ini` 内容即 `yanshen2.0.8.dll`，所以那几份
  配置**是 2.0.8 自己写出来的**，共 **527 个键**（顶层 218 个 0/1 开关 + 子系统 68 个
  `_是否勾选`）。这比 GUI 转储强一档——转储属 2.0.7 家族。合并去重后原生开关全集 **311 个**。
  **全部 GBK 编码**（C# 已按 codepage 936 读，不要"修"成 UTF-8）。
- **带 `a` 后缀的配置键（`宠物吸血a`、`概率格挡a` 等）是原生约定，不是 C# 私自加的。**
  曾被判成 INVENTED，实测原生 `config.json` 里就是带 `a` 的。
- **标签名 ≠ 配置键名，据标签找不到就判 MISSING 会误报。** 已知两例：
  `超级攻击触发` 的真键是 `super攻击触发`、`高级主号倍功暴击` 的真键是 `主号高级暴击`。
- **人物存档 section type `0x79` 是一道会毁数据的单向门（YS-D1），动它之前先看清。**
  C# 把眼神物品扩展塞进人物 ScriptData 的 `0x79` section。**加载侧是安全的**：原生跳表
  只有 9 项（`0x6E4510 cmp eax,8` / `0x6E4513 ja 0x6E4856`），越界臂记一行日志后按
  `0x6E48C6 lea eax,[edx+eax+7]` 跳过。**存档侧是致命的**：人物编码器
  `0x6E4DD3..0x6E4ECF` 只按内存态重建 6 个固定 section，**没有任何 carry-over 缓冲**。
  所以只要 GameSvr 换回原版 Delphi 跑一次，玩家上下线后 `ys1..ys17` / `jp1..jp6` /
  装备出处**永久清零且无提示**——把「逐个组件替换、可回退」变成了单向门。
  既不能保留现状当没事，也不能直接删（当场丢数据），需要配套迁移方案，属 §1.4。
- **英雄 dynData 的 type-7 carrier 是忠实的，别"修"它。** 原生确实会把 selector 不符的
  记录原样搬进 carry-over（`0x68AEC8 mov ecx,0x10` → `obj+0x688`）并在存档时回写。
  但保命的是 **tag 字节 13..15 恒非零**，原生比的是 +12 处**整个 DWORD**（`0x68AE73`），
  不是一个字节——谁把那三个 tag "简化"掉，就会静默丢数据。
  另注：**`type 7` 在人物容器里步长是 12，在英雄容器里是 16**，这不是 bug，
  不要把 `RecordSize` 统一成 12。
- **因此眼神按「接口复刻」而非「实现复刻」。** 权威证据源按可信度排序：
  随包 `AllFuc.pas`（**已解密**，27 字节重复 XOR，密钥见 `staging/_ys208_plain/`，
  并已被生产日志的报错行列号独立验证）> `RunQuest.pas` 回调声明 > 官方使用例子与更新日志
  > 生产配置与生产脚本 > GUI 控件枚举。
- **生产网关的流量计数器是判定 ident 死活的一手证据，此前没人用过。**
  `D:/光头卧龙/mud2.0/GateServer/GameGate2/procMsgLog/` 下的 `client_AppearTimes.ini`
  与 `srv_AppearTimes.ini` 是**真实线上每个 ident 的出现次数**。
  静态字节说明「原生认不认」，这两个文件说明「线上到底发不发」，两者互补，缺一不可。

### 5.1.0.6 眼神改写宿主有**三**种手段，图谱里只记了两种

除了「常量改写」和「指令改写」，还有第三种，而且是英雄/宠物域绝大多数
「使能支」的实际所在：**trampoline**。

- 安装器两个：`0x10032CC0`（71 个站点）、`0x10032FD0`（30 个站点），共 **101 处**。
- 桩体不是 memcpy 出来的，而是从 `.rdata` 里的 **dword 数组模板**拼装的。
  模板是纯数据，**没有被 Themida 虚拟化**，所以可以直接解码。
- `staging/_ys_swc/q06_tramp.py` 已解出其中 93 个，建议并进图谱。

只按「找 memcpy / 找改写的字节」去数补丁站点，会漏掉这 101 处里的绝大部分，
从而得出「这个开关没有补丁站点、所以是死键」的错误结论。

### 5.1.0.7 眼神**没有**「按名注册脚本函数」这个机制

此前一直假设插件把 `YsXxx` 之类的名字注册进某张表，于是反复有人去找那张表、
找不到就标 BLOCKED。**那张表不存在。**

插件劫持 `TPlayObject.GetBagItemCount(string): Integer`，把它当命令通道用：
参数是 `!!!!` 开头的魔法串，返回值就是 `GetBagItemCount` 的返回值。
钩子在 **`0x1005E4D0`**，按顺序匹配 9 个前缀。**合计 86 个原生入口**：

| 类型 | 数量 | 分发器 | 上界判定 | 跳表 |
|---|---|---|---|---|
| 数字型 `!!!!集成函数,<id>,` | 41 | `0x100761A0` | `0x100766F0 83 BD AC FE FF FF 28  cmp …,0x28` / `ja` | `0x10077A78` |
| caret 型 `!!!!爱心分割^<id>^` | 38 | `0x1005DBA0` | `0x1005DD0A 83 FF 25  cmp edi,0x25` / `ja` | `0x1005E3D8` |
| 中文隧道 | 7 | 内联在钩子里 | — | — |

几条直接可用的结论：

- **caret 的上限就是 38，没有第 39 项**，这独立证实了「2.08 只多 caret 38」。
- 字段不足时返回 **-888**（`B8 88 FC FF FF`），不是 0 也不是 -1。
- `AllFuc.pas` 只用到 40 个数字型（跳过 6），但**插件把 6 也实现了**（`0x10070BF0`）。
  拿 `AllFuc.pas` 的用量当插件实现上限会低估。
- **生产脚本 100% 走隧道路径**，C# 里那些「内置名」路径在真实部署里根本不可达。
  所以隧道侧的参数顺序错误比内置名侧的错误严重得多——已经踩过一次：
  元素隧道把 `(元素类型, 装备位)` 传成了 `(装备位, 元素类型)`。

**被这条推翻的既有材料**：`yanshen_scriptapi_20260813.md` 把「插件直接注册的原生
YsXxx 名 20 个」计入总面数——该机制不存在；同一份材料把壳称作 VMProtect，
与本文件已定案的 Themida 冲突。`allfuc208_tunnels.txt` 只列了 AllFuc 用到的
16 caret / 40 数字，容易被误读成插件的实现上限（实际是 38 / 41）。

### 5.1.2 删 INVENTED 之前必须先查流量计数器（血的教训）

主代理已用计数器逐项核过协议审计的 INVENTED 名单，结果**两个方向截然不同**：

- **SM 侧 20 项：真实流量中 0 项出现** —— 名单可信，可以删。
- **CM 侧名单里有 3 项正在线上跑**：
  `1018` **50,911 次**、`3202` **5,499 次**、`1271` **217 次**。
  这三项**不许按名单批量删**，必须逐个裁决。「原生不分发」与「客户端不发」是两回事：
  客户端照发、原生照忽略是完全可能的，此时删掉 C# 处理分支才是对的；
  但也可能是静态扫描漏了分发路径。**没查清楚之前不许删。**

**规矩：任何 ident 的删除，提交信息里必须同时给出「静态零命中」与「流量计数器未出现」两项证据。**
只有静态证据就删，等于拿线上功能赌扫描器的完备性。

### 5.1.3 攻击族 ident 之争：线上无影响，按忠实度改即可

真实客户端流量里，扩展攻击族**只有 `3024`（270 万次）和 `3025`（5.3 万次）**。
`3026 / 3027 / 3028 / 3035` 与 C# 现用的 `3036 / 3037 / 3038` —— **七个全部一次都没出现**。
所以这组编号改与不改，对该部署**零可观测影响**：按原生改成 3026/3027/3028 是提升忠实度，
不是修线上故障，不必当紧急项。注意 `3027` 在原生走**独立处理器 `0x6D9F4B`**，
不是共享动作处理器，不是改个数字就完事。

（订正：曾有报告称「客户端发过 3028」，实测 `client_AppearTimes.ini` 里**没有 3028**。）

**这同时关闭了 CM 代理标记的 B1「改号会不会打挂手游客户端」。** 它的顾虑是：
若手游客户端是照着 C# 的错号写的，改成 3026/3027/3028 会让攻击失效，并建议合并前抓包。
不需要抓包——计数器已经给出答案：**两套编号客户端一个都不发**，只发 3024/3025。
所以改号在两个方向上都不可观测，**不是上线风险点**。

### 5.1.5 攻击族三个号语义各不相同，不是「改个数字」

（CM 代理逐条反出来的，落地时务必照做，否则会在原生不出手的 ident 上放技能）

- **3026** → 动作码 1018 → 臂 `0x77092A` → `call 0x771BB8`，而 `0x771BB8` 全函数是
  `55 8B EC 33 C0 5D C2 04 00` —— **空桩恒返回 FALSE**。唯一效果是
  `0x7707E3 mov [esi+0x154],al` 转向。
- **3028** → 字节索引表 `0x6EC178[26]=0x0B` → slot 11 = `0x6EC2D7`，那是 `0x6EC078`
  自己的尾巴，**连动作执行器都不进**，朝向也不改。
- **3027** → 独立处理器 `0x6D9F4B`，与共享臂只差一条指令
  （`0x6D9F9B mov dx,[msg+8]` 取 **Tag** vs `0x6D9EFF` 取 Ident），
  是「客户端自己点名动作码」的通用包。
- 由此：**抱月刀原生只能靠 `3027 + Tag=1011/1012` 触发**
  （`[esi+0xBC]` ← 魔法 34 = SKILL_CROSSMOON）；
  **莲月（魔法 38）在这条链路上根本没有缓存槽位**。

### 5.1.6 「原生认识」≠「原生有处理器」：544 vs 311

CM 分发树里有 **311 个真实处理器**，另有 **233 个「跳转表有槽位但指向 default 标签
`0x6DBC2C`」的占位项**，合计 544 个可达 ident。前序报告把两者混进同一张表，导致
**9 项被误判 INVENTED**：`1018 / 1107 / 2003 / 3202 / 4423 / 4481 / 4644 / 15999`
以及 `42` 的 ack 分支。它们的 C# 分支体是纯 `break;`，**复刻的正是 native default 的
静默丢弃，属 FAITHFUL**；照原报告去删会连带删掉三个审计工具钉住的正确契约。

值得注意的是这批里的 `1018 / 3202 / 1271` 正是 §5.1.2 用流量计数器拦下来的三项——
**字节分析与线上流量两条独立路径得出同一结论**。

### 5.1.4 计数器顺带印证的两条

- **`SM 3555` 出现 4,509,810 次** —— 计时能力状态帧是真实且高频的。
  曾有报告把它判为 INVENTED 建议删除，那个判断是错的（该代理自己也已用字节推翻）。
- **`SM 629`（ACT_GOOD）6,921 万次、`SM 630`（ACT_FAIL）108 万次** —— 这对确实是热路径。
- `SM 3420` 与 `SM 12326` **都未出现**，所以定点坐标回包那次改动**得不到流量佐证**
  （定位石是低频功能，该计数器覆盖期内没人用）。它仍然只由字节证据支撑。
- **那份"来路不明"的 GUI 转储已定年，可以放心用。** 它是 **2025.05.08 版的
  yanshen2.0.7.dll**：转储里能命中 `BB_Attact`(2025.01.23)、`My_ShangHai`(2025.02.16)、
  `角色爆出物品全区提示`(2025.05.08)，而 2025.08.11 与 2026.04.26 两批共 9 项功能
  **全部 0 命中**。所以它是真实的 2.0.7 家族构建，只是磁盘上没有这一份
  （SizeOfImage `0x1B21000`，与三份 2.0.8 的 `0x2BB3000` 和两份 2.0.7 的
  `0x1A01000`/`0x29C2000` 都不同）。
- **相对 2.08 的已知缺口因此收敛成两条日志条目 + 4 天空白**：
  2025.08.11（沙巴克武器升级防破碎、上线复活时间修复、宠物无视隐身 `s(1,221)`、
  新魔法盾修正 `s(1,57)`）、2026.04.26（聚灵珠解绑、屏蔽原生死亡爆装、护身伤害比例
  `s(1,224)`、变量判断免战 `s(1,227)`、怪物爆出次数 `s(1,225)/(1,226)`），
  外加日志止于 04-26 而 DLL 编译于 04-30 的 4 天。**更新日志里从未出现过"2.0.8"字样**
  —— 作者把 2.0.7 一路增量更新、某次改了文件名，存在的是日期边界不是版本边界。
- **C# 覆盖率不要引用 81.9% 那个数。** 那是按"中文标签是否出现在某个 .cs"算的，而
  `YanshenApi.cs` 塞满了死代码访问器和帮助文案。实测：595 个 public 成员里 **445 个
  （74.8%）全库零引用**，229 个功能开关键里 **190 个（83%）无任何活消费者**，真正可达
  的只有 39 个。**判定覆盖度要看有没有活消费者，不能看标签出现过没有。**
- **随包那份 S 变量表有作者笔误，而 C# 是对的。** 千分比属性 152–161 / 164–173 共 20 个
  槽位，随包 `.txt` 把"最大/最低"写反了，DLL 内置帮助文本（RVA `0x2A27D8`）是另一种排法，
  C# 抄的是 DLL 那版。**不要照着随包文档去"修" C#。**
- **生产实测只用到 125 个 API 里的 5 个**，且 100% 经 `{$I AllFuc.pas}` 封装调用，
  从不裸调内置名。所以 C# 那套内置名派发层在真实部署里不可达，相关缺陷（如参数错位）
  实际影响面为 0，排优先级时不要被数量吓到。

### 5.1.1 已被证伪的眼神材料（照抄会把实现带偏）

- **`MyJson/bags.bin` 这个字符串在转储里根本不存在。** `yanshen_survey` 把 RVA `0x2AA850`
  记成 `MyJson/bags.bin` 是错的（全表扫 `.bin` 零命中）。真实是**每角色一个文件**
  `Gs1/MyJson/bags/<角色名>.bin`。照错版实现会做成全服共用单文件，方向完全错。
- **`yanshen_config_recycle` 报告的整个回收章节写的是沙箱数据，一条都不能照抄。**
    最初只发现 `无限背包_是否固定` 一个键错（报告说 `"V变量控制格子"`，生产实测
    **`"固定格子"`**，`变量v1`=10 不是 20、`变量v2`=1 不是 188）。后续逐文件比对表明
    **整章都是 `_ys208_runtime` 插件沙箱的内容**：两份 `recycle.json` 连一个变量号都不相同。

    | | 生产（md5 `af897884`，11514 B） | 报告/沙箱（`94adb8ba`，7868 B） |
    |---|---|---|
    | 类型名 | 11 个中文名 | 12 个 `类型N` |
    | 总开关 | `{10,1..10}` | `{100,n}` |
    | 倍率 | `{10,199}` | `{101,n}` |
    | 极品 / 元素 | 125 / 126 | 201-212 / 301-312 |
    | 产出 | `其他` 发声望，**灵符全 0** | 只产灵符 |

    照报告实现会把**每一条规则都指向错误的变量**。
    **凡涉及眼神配置，一律以 `D:/光头卧龙` 生产文件为准，先算 md5 确认不是沙箱那份。**
    佐证：`V(10,1)` 在生产里是「商店装备」回收开关（只取 0/1），拿它算背包格子数显然
    不是本意，反证生产确实走 `固定格子` 分支。
- **大背包 `.bin` 里的 `01 FF 00 7A` 不是记录分隔符。** 它是
  「`0x54=01, 0x55=0xFF, 尾部全零`」这种记录形状经零游程编码后的巧合串，按它切分会漏记录
  （两个样本里它一次都不出现）。正确容器格式见
  `staging/yanshen_bigbag_format_20260813.md`：零字节游程编码（`00 XX` = XX 个零）
  外层 + 16 字节全零头 + N × 208 字节定长记录，31/31 样本可逐字节往返。

### 5.2 眼神回收系统：修复必须按序，乱序会删玩家装备

C# 侧回收当前是**整体静默失效**（`initys` 在生产树 3326 个文件里 0 命中，
`Enabled("高级回收")` 恒 false 直接返回；配置解析异常又让快照恒为 null）。
所以"只删不给"这个缺陷目前**不会触发**。

**危险在于修复顺序**：一旦修好前两道门而没修开关与产出，删装备的闸门就打开了——
生产配置里 313 件装备名（含 124 个 `★` 极品变体、24 本技能书）会被**无偿删除**，
且因极品/元素开关未实现，玩家刻意保留的极品装也一并删掉。

**必须按 门① → 门② → 配置快照保留完整类型规则 → 总开关/极品开关/元素开关 →
产出结算与倍率 的顺序推进，不许只修前两道就收工。**

产品代码只有 **GameSvr** 在交付路径上（用户线上跑的是原版 Delphi 的
DBServer / LoginGate / GameGate，只有 GameSvr 换成 C#）。

---

## 六、代理工作流

1. **建自己的隔离工作树**（如果需要写代码）：
   `git -C D:/loym2/LyoMir2-master worktree add -b agent/<名字> D:/loym2/wt/<名字> <基线SHA>`
   只读代理不需要建，直接读 `pois11-fix`。

2. **先读现成材料再动手**：相关的 `AuditTools/*/Program.cs` 里的断言往往已经是一份
   免费的契约清单；`staging/` 下可能已有前人的分析文档。

3. **复核一切**：台账的 evidence、代码注释、提交信息、前人报告，都要用字节复核。

4. **产出写 UTF-8 文件**，不要依赖控制台。

5. **报告必须包含**：
   - 判定计数（各判定各多少）
   - `DIVERGENT` / `MISSING` / `INVENTED` **完整清单，直接列在回复正文里**（不要只说"见文件"）
   - 每项的原生证据（VA + 字节）、C# 位置（文件:行）、玩家可见后果、最小修复方案
   - `BLOCKED` 项及各自缺什么证据
   - 建议的优先级排序及理由
   - 你在复核中发现的**台账/注释/前人结论的错误**（这部分价值很高，务必写）

6. **敢于推翻**。如果字节显示"台账错了、C# 是对的"，就这么写。
   照着错误的台账去"修"会把对的改坏——这种事已经发生过。

---

## 七、给主控的补充

- 集成代理分支时逐个 cherry-pick + 构建验证，**串行**。
- 冲突裁决要回到字节，不要默认取 incoming。
- 代理之间可能对同一处得出不同结论，以字节证据强的为准；
  代理主动说"对方比我好"的判断通常可信。
- 落地前后各跑一次相关审计工具，用 PASS/FAIL 变化量化改善。
